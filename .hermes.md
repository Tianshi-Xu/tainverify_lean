# TrainVerify remote execution authority

This repository is the complete working context for the dedicated remote TrainVerify Hermes agent. Read `AGENTS.md` before editing; its Lean/proof-engineering rules remain mandatory. Also load the installed TrainVerify/Lean/release skills whenever relevant.

## Current authority and exact handoff

- Start from the actual checked-out commit and audit it before any edit. The expected migration base is `7fb53c40` on `feat/gpt2-new-compiler-spike`; recent bounded-proof commits are `4250ae5c`, `65b3c0c8`, `cc66071a`, `e721cb6b`, `7fb53c40`.
- Preserve the already-complete GPT-2 terminal chain. The active mission is YOCO fresh authority → generic compiler → five public contracts, then release closure.
- Continue the current serial cleanup of 500k-heartbeat proof families. Do not redo completed work or weaken theorem statements/contracts.

## Non-negotiable resource boundaries

- Lean toolchain is Lean `4.32.2`.
- Export `LEAN_NUM_THREADS=1`. At any moment there may be at most **one** Lean compiler/build process. Before starting Lean, inspect for an existing repo-local Lean/lake process; serialize and wait rather than overlap.
- Every declaration must elaborate with `maxHeartbeats` in `1..500000`. Unlimited/zero and values above 500000 are forbidden.
- Never run aggregate `lake build`. Use the narrowest module/file target and incremental cache. Do not launch GPU training merely to validate setup.
- Long commands must have bounded execution, durable logs/receipts, and exact PID/session ownership. Do not infer success from silence or stale `.olean` files.

## Compiler and contract invariants

- The generated proof source must remain below 2.5 MB.
- Generated/released source must contain no `sorry`, `sorryAx`, `admit`, `axiom`, `unsafe`, or `False.elim` (comments/allowlisted upstream declarations must be handled only by the repository's canonical audited policy; never weaken the scanner).
- Preserve ordered `pm_tids` and dynamic `K`; never sort away semantic order or hard-code K.
- Publication must be deterministic and atomic. Failure must preserve the previous destination and leave no backup or staged/temp orphan.
- Perform a second clean regeneration and byte-compare all released artifacts.
- The five public theorems/contracts and the joint-witness axiom receipt are mandatory release evidence. Validate theorem statements, not merely symbol names.
- Preserve the schema-3 baseline and reject stale/older schema receipts.
- Fresh Python/source authority must flow into the generic compiler; do not hand-patch final generated proof output as a substitute for compiler correctness.

## Required release sequence

1. Audit migration integrity: record `pwd`, branch, exact HEAD/tree, clean status, toolchain, relevant skill availability, and current active processes. Confirm no other Lean process before any Lean command.
2. Read the YOCO regen README, current receipts/tests, and the recent commit chain. Resume from the first genuinely incomplete gate.
3. Use test-driven, narrow changes. Verify each proof family serially under 500k before moving on.
4. Run the canonical fresh-authority and generic-compiler pipeline under the resource limits. Persist exact command, input hashes/sizes, inner exit status, output paths, and logs.
5. Run all source-policy, five-public-contract, joint-witness/axiom, schema-3, atomic-publication, determinism, and clean second-regeneration gates.
6. Request an independent **exact-diff** final review against an immutable candidate commit/tree. Reproduce and resolve every BLOCK finding; do not accept summary-only review.
7. Commit only explicit intended paths after inspecting the staged diff. Push `main` **only after every gate is green**, independent review is closed, and the final tree/status are rechecked. Do not push the feature branch as a substitute.

## Acceptance preferences distilled from project history

- Upstream semantic fidelity outranks downstream proof success. Compare every Denote/operator/compiler assumption against Python authority before trusting downstream theorems.
- Every new sharding-commute axiom-like boundary must instead be justified or exposed as an explicit contract hypothesis, and shape/consistency witnesses must be run immediately. No vacuous proof chain.
- `#print axioms`/receipt checks are required for public proof closure; a green build alone is insufficient.
- `.olean` timestamps and provenance must postdate the current run; stale cache is not evidence.
- Prefer direct lemma application to broad `simp`; broad elaboration frequently violates heartbeat limits.
- Do not interrupt for routine choices. Work autonomously through the ordered gates; stop only on one precise, irreducible blocker and report exact evidence.
- Never stash/reset/checkout away concurrent work. If the tree changes unexpectedly, audit ownership and fail closed.

## Durable reporting

Maintain a timestamped log under `.hermes-runs/` (ignored or explicitly excluded from release artifacts) with commands, exit codes, hashes, PID/process checks, and gate receipts. Final report must identify the exact candidate commit/tree, generated artifact hashes, five theorem checks, axiom receipt, schema, determinism comparison, independent review result, and remote-ref readback if `main` is pushed.
