/- Auto-generated by Verdict/graph_to_lean.py --split-goals
    Goal: 5 (tensor id: 4680) cut_to_full bridge.
    Uses denoteGraph_slice_agrees (op-generic) — zero op-specific handwriting.
-/
import denote.goals.Goal_5
import denote.GraphSlicing

set_option linter.style.longLine false
set_option maxRecDepth 100000
set_option maxHeartbeats 1600000

namespace TrainVerify.Denote.GeneratedGoals
open TrainVerify.Denote TrainVerify.Denote.Generated

-- Inline nodup facts (per-file; a future GraphFacts.lean can dedupe these).
private theorem local_sm_nodes_nodup_5 : sm.nodes.Nodup := by native_decide
private theorem local_pm_nodes_nodup_5 : pm.nodes.Nodup := by native_decide

theorem sm_goal_5_sublist : List.Sublist sm_goal_5.nodes sm.nodes := by decide
theorem pm_goal_5_sublist : List.Sublist pm_goal_5.nodes pm.nodes := by decide
theorem sm_goal_5_numRanks_eq : sm.numRanks = sm_goal_5.numRanks := by native_decide
theorem pm_goal_5_numRanks_eq : pm.numRanks = pm_goal_5.numRanks := by native_decide

def sm_goal_5_tid_set : List Tid := [4677, 4679, 4680]

theorem graphTids_sm_goal_5_subset :
    ∀ tid, graphTids sm_goal_5 tid → tid ∈ sm_goal_5_tid_set := by
  intro tid htid
  rcases htid with ⟨n', hmem, hins⟩ | ⟨n', hmem, houts⟩
  · simp only [sm_goal_5, List.mem_cons, List.not_mem_nil, or_false] at hmem
    subst hmem
    simp only [List.mem_cons, List.not_mem_nil, or_false] at hins
    rcases hins with rfl | rfl
    all_goals simp [sm_goal_5_tid_set]
  · simp only [sm_goal_5, List.mem_cons, List.not_mem_nil, or_false] at hmem
    subst hmem
    simp only [List.mem_cons, List.not_mem_nil, or_false] at houts
    subst houts
    simp [sm_goal_5_tid_set]

theorem sm_goal_5_no_interference :
    ∀ n ∈ sm.nodes, n ∉ sm_goal_5.nodes →
      ∀ tid, graphTids sm_goal_5 tid → tid ∉ n.outs := by
  have hkey : ∀ n ∈ sm.nodes, n ∉ sm_goal_5.nodes →
      ∀ tid, tid ∈ sm_goal_5_tid_set → tid ∉ n.outs := by
    unfold sm_goal_5_tid_set
    native_decide
  intro n hn hn_notlocal tid htid
  exact hkey n hn hn_notlocal tid (graphTids_sm_goal_5_subset tid htid)

def pm_goal_5_tid_set : List Tid := [4677, 4680, 7389, 7390, 7391, 7392]

theorem graphTids_pm_goal_5_subset :
    ∀ tid, graphTids pm_goal_5 tid → tid ∈ pm_goal_5_tid_set := by
  intro tid htid
  rcases htid with ⟨n', hmem, hins⟩ | ⟨n', hmem, houts⟩
  · simp only [pm_goal_5, List.mem_cons, List.not_mem_nil, or_false] at hmem
    rcases hmem with rfl | rfl | rfl
    all_goals
      simp only [List.mem_cons, List.not_mem_nil, or_false] at hins
      simp [pm_goal_5_tid_set] at *
      tauto
  · simp only [pm_goal_5, List.mem_cons, List.not_mem_nil, or_false] at hmem
    rcases hmem with rfl | rfl | rfl
    all_goals
      simp only [List.mem_cons, List.not_mem_nil, or_false] at houts
      simp [pm_goal_5_tid_set] at *
      tauto

theorem pm_goal_5_no_interference :
    ∀ n ∈ pm.nodes, n ∉ pm_goal_5.nodes →
      ∀ tid, graphTids pm_goal_5 tid → tid ∉ n.outs := by
  have hkey : ∀ n ∈ pm.nodes, n ∉ pm_goal_5.nodes →
      ∀ tid, tid ∈ pm_goal_5_tid_set → tid ∉ n.outs := by
    unfold pm_goal_5_tid_set
    native_decide
  intro n hn hn_notlocal tid htid
  exact hkey n hn hn_notlocal tid (graphTids_pm_goal_5_subset tid htid)

theorem goal_5_cut_to_full (h : goal_5_stmt_cut) : goal_5_stmt := by
  intro initSM initPM hSM hPM hInit
  have hSMcut : StoreShapesHold initSM sm_goal_5InitEnv := by
    intro tid sh hsh
    rw [sm_goal_5InitEnv] at hsh
    have hmem := mem_of_shapeEnvOfList_eq_some hsh
    simp only [sm_goal_5InitShapes, List.mem_cons, List.not_mem_nil, or_false, Prod.mk.injEq] at hmem
    rcases hmem with ⟨rfl, rfl⟩ | ⟨rfl, rfl⟩
    · exact hSM 4677 [4096] (by native_decide)
    · exact hSM 4679 [154880, 1024] (by native_decide)
  have hPMcut : StoreShapesHold initPM pm_goal_5InitEnv := by
    intro tid sh hsh
    rw [pm_goal_5InitEnv] at hsh
    have hmem := mem_of_shapeEnvOfList_eq_some hsh
    simp only [pm_goal_5InitShapes, List.mem_cons, List.not_mem_nil, or_false, Prod.mk.injEq] at hmem
    rcases hmem with ⟨rfl, rfl⟩ | ⟨rfl, rfl⟩ | ⟨rfl, rfl⟩
    · exact hPM 4677 [4096] (by native_decide)
    · exact hPM 7389 [77440, 1024] (by native_decide)
    · exact hPM 7390 [77440, 1024] (by native_decide)
  have hInitCut : InitGoalsHold pm_goal_5.numRanks goal_5_cut_initGoals initSM initPM := by
    simp only [goal_5_cut_initGoals]
    have hnr : pm_goal_5.numRanks = pm.numRanks := pm_goal_5_numRanks_eq.symm
    rw [hnr]
    exact hInit
  have hcut := h initSM initPM hSMcut hPMcut hInitCut
  have h_agree_sm : StoreAgreesOn initSM initSM (graphTids sm_goal_5) := fun _ _ => rfl
  have h_agree_pm : StoreAgreesOn initPM initPM (graphTids pm_goal_5) := fun _ _ => rfl
  have h_sm_slice : denoteGraph sm_goal_5 initSM 4680 = denoteGraph sm initSM 4680 := by
    apply denoteGraph_slice_agrees sm sm_goal_5 sm_goal_5_numRanks_eq sm_goal_5_sublist
      local_sm_nodes_nodup_5 initSM initSM h_agree_sm sm_goal_5_no_interference 4680
    refine ⟨{ rank := 0, op := "OpName.FW_embedding", ins := [4677, 4679], outs := [4680] }, ?_, ?_⟩
    · simp [sm_goal_5]
    · simp
  have h_pm_slice_4680 : denoteGraph pm_goal_5 initPM 4680 = denoteGraph pm initPM 4680 := by
    apply denoteGraph_slice_agrees pm pm_goal_5 pm_goal_5_numRanks_eq pm_goal_5_sublist
      local_pm_nodes_nodup_5 initPM initPM h_agree_pm pm_goal_5_no_interference 4680
    refine ⟨{ rank := 0, op := "OpName.AllReducePrim", ins := [7391, 7392], outs := [4680] }, ?_, ?_⟩
    · simp [pm_goal_5]
    · simp
  simp only [goal_5, CoarseLineageHoldsWithInit] at hcut
  simp only [List.map, goal_5] at hcut
  rw [h_sm_slice, h_pm_slice_4680] at hcut
  simp only [List.map]
  exact hcut

-- If prove_goal_{gid}_cut is available, uncomment `goal_{gid}_intermediate`:
/-
theorem goal_5_intermediate (initSM initPM : Store)
    (hSM : StoreShapesHold initSM smInitEnv) (hPM : StoreShapesHold initPM pmInitEnv)
    (hInit : InitGoalsHold pm.numRanks initGoals initSM initPM) :
    InitGoalHolds pm.numRanks goal_5 (denoteGraph sm initSM) (denoteGraph pm initPM) := by
  have hfull : goal_5_stmt := goal_5_cut_to_full prove_goal_5_cut
  have := hfull initSM initPM hSM hPM hInit
  unfold InitGoalHolds
  simp only [goal_5]
  exact this
-/

end TrainVerify.Denote.GeneratedGoals
