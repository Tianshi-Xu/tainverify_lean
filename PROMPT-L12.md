# L12 zigzag-band pilot — Iroha task for Copilot

## Context

You are working in `/tmp/tv-l12-pilot` on branch `iroha-l12-pilot` of the
`trainverify` Lean project (owned by iroha, the user's AI assistant).

The file `denote/yoco_goals/Pattern_3.lean` currently proves
`sm_pm_router_commute_L{k}` for **k = 0..11** (the sliding-window attention
band). Layers **L12..L23** are the zigzag-band and have NOT been done.
Your job is L12 only. Layers L13..L16 will be dispatched to sibling workers
after you succeed.

**Golden rule (子鱼's standing order):** upstream fidelity first. Never
introduce a `sorry` or an axiom. All commutes must be kernel-clean
(`#print axioms X` returns `[propext, Classical.choice, Quot.sound]`, plus
`Lean.ofReduceBool + Lean.trustCompiler` only if `native_decide` is used).

## Existing pattern (fully worked)

`sm_pm_router_commute_L{k}` for k=2..11 is generated by the elab macro
`mk_router k` at Pattern_3.lean:11536. The commute assembly chain per layer
is:

  qproj_Lk (mk_qproj)  ─┐
  kproj_Lk (mk_kproj)   ├── attention_Lk (mk_attention) ─┐
  vproj_Lk (mk_vproj)  ─┘                                 │
  rms_Lk   (mk_rms)                                       ├── carry_Lk
  qlin_Lk  (mk_qlin)                                      │   (mk_carry_a
  klin_Lk  (mk_klin)                                      │    + mk_carry_b)
  pm_attn_shard_shapes_Lk (mk_pm_attn_shard_shapes)      │
  nl_Lk    (mk_nl)                                        │
  gate_mul_Lk (mk_gate_mul)                               │
  moe_gmm_Lk  (mk_moe_gmm)                                │
  router_commute_Lk (mk_router)  ←─────────────────────── L{k} main

All `mk_*` macros live in Pattern_3.lean between lines 10140 and 13455.
`mk_attention` at line 13455 is the key one — it hardcodes
`applyNodeRingAttn_sliding_window_of_singleton` and
`applyNodeRingAttn_sliding_window_out`. Everything else is op-agnostic.

## Denote layer facts (already true, don't re-derive)

`denote/Denote.lean` has the FULL zigzag counterpart to sliding_window:

- `applyNodeRingAttn_zigzag`                          def   line 21133
- `applyNodeRingAttn_zigzag_singleton`                thm   line 21443
- `applyNodeRingAttn_zigzag_of_singleton`             thm   line 21559
- `applyNodeRingAttn_zigzag_out`                      thm   line 21657

**Structurally identical** to the `_sliding_window_*` versions
(same allGather-fw_attn_varlen-chunk shape). The ONLY differences at the
Denote level are (1) op string `"OpName.FW_attn_zigzag"` vs
`"OpName.FW_attn_sliding_window"`, and (2) params slot 5 = 0 vs
windowLeft=512.

## L12 graph facts (verified from denote/yoco_goals/Goal_3.lean)

- The sliding-window band L0..L11 uses 12 SM ops + 24 PM ops with op
  string `"OpName.FW_attn_sliding_window"`, params `[16,4,64,64,1,512]`.
- The zigzag band L12..L23 uses 12 SM ops + 24 PM ops with op string
  `"OpName.FW_attn_zigzag"`, params `[16,4,64,64,1,0]`.
- Layer stride for SM output = 54 (verified across k=0..11). For L12 the
  SM attention output should be `4696 + 54*12 = 5344` — please verify
  with a `grep -n 'FW_attn_zigzag' denote/yoco_goals/Goal_3.lean` and
  count layers.
- PM router stride = 186 (verified in `mk_router`). Same arithmetic
  applies: L12 PM tids = base + 186*(12-2).

## Task

### Deliverable

`sm_pm_router_commute_L12` proven kernel-clean, plus every helper it
requires (attention_L12, carry_L12, nl_L12, qproj_L12, kproj_L12,
vproj_L12, rms_L12, qlin_L12, klin_L12, pm_attn_shard_shapes_L12,
gate_mul_L12, moe_gmm_L12, router_commute_L12, buddy_sm_12, buddy_r0_12,
buddy_r1_12, nSM_12/nR0_12/nR1_12).

### Approach (recommended — don't feel bound if you find better)

1. **Verify strides for k=12.** grep the graph, confirm SM/PM tid
   formulae still hold. Report the actual tid numbers you found so I can
   sanity-check.

2. **Fork `mk_attention` → `mk_attention_zigzag`.** Copy the entire elab
   block (Pattern_3.lean:13455..~13860) verbatim, then patch these
   substitutions:
   - `"OpName.FW_attn_sliding_window"` → `"OpName.FW_attn_zigzag"`
   - `applyNodeRingAttn_sliding_window_of_singleton` → `_zigzag_of_singleton`
   - `applyNodeRingAttn_sliding_window_out` → `_zigzag_out`
   - `params := [16, 4, 64, 64, 1, 512]` → `params := [16, 4, 64, 64, 1, 0]`
   - naming suffix on generated theorems: e.g. don't clobber L2..L11.
     Simplest: gate `mk_attention_zigzag k` on `k ≥ 12`.
   - stride arithmetic: for sliding it was `+54*(k-3)` etc. For zigzag,
     check whether the arithmetic still works from k=12. It probably does
     because tid layout is a single arithmetic progression, but verify.

3. **`mk_pm_attn_shard_shapes`, `mk_carry_a`, `mk_carry_b`,
   `mk_router`, etc.** — most or all of these don't touch the attention
   op string. Try `mk_pm_attn_shard_shapes 12` etc. first — many will
   just work. Only fork the ones that break.

4. **Assemble `sm_pm_router_commute_L12`** using `mk_router 12` if the
   macro doesn't need forking. If it does, fork it to `mk_router_zigzag`.

5. **Build check** after each layer of macros:
   `lake build TrainVerify.Denote.YOCOGoals.Pattern_3` (or however this
   project builds — inspect `lakefile.toml` / `lakefile.lean`).
   FAST-iteration tip: develop the L12 lemmas in a scratch module
   `denote/yoco_goals/Pattern_3_L12_spike.lean` that imports Pattern_3,
   so you get the cached olean (~4s builds) instead of the full ~25-30min
   Pattern_3 compile. Only paste the verified block back into Pattern_3
   AFTER it works.

6. **Kernel audit.** For the final `sm_pm_router_commute_L12`, add
   `#print axioms sm_pm_router_commute_L12` at the bottom of the file
   (temporarily) and verify the output is exactly
   `[propext, Classical.choice, Quot.sound]` (± `Lean.ofReduceBool,
   Lean.trustCompiler` if you needed `native_decide`).

7. **Commit + push.** After the final build succeeds and audit passes:
   ```bash
   git add -A
   git commit -m "Iroha L12 zigzag-band pilot: prove sm_pm_router_commute_L12 (zero sorry, kernel-clean)"
   git push origin iroha-l12-pilot
   ```
   Do NOT open a PR. Iroha will merge to main manually after review.

### Constraints & pitfalls

- **NO sorry, NO new axioms.** Not even temporary. If you get stuck,
  leave notes in `PROMPT-L12-STATUS.md` and stop — do not paper over.
- **NO python bulk regex on Pattern_3.lean.** (Lesson 26 in
  `../AGENTS.md` — different sites need different substitutions.)
  Use `patch` tool for targeted edits, or write to a scratch file.
- Read `../AGENTS.md` (the trainverify AGENTS.md) top-to-bottom before
  starting — it has 31 numbered rules that codify iroha's blood-lessons
  on this codebase. Rules #12, #19, #23, #26 are especially relevant
  here.
- Set-option pitfalls: `set_option ... in` doesn't accept docstring gap
  (rule #23). Use `-- ...` line comments.
- **Estimated effort**: 4-8 hours of build/fix loops. Don't quit early;
  don't self-throttle. If context is running low, commit intermediate
  progress with a clear TODO comment and note in
  `PROMPT-L12-STATUS.md` exactly where you paused.

### Reporting

When you finish (or hit a blocker you truly can't unblock), write
`PROMPT-L12-STATUS.md` at the repo root with:
- What you accomplished (list committed SHAs).
- What macros you forked and why.
- Actual tid numbers you verified for L12.
- `#print axioms` output.
- Anything surprising you learned about zigzag semantics.
- Any blocker + your best hypothesis for it.

Then STOP. Iroha will pick up from there.

## References

- Pattern_3.lean:7413 — `sm_pm_attention_L1_commute` (hand-written sliding-
  window template used as basis for `mk_attention`).
- Pattern_3.lean:7889 — `sm_pm_router_commute_L1` (assembly template).
- Pattern_3.lean:11536 — `mk_router` elab.
- Pattern_3.lean:13455 — `mk_attention` elab (the main thing to fork).
- Denote.lean:21133..21700 — full ring-attn machinery (both zigzag and
  sliding_window helpers side by side).
- Goal_3.lean:539 — first `FW_attn_zigzag` occurrence.

Godspeed. — iroha
