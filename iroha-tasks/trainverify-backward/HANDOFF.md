# Backward / DP work line

Latest: see **LayerNorm source and dβ kernel checkpoint** below and `ACCEPTANCE.json.layernorm_dbeta_checkpoint`. Earlier sections are chronological checkpoints, not current TODOs.

## Ownership and baseline
- Owner: Methode, Discord backward thread; independent branch `trainverify-backward`.
- Worktree: `/home/v-zhouziyu/work/trainverify-backward`.
- Immutable base: `b1b2fb45a521488f9b7b517982e11fabaaaf0d10` (clean when created).
- Forward tree is read-only: `/home/v-zhouziyu/work/trainverify-dp-prefix-cost-closure`.
- Own this document and new backward-specific authority/read helper/tests only. No canonical renderer/scanner/import/public entry edits; exact module contracts and commits will be handed to forward integration.

## Existing evidence and reuse
- `Verdict/runtime_prefix.py` already supports BW_linear, with ordered `[dy,x,w]`, two outputs and rank2/rank3 shape checks. Existing dX/dW theorem families are under audit, not to be reimplemented blindly.
- `denote/SourceValueRead.lean` supplies original successful run / SAME-final-Store source reads; `SourceLinearRead.lean` is the forward pattern.
- Forward local acceptance: audit `dp-prefix-cost-closure/output-projection/STATUS.md` records output projection source commit `d9f53deb49f839d731a031c64c1e08ad27f464a6`. This is historical evidence, not a new build in this worktree.
- Existing real captures to reuse, without recapture: `trainverify-audits/general-parallel-internal1/global-b2/capture.pkl` and `p2-r4/capture.pkl`.

## First slice / open obligations
Audit original FW/BW pairing, saved x/w identity/version and cotangent before adding source read certificates. Distinguish source authority from exported DTO and renderer. No assumptions about loss/seed/DP scaling; no output equality premises.

Input reconstruction from the forward chain, seed provenance, branch accumulation and DP/WRED remain explicit upstream obligations until actually discharged. Local conditional read theorem is not a whole-model result.

## Source boundary checkpoint (Python-only)
- Own: `trainverify/backward_linear_authority.py`, `scripts/tests/test_backward_linear_authority.py`, this document and `inspect_capture.py`.
- `bind(cells, source_index)` consumes actual transient `IRBpOperation` / `.mirror`, complete original ordered IR metadata and source fullrefs. It records the independent cotangent/saved-X/saved-W roles, dX/dW outputs and contraction convention; it rejects changed saved versions/owners, reordered outputs, mismatched derivative identities and exact-fullref suffix overwrite.
- Raw caller cells must come from the existing source loader. Binding this record to writer/export/call authority, lowered graph execution order and the shared proof DAG remains the next interface step. This record is not itself an authenticated whole-model certificate.
- Actual captures: first BW is SM cid62 / FW cid57 and PM rank0 cid348 / FW cid345. Source formulas: `Denote.lean:381` rank2/rank3 backward agrees with local `torch.nn.functional.linear` derivative over supplied cotangent; no added local scaling. PM first dX has value-part `(0,2)` (partial sum, not a numerical division by two). Original runtime seed/normalization remains an external obligation.
- Executed strict RED→GREEN: missing binder → positive; six fullref/order failures → guard → green; five newly exposed mirror/metadata/version/suffix failures → guard → green. `saved-ir` is rejected by the prior saved-fullref guard, not counted as new metadata-guard coverage.
- Test fixture clones all Cell slots explicitly: ordinary `copy.copy(Cell)` drops transient IR authority through `__getstate__`; clone acceptance is checked before mutations.
- Focused command (13 passed, 0 skipped):
  `TRAINVERIFY_CAPTURE_ROOT=/home/v-zhouziyu/trainverify-audits/general-parallel-internal1 PYTHONPATH=/home/v-zhouziyu/work/trainverify-backward:/home/v-zhouziyu/work/trainverify-backward/Verdict /home/v-zhouziyu/.venvs/trainverify-capture/bin/python -m pytest /home/v-zhouziyu/work/trainverify-backward/scripts/tests/test_backward_linear_authority.py -q --tb=short`
- Source-only inspection: same PYTHONPATH/Python, run `/home/v-zhouziyu/work/trainverify-backward/iroha-tasks/trainverify-backward/inspect_capture.py`; uses `_prepare_rank_cells` in memory, never build_graph rank-cell cache.

## Source-read checkpoint (kernel accepted, not whole-model)
- First Python-only checkpoint: `0e55e22d7ae83a5e29c8627a9e7af6e3fef9c240`.
- Own additionally: `Verdict/runtime_backward_linear_reads.py`, `trainverify/denote/SourceBWLinearRead.lean`, `scripts/tests/test_runtime_backward_linear_reads.py`, files under this handoff directory, and the one scoped `.gitignore` entry for local kernel output.
- `render_read(view, cells, snapshot, source_index, order, label)` binds original BW+FW writer/export/call and ordered fullrefs to the lowered source graph, revalidates execution order, rejects nonempty params and checks the complete selected-node/execution suffix. It returns two named dX/dW theorem fragments plus explicit source/caller obligations. It never edits or publishes the canonical entry.
- Caller contract: import `denote.SourceBWLinearRead` and retain the existing `*Graph`, `*Scope`, `*Peers`, `*InputRequests`, `*Node_i`, `*DenoteWithInputs` definitions. Each theorem takes only the original successful run and concludes the corresponding final-Store `bw_linear` projection. Seeded runs may instantiate the initial Store with the authenticated existing seeded initializer; no gradient value is assumed.
- Actual first SM + all four PM ranks: source/execution indices `(64,64), (109,217), (345,219), (581,639), (817,641)`. Complete original tensor inventory, injective lowering IDs, shapes and execution order match the accepted canonical data before rendering.
- Final focused gate: **72 tests + 443 subtests passed**, zero failures/skips. Includes new call/export/port mutations, source params RED→GREEN, invalid initial cotangent RED→GREEN, and independent node-vs-IR CID/name RED→GREEN. Existing seeded-prefix and scheduling regression suites are included.
- Serial Lean v4.32.2: 2 generic read declarations, 7 witness declarations, 10 actual graph declarations passed; each exact axiom set is only `propext`, `Classical.choice`, `Quot.sound`. Nonzero caller witness uses dy `[2,3,5]`, x `[2,3,7]`, w `[5,7]`; aliased outputs are rejected by InputSchedule. `ACCEPTANCE.json` records exact bytes/objects and real exit codes.
- Kernel setup used verified source/object dependencies in a private per-module overlay. Importing the whole canonical RuntimeWorld initially exposed unused missing helper caches; the correct minimal dependency is its unchanged `TrainVerifyRuntimeWorldData`, avoiding unrelated forward proofs. No shared cache writes or parallel Lean processes.
- Reproduce actual source fragment using the documented capture env/PYTHONPATH and `iroha-tasks/trainverify-backward/render_actual.py`; then run `python /home/v-zhouziyu/work/trainverify-backward/iroha-tasks/trainverify-backward/check_kernel.py <absolute .lean path>` serially for the helper, LeanWitness, and `.hermes/backward-kernel/ActualBWLinearRead.lean`. Standalone scripts use absolute resolved roots. Existing captures and accepted dependency receipts are required, not fabricated.

## Independent review follow-up
- Review of exact `9864fd52` independently reproduced two rejected-at-no-boundary defects: a raw mirror function signature different from `torch.nn.functional.linear`, and a changed paired FW opcode in the lowered graph, both previously yielded unchanged read fragments. Parent reproduced both with failing regression tests before patching.
- The binder now checks the actual original forward function signature; the renderer joins opcode/kwargs/params/fullrefs/shapes/scope for **both** FW and BW. This does not change any theorem statement or operator semantics.
- Final targeted Python gate: **26 passed**. In-memory deletion of each new guard independently causes its named regression to fail with `DID NOT RAISE ValueError`; restored tests pass. Fresh actual rerender is byte-identical to the already kernel-checked 8,480-byte fragment, so no unrelated Lean recompilation was needed.
- The external review session ended on a provider error before a final verdict; no independent final PASS is claimed. Findings above were separately reproduced and closed by the parent.

## Seed / BW_sum checkpoint
- Linear source consistency follow-up committed as `d10d8016d44ea926778ed0e5b47d51350a78d07e`.
- New owned modules: `Verdict/runtime_backward_seed_reads.py`, `trainverify/denote/SourceBWSumRead.lean`, `scripts/tests/test_runtime_backward_seed_reads.py`; diagnostic caller, actual generated fragment and reproducible seed runner are in this directory.
- Public `render(worlds, config, handoff_root)` calls existing `runtime_seed_feed.load_bundle` afresh. Actual complete source captures, existing run/event/PT inventories, input reference and current-run parameter values were validated. The observed unit seeds are not a default normalization decision. The generic sum theorem supports arbitrary supplied seeds; witness seed=3 proves every output value is 3 with shape `[2,3,5]`, no division by tensor size.
- The new fragment derives original BW_sum reads, final seed preservation via the existing original-run frame theorem, then broadcast tensor values from the existing authenticated seeded initializer. Fullrefs/mirror metadata/function identity, writer/export/call, params and complete execution suffix are retained. It does not alter the seed adapter or canonical entry.
- Final focused gate: **36 tests + 40 subtests passed**. New seed-ref RED→GREEN plus positive source/runtime authentication. Four in-memory guard removals (seed ref, seed value contract, original function, params) each fail the exact regression with `DID NOT RAISE`; restored tests pass. The suffix negative is rejected by the inherited full scheduler guard and is not counted as coverage of the later local suffix check.
- Serial Lean: **1 generic + 10 witness + 15 actual declarations**, all kernel accepted with only the kernel triple. `ACCEPTANCE.json.seed_checkpoint` records exact sources, outputs and real exit codes. Only unchanged Data + seed Prefix0000/Support and existing frame dependencies were reused; no whole-model rebuild or capture-cache mutation.
- Reproduce actual seed fragment: same capture env/PYTHONPATH/Python, run `iroha-tasks/trainverify-backward/render_actual_seeds.py`, then serial checker on `SourceBWSumRead.lean`, `SumWitness.lean`, `.hermes/backward-kernel/ActualBWSeedRead.lean`.

## Backward AllToAll / cotangent checkpoint
- Seed/BW_sum checkpoint committed as `4b505bd26ae73a56b997711c11e074c0555cb0db`.
- New owned module `Verdict/runtime_backward_cotangent_reads.py` freshly loads the existing source collective snapshot, requires the actual `backward_context.status == structurally-bound`, and joins exact ordered peer gradients to the already authenticated BW_sum certificates and the original BW_linear input.
- Real backward nodes retain raw FW-looking kwargs `[idim=2,odim=1]`; independently bound runtime/autograd scope proves their effective parameters are `[1,2]`. Source indices 108/344/580/816 feed the four original linear cotangents. No group sorting, dropped contributions or across-DP pairing is introduced.
- The new shared-DAG fragment reuses prior seed and linear theorem names. Its conclusions expose each faithful sender-split/receiver-gather cotangent expression and substitute it separately into dX and dW. It does **not** yet prove that the corresponding DP/TP reconstructed gradients equal the SM gradient.
- Exact actual fragment: **16 kernel declarations**, all kernel triple. Final **27 focused tests + 12 subtests passed**. Effective-dimension, peer-order, and gradient-version guard deletions each trigger their named regression. Missing/duplicate contributions are rejected after the fresh seed predecessor was accepted.
- Inherited faithful-scope heldouts include noncontiguous K=3 groups. Replayed **24 CPU source-flow cases for K=1,2,3** using asymmetric position/rank values; K3 output explicitly retained rank order. This is a derived source-flow CPU check, not distributed Torch execution; the new capture-to-DAG slice was actually exercised on TP2/DP2 only.
- Reproduce with `render_actual_cotangents.py` followed by the serial checker on `.hermes/backward-kernel/ActualBWCotangentRead.lean`; it imports the already checked actual seed and linear fragments instead of regenerating another proof architecture.

## dW → DP WRED checkpoint
- Cotangent checkpoint: `2e2dd59eebabcd391935a7929c8f25bcf9122b5b`.
- New owned files: `Verdict/runtime_backward_wred_reads.py`, `trainverify/denote/SourceWREDRead.lean`, `scripts/tests/test_runtime_backward_wred_reads.py`, `WREDWitness.lean`, `render_actual_wred.py` in this handoff directory.
- `render(worlds, indices, capture_path, rank_code_directory)` freshly authenticates original reducer source with the existing `attach_wred_scopes`; indices are original BWlinear source projections, not execution positions. Each local dW must have a unique direct local WRED consumer, and all ordered DP peers must be present in the selected linear projection. Intervening accumulation is explicitly unsupported, not bypassed.
- Each peer is joined through original raw reducer `parameter`, `grad_input`, placement identity/indmap/valmap/plan rank and distinct strictly-integer DP scale units. The original `_gid2wid` can be empty and is not used to guess parameter identity. Original SUM/zero=0/nreplicas=1 is required; no division is added. Writer/export/call and original gradient/parameter versions remain in the interface record.
- Actual `lm_head.weight`: groups `[0,2]`/`[1,3]`, BWlinear dW source versions 875v1/878v1, WRED outputs v2; reducer source indices 232/468/704/940. Original source call_instance=5 is retained. No model/TID/rank-count special case was added to the renderer.
- `SourceWREDRead.wred_of_success` inverts the checked original step; `wred_value_of_split` uses original run/scope/split/full-suffix preservation to conclude `t out = cross_dp_wred (inputs.map t)`. Successful execution supplies WredContract; no output shape/value assumption. Actual `_dw` callers substitute each existing separate BWlinear `.2` theorem in ordered SUM inputs. Parameter identity is a source-binding obligation, not something WredContract proves.
- Strict missing-symbol/bridge RED→GREEN; strict DP coordinate negative first exposed acceptance of `False` as unit 0, then fixed. Final **26 focused tests + 27 subtests** passed. Post-attachment negatives cover mean/replica/bool scaling, peer order, peer parameter/name/slice/version, duplicate DP unit and missing contributions. Four in-memory guard removals each make its named regression fail with `DID NOT RAISE`; restoration passes. Inherited scheduler/linear checks already reject overwritten dW suffixes; no independent new suffix-guard coverage is claimed.
- Serial exact-byte Lean: **2 helper + 9 K3 witness + 8 actual graph declarations**, only `propext`, `Classical.choice`, `Quot.sound`. Witness uses ordered noncontiguous ranks `[0,2,5]`, shape `[2,3]`, position-dependent gradients with distinct bases 2/7/11, successful original run and output shape/value conclusions (positions 0/5 yield 20/35). `ACCEPTANCE.json.wred_checkpoint` records real exits/sources/objects. The capture connection itself is exercised on existing TP2/DP2, not claimed tested on every topology.
- Reproduce with the established capture env/Python: `render_actual_wred.py`; serial `check_kernel.py` on absolute paths to `SourceWREDRead.lean`, `WREDWitness.lean`, `.hermes/backward-kernel/ActualBWWREDRead.lean`. Imports reuse unchanged canonical Data and already checked ActualBWLinearRead; no canonical entry changes or full rebuild.
- Runtime reducer hooks, AMP/optimizer behavior and Torch storage refinement are outside this source-model slice. No proof/public/Torch flag is promoted.

## Backward final-store shape projection checkpoint
- WRED checkpoint: `f0421480e5dba2e899586a50d957ef1138a3b3fb`.
- New owned `Verdict/runtime_backward_prefix_shapes.py` projects cotangent, saved-primal and dX shapes from the **existing complete shared PM prefix DAG**, not from capture shape assertions. It reuses named writer/shape/frame certificates and one final-State equality; it does not generate another whole-model prefix or change canonical entries.
- Input contract remains `pmSeededPrefixInitShapes s` (unwritten parameter shapes) plus the original successful seeded run; final shape equations are conclusions. A concrete shape-indexed Store proves the entire initial shape contract and an original successful PM run simultaneously satisfiable. This is a formal source-model witness, not a replay of Torch parameter values.
- Fresh BW source binding precedes each projection. Prefix coverage must match the full execution permutation; missing/duplicate projections and missing run/writer/shape/suffix-frame declarations fail closed. The inventory selects theorem names only; it is not semantic proof authority. Acceptance requires imports of the exact original Data/prefix modules and kernel checking. Fullrefs/shapes/order were checked against the same canonical Data receipt in the actual replay.
- Final gate: **17 focused tests**, including six after-positive-control negatives. Four in-memory inventory-guard removals are caught. **16 actual kernel declarations**, only the kernel triple: shared run/final-state, 12 PM shape queries, and initial-contract/original-run satisfiability. Source is 16,260 bytes, reusing 157 existing frame edges; no prefix rebuild. `ACCEPTANCE.json.backward_prefix_shapes_checkpoint` holds real source/object/exit evidence.
- Actual result: PM G `[1,16,128]`, saved-X and dX `[1,16,64]` on each of four ranks. No cross-rank saved-X value equality was assumed. The checker now accepts any explicitly imported numbered prefix block by verifying and reusing the prior module closure in the private overlay, under the unchanged global serial-Lean rule.
- Replay: `render_actual_prefix_shapes.py`, then serial `check_kernel.py` on `.hermes/backward-kernel/ActualBWPrefixShapes.lean`, using the documented capture environment. SM ordinary-fold success has no corresponding shape DAG in the inspected artifact; SM shapes and cotangent reconstruction remain to be established rather than assumed.

## Independent-saved-X dX adapter checkpoint
- PM shape projection checkpoint: `987e280795ea5ec15a86d93f185387b4d44eb3c3`.
- `trainverify/denote/SourceBWLinearDxUnit.lean`: `source_bw_linear_dx_shape_only` reuses the transpose identity twice; `source_bw_linear_dx_batch_tp_unit` composes that identity with existing DP batch chunk commute and TP output-row reduction. No new finite-sum mathematics or dW relaxation. All G/X/W list lengths are explicit; a separate local-list-length theorem proves no truncation.
- The generic input contract supplies positive D/T/B/S/O/I, legal DP unit u, full/local input shapes, `chunk0 G = gather2 gs`, and `W = gather0 ws`. It concludes the DP chunk of global dX equals the ordered TP sum of local dX. No global/local or peer saved-X value equality and no output-value premise occurs. Original source-read/cotangent/parameter bindings must establish this contract before applying it to the actual graph.
- Parent ran missing-symbol RED, imported the ended isolated edit-only worker's two owned files, and repaired witness-only elaboration (explicit Tensor.ext operands and nil membership simplification). **3 generic + 7 witness declarations** passed serial exact-source kernel/axiom checks. Witness D2/TP3, dimensions B/S/O/I=1/2/1/4, uses different gradients across DP units and TP ranks, different saved-X values 11/13/17 versus global 19, and proves the wrong DP unit is unequal.
- `scripts/tests/test_backward_dx_shape_only_numeric.py`: **1 CPU Torch autograd test passed**, directly calling `torch.nn.functional.linear`; controls distinguish wrong DP unit, invented mean, dropped rank and wrong G/W pairing. dW differs when saved-X values differ and agrees after restoring the original saved-X values. This is a small CPU source-operator check, not distributed execution or a Torch-refinement theorem.
- Reproduce serial checks for `KRankBWLinearDxRow.lean`, `KRankBWLinearDxSequence.lean`, `SourceBWLinearDxUnit.lean`, `DxUnitWitness.lean` with the existing checker/private overlay. `ACCEPTANCE.json.dx_unit_adapter_checkpoint` records the final exact bytes and real exits.

## Seed-primal shape follow-up
- dX adapter checkpoint: `2663cb9f3f44dcf0b9345c0f277c4a1799e48d82`.
- Optional `seed_config` in the same prefix projection module freshly invokes the existing authenticated seed reader before selecting each BW_sum saved primal. Four additional original final-store shape conclusions `[1,8,256]` now feed the faithful reverse-AA constant-value step; no extra shape assumption is added.
- Missing API tracer RED→GREEN; **15 focused tests** including the existing seed identity/signature/params/suffix negatives. Those seed rejections remain inherited predecessor coverage, not new guard coverage. **20 actual kernel declarations** (the prior 16 plus four seed-primal reads) passed with the kernel triple. The actual replay now requests this option; `ACCEPTANCE.json.seed_primal_shapes_checkpoint` records the newer bytes.

## Next real blocker / integration status
- Seed → BW_sum → faithful reverse AllToAll → original dX/dW expressions are locally connected under the original successful seeded run.
- Distributed gradient equality still needs **final LayerNorm saved-X reconstruction**, **initial weight frame/layout**, and source-derived shape relations. Forward saved-X value reconstruction remains an explicit interface obligation of the forward line; dW cannot discard it just because dX uses only X's shape.
- Direct dW contribution → DP WRED source-read/scaling is now kernel checked locally. Next is source-bound shape/initial-weight frame/layout needed to reconstruct dX without assuming saved-X values; dW reconstruction still requires the forward saved-X value interface.
- Read-only forward inspection saw HEAD `9b2b71ab` with active staged attention/residual work; those files were not changed or imported. Its final saved-X closure is not claimed available.
- All emitted proof/kernel/public/Torch flags remain false; external exact-byte receipts certify these local kernel declarations only. Canonical/main integration not done; no push, publication or cron.

## Resumed constant-cotangent checkpoint
- Imported the ended edit-only worker's two files. Parent fixed explicit shape arguments in chunk rewrites and completed the witness's definitional saved-shape step; no mathematical contract changed.
- `SourceConstantCotangent.lean`: arbitrary Scalar c through BW_sum, DP chunk0, TP gather2, and faithful sender-chunk/receiver-gather AA12. **5 generic + 9 non-unit seed witness declarations** pass the serial kernel gate, kernel triple only. Witness retains D=K=3, c=3 and asymmetric dimensions.
- New owned `Verdict/runtime_backward_constant_cotangents.py` freshly invokes authenticated seed/collective reads and existing prefix shape projections. It emits only dependent constant-value declarations, not another prefix. **Four original PM G tensors are proved equal to constant1 [1,16,128]**, under the original initial-shape contract and successful run; the unit value comes from the authenticated seed loader.
- `render_actual_constants.py` exercises the original capture/fullref inventory. **5 actual declarations**, including the existing caller satisfiability witness, pass with the kernel triple. **19 focused Python tests passed** across new constant composition and prior cotangent/prefix suites. Missing shape/seed remain predecessor rejection coverage; no new mutation coverage is claimed for them.
- Actual artifacts and receipts are `.hermes/backward-kernel/ActualBWConstantCotangents*`; exact receipts copied to `ACCEPTANCE.json.constant_cotangent_checkpoint`.
- SM sparse-shape implementation and ReduceScatter source-read are separate candidates/ongoing work, not accepted by this checkpoint. SM-PM G/W relation and dX reconstruction remain open. No canonical entry edit, publication or flag promotion.

## SM shape → original dX → ReduceScatter checkpoint
- `Verdict/runtime_backward_sm_shapes.py` follows a sparse original source shape dependency graph: 26 forward nodes plus BW_sum, stopping at the two explicit view anchors rather than rebuilding a SM whole-prefix or assuming attention values. Original signatures/IR ports/fullrefs/writer/export/order/suffix are freshly joined. The 31 actual declarations derive X1318 `[2,16,64]`, saved sum primal1319 and G1375 `[2,16,256]` from nine final parameter shapes plus the original successful run.
- `reuse_forward_entry.py` links only exact source/object-verified accepted forward entry dependencies into the private overlay; it never rebuilds or rewrites the forward entry. `render_actual_parameters.py` selects the existing canonical initial-contract projections and reuses `initialParameterValues_final`; 12 declarations. Its inventory merely selects conjunction positions; the imported canonical contract and kernel gate establish them.
- `render_actual_inputs_witness.py`: 3 declarations prove the complete seeded initial parameter contract, all 100 PM initial-shape premises and both successful original runs simultaneously satisfiable. SM parameters are nonzero constant-one tensors; PM parameters are their exact ordered slices/copies. No runtime Torch value replay is claimed.
- `Verdict/runtime_backward_dx_values.py` freshly joins source seed/cotangent/shape and reducer parameter placements. Original DP scale units, not arithmetic on rank IDs, select the two TP units. It derives SM G=constant1, cross-graph G chunk/gather, ordered W relations, then instantiates the existing shape-only dX theorem. No saved-X value equality is assumed. Seven actual declarations prove the two original results:
  - `backwardDxUnit_0`: `chunkPrimDimN 0 2 0 (t 1374) = tensorSum [q 279, q 581]`.
  - `backwardDxUnit_1`: `chunkPrimDimN 0 2 1 (t 1374) = tensorSum [q 885, q 1187]`.
- Caller context: `hs : smSeededDenoteWithInputs s = some t`, `hp : pmSeededDenoteWithInputs p = some q`, `hv : InitialParameterValues (smInitialWithSeeds s) (pmInitialWithSeeds p)`, and `hi : pmSeededPrefixInitShapes p`. The joint witness inhabits this exact context. Initial parameter values are a real input relation, not a conclusion asserted as a premise.
- `SourceReduceScatterRead.lean` adds original successful-step and SAME-final-Store read wrappers without editing shared SourcePrimitiveRead/Steps. Three helper +11 K3 noncontiguous-rank witness declarations pass kernel. Source success alone does not authenticate ordered peer/shapes: `runtime_backward_reduce_scatter_reads.py` freshly checks these against original nnScaler backward scope, raw kwargs/opcode, gradient readpoints, positive/equal/divisible shapes and the complete suffix. Eight actual read declarations pass kernel.
- `runtime_backward_dx_consumers.py` composes those original reads with dX unit results. Ten actual declarations prove SM full dX shape, the four consumer value/shape pairs, and caller nonvacuity. Outputs `[276,579,882,1185]` correspond to `(DP unit,TP local index)=[(0,0),(0,1),(1,0),(1,1)]`:
  `q out = chunkPrimDimN 1 2 local (chunkPrimDimN 0 2 unit (t 1374))`, with output shape `[1,8,64]`.
- Final parent gate: **52 focused tests**, plus six independently killed guard mutations with `DID NOT RAISE ValueError` (strict DP type, raw parameter placement, SM raw signature, RS lowered op, RS peer readpoints, consumer ordered dX). Two substantive independent reviews PASS; their suggestion for a coordinated DTO/view/writer permutation test is now retained. A final two-line review confirms exact-byte reuse of the previously kernel-accepted common-binder consumer artifact. All 85 declarations in this checkpoint use only the kernel triple. No full prefix rebuild, shared cache mutation, entry edit, push or publication.
- Replays in this directory: `render_actual_sm_shapes.py --output-dir <backward-kernel>`, `render_actual_parameters.py`, `render_actual_inputs_witness.py`, `render_actual_dx.py`, `render_actual_reduce_scatter.py --lean` (stdout), `render_actual_dx_consumers.py`. Kernel order: generic RS helper → RS witness; actual SM shapes → parameters → joint witness → dX values → RS read → dX consumers. Unchanged inputs/objects may be reused only after exact-byte comparison. `ACCEPTANCE.json.sm_dx_consumer_checkpoint` holds every local receipt.
- Environment correction: this runtime exposed a child's persistent PYTHONPATH export to the parent. Use command-local `PYTHONPATH=/absolute/worktree:/absolute/worktree/Verdict` for direct replays and mutation checks; check module `__file__` rather than assuming Git worktree isolation also isolates shell exports.

## External value-interface status at the dX checkpoint (historical)
- Read-only latest forward acceptance is `5954a547a6c7ef30b21222ee34fb8f2bad38c6fb`. Its accepted endpoint is the first attention residual (`t1287`), with residual alias/exchange work active afterward. It has not yet established the **final** LayerNorm saved-primal relation; no forward files were modified/imported from that dirty worktree.
- Next actual BW_layernorm is SM source/execution65, inputs `[1374,1317,1234,1235]`, outputs `[1373,1259,1260]`. PM source→execution `111→221,347→224,583→643,819→646`; ordered `[G,X,gamma,beta]` are `[276,272,14,15]`, `[579,575,317,318]`, `[882,878,620,621]`, `[1185,1181,923,924]`. Raw normalized_shape `[64]`, eps `1e-5`. Its source-read wrapper is still a separate next backward task; source shape observations do not establish X values.
- Required forward SAME-final-Store contract: for ranks0/1/2/3 respectively, `q272/q575/q878/q1181 = chunk1 2 local (chunk0 2 unit (t1317))`. This must come from the original forward saved-primal chain, not be invented to finish the backward theorem.
- Gamma `[q14,q317,q620,q923] = t1234` and beta `[q15,q318,q621,q924] = t1235` are already covered by the initial parameter relation and final frame; do not introduce independent duplicate value assumptions.
- lm_head dW still requires `q80=q383=chunk0 2 0 (t1318)` and `q686=q989=chunk0 2 1 (t1318)`. Note the distinction: final LN saved input is **1317**, lm_head saved input is its output **1318**. Existing dW→DP SUM WRED reads remain accepted, but SM–PM dW equality is not proved.
- All generated proof/kernel/public/Torch flags stay false. These exact local kernel receipts are not canonical/public whole-model integration and not coverage of all three target models. The cross-graph continuation is blocked on the owned forward value interface, not on missing cotangent/dX/ReduceScatter mathematics.

## LayerNorm source and dβ kernel checkpoint
- `backward_layernorm_authority.py` and `runtime_backward_layernorm_reads.py` authenticate the original affine last-axis LayerNorm mirror, exact four-input/three-output ports, saved versions, derivative identities, original signature, normalized shape and epsilon (`1e-5` in this source). `SourceBWLayernormRead.lean` projects dX/dgamma/dbeta from the SAME final Store after the complete original suffix, with three distinct outputs and four input nonwrites. Three helper +17 inhabited/alias-control witness +15 actual read declarations pass the serial kernel gate.
- `runtime_backward_prefix_shapes.render` now optionally projects freshly authenticated LayerNorm saved primals, with `include_run=False` reusing the accepted shared final-run proof. Default existing prefix projection is byte-identical to its accepted 20-declaration artifact. No second PM or SM prefix was constructed.
- `runtime_backward_layernorm_context.py`: **14 actual declarations** prove PM X272/575/878/1181 shapes `[1,8,64]`, SM X1317 shape `[2,16,64]` through the already-read final FW LayerNorm and X1318 shape, eight gamma/beta final-value/shape projections from the canonical initial parameter frame, and the existing joint caller witness. These are not saved-X value relations.
- `runtime_backward_layernorm_wred.py`: **16 actual declarations** read the original gamma and beta reducers and their source contributions. This replicated affine parameter reduces across both TP and DP: original ranks `[0,1,2,3]`, ordered `(scale_unit,plan_rank)` pairs `[(0,0),(0,1),(1,0),(1,1)]`, SUM/zero0/nreplicas1. Distinct DP units alone would incorrectly reject this actual reducer. Parameter parent/full slice/name and every original derivative output remain independently joined; no accumulation/mean is inserted.
- `SourceBWLayernormDbetaUnit.lean` supplies three kernel-checked generic interfaces: saved-value invariance, arbitrary-K sequence reduction, and arbitrary-K batch reduction. Only G values and complete input shapes/list lengths are required. DP uses contiguous flat rows and reuses `KRankBWLayernormParam` finite-sum mathematics; it does not confuse axis0 with axis1 when B>1. `DbetaUnitWitness.lean` contributes six declarations with K3/B2/S3/H2, nonconstant rank-dependent G and differing saved-X/gamma/beta values. CPU Torch autograd additionally exercises D=TP3 with differing saved values and wrong-mean/drop/duplicate-unit controls; dgamma changes with X. This is not a distributed Torch refinement.
- `SourceTensorSumGroups.lean` proves ordered nested tensor sums equal the flattened sum for nonempty shape-compatible groups. No peer permutation is hidden.
- `runtime_backward_layernorm_dbeta_values.py` derives its G input reconstruction from the existing original SM dX → PM ReduceScatter theorems. Intermediate DP zero-X records are proof-only witnesses justified by dβ invariance, not modified source inputs. It proves:
  - `t1260 = tensorSum [q70,q373,q676,q979]`;
  - each final `q71/q374/q677/q980 = t1260` and each has shape `[64]`;
  - global dβ shape and the SAME successful-run/input-contract witness.
  These are **11 actual declarations**, with no saved-X value or output-equality premise. All nine new local kernel roots total **86 named declarations**, only `propext/Classical.choice/Quot.sound`. Source/object receipts and the final Python/review outcomes are recorded in `ACCEPTANCE.json.layernorm_dbeta_checkpoint`.
- Five in-memory source/reducer guard removals are independently killed (`DID NOT RAISE ValueError`): epsilon, original saved metadata, function signature, strict reducer coordinates and shared parameter placement. The final source review covers the exact 19 source/test/replay files; this chronological handoff and copied receipts are documentation, not new mathematical authority.
- Replay using the existing command-local capture environment: `render_actual_layernorm.py`, then `render_actual_layernorm_dbeta.py`. Kernel order: SourceBWLayernormRead → LayernormReadWitness → ActualBWLayernormRead → ActualBWLayernormContext → ActualBWLayernormWRED; existing KRankBWLayernormParam → SourceBWLayernormDbetaUnit → DbetaUnitWitness → SourceTensorSumGroups → ActualBWLayernormDbeta. Do not rebuild already accepted prefix/entry modules. The initial serial queue stopped on real helper elaboration errors; later per-file receipts supersede those failures. No theorem statement or source semantics was weakened to fix elaboration.

## Remaining cross-graph value boundary after dβ
- Latest read-only forward acceptance is `c23a0c68a6b4a8daa29fb06c9f0e54cd83459fe6` (residual aliases/faithful hidden-sequence frontiers). Its next active slice is the FFN LayerNorm, not the final LayerNorm. Forward `runtime_initial_relations.py` and a frontier-LN entry test are active owned changes; they were not touched.
- Remaining final-LN dX/dgamma still need `q272/q575/q878/q1181 = chunk1 2 local (chunk0 2 unit (t1317))` from the original forward value chain. Gamma/beta replication and all required shapes are already available; do not reintroduce those as duplicate assumptions.
- lm-head dW still needs `q80=q383=chunk0 2 0 (t1318)` and `q686=q989=chunk0 2 1 (t1318)`. dW and LayerNorm dgamma do not inherit the dβ saved-value relaxation.
- Public/canonical/whole-model/Torch flags remain false; no push, publication, recapture or shared cache mutation. This is an actual GPT backward slice, not completion of GPT-2/YOCO 3B/YOCO MoE 0.4B as a whole.

Final code gate for this checkpoint: **63 focused tests passed / 310.34s**, five killed guards, all86 local kernel declarations passed. Final19-file source review `ed44925c2a2433a808fa75fb9fcebdf29c819bae` has a parsed persisted PASS; no source changed afterward. The earlier300s outer test timeout was superseded by this complete successful run.
