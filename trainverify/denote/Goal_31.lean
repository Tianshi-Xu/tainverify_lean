/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 31 (tensor id: 135)
    SM: BW_matmul(140, 112, 108) → [139, 135]
    PM: AllToAll on inputs (dim 1→2, dim 1→3),
        BW_matmul per rank,
        AllToAll on dy outputs (dim 2→1)
    Key: batchedMatmul(transpose2d(X), Y) distributes over dim-2 chunks of X
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common

set_option linter.style.longLine false
set_option linter.flexible false
set_option linter.unusedSimpArgs false

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_31 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_matmul", ins := [140, 112, 108], outs := [139, 135] },
  ]

def pm_goal_31 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [424], params := [1, 2] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [420], params := [1, 3] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [425], params := [1, 2] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [421], params := [1, 3] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [426], params := [1, 2] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [422], params := [1, 3] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [427], params := [1, 2] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [423], params := [1, 3] },
    { rank := 0, op := "OpName.BW_matmul", ins := [140, 420, 424], outs := [440, 441] },
    { rank := 1, op := "OpName.BW_matmul", ins := [140, 421, 425], outs := [442, 443] },
    { rank := 2, op := "OpName.BW_matmul", ins := [140, 422, 426], outs := [444, 445] },
    { rank := 3, op := "OpName.BW_matmul", ins := [140, 423, 427], outs := [446, 447] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [441, 443, 445, 447], outs := [317], params := [2, 1] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [441, 443, 445, 447], outs := [319], params := [2, 1] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [441, 443, 445, 447], outs := [321], params := [2, 1] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [441, 443, 445, 447], outs := [323], params := [2, 1] },
  ]

def sm_goal_31InitShapes : List (Tid × Shape) := [
  (108, [16, 8, 64, 16]),
  (112, [16, 8, 64, 64]),
  (140, [16, 8, 64, 16]),
]

def sm_goal_31InitEnv : ShapeEnv := shapeEnvOfList sm_goal_31InitShapes

def pm_goal_31InitShapes : List (Tid × Shape) := [
  (140, [16, 8, 64, 16]),
  (304, [16, 2, 64, 16]),
  (305, [16, 2, 64, 16]),
  (306, [16, 2, 64, 16]),
  (307, [16, 2, 64, 16]),
  (400, [16, 2, 64, 64]),
  (401, [16, 2, 64, 64]),
  (402, [16, 2, 64, 64]),
  (403, [16, 2, 64, 64]),
]

def pm_goal_31InitEnv : ShapeEnv := shapeEnvOfList pm_goal_31InitShapes

def goal_31_cut_initGoals : List LineageGoal := initGoals ++ goal_31_prereqs

def goal_31_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_31 pm_goal_31 goal_31 sm_goal_31InitEnv pm_goal_31InitEnv goal_31_cut_initGoals

/-! ## Shape helpers -/

private theorem batchedMatmul_4d_shape (x y : Tensor) (d0 d1 n k m : Nat)
    (hx : x.shape = [d0, d1, n, k]) (hy : y.shape = [d0, d1, k, m]) :
    (batchedMatmul x y).shape = [d0, d1, n, m] := by
  unfold batchedMatmul; rw [hx, hy]; simp [Tensor.mkShape]

private theorem transpose2d_4d_shape (x : Tensor) (d0 d1 d2 d3 : Nat)
    (hx : x.shape = [d0, d1, d2, d3]) :
    (transpose2d x).shape = [d0, d1, d3, d2] := by
  unfold transpose2d; rw [hx]; simp [Tensor.mkShape]

/-! ## Part 1: valAt helpers for batchedMatmul -/

-- batchedMatmul [16,8,64,64] @ [16,8,64,16] → [16,8,64,16]
private lemma valAt_bm_full (g t : Tensor) (idx : Nat)
    (hg : g.shape = [16, 8, 64, 64]) (ht : t.shape = [16, 8, 64, 16])
    (hidx : idx < 131072) :
    valAt (batchedMatmul g t) idx =
    ∑ l ∈ Finset.range 64,
      valAt g (idx / 1024 * 4096 + (idx % 1024) / 16 * 64 + l) *
      valAt t (idx / 1024 * 1024 + l * 16 + idx % 16) := by
  cases g with | mk sg vg =>
  cases t with | mk st vt =>
  simp only at hg ht; subst hg; subst ht
  unfold batchedMatmul
  dsimp only [List.reverse, List.reverseAux, List.append]
  simp only [valAt, Tensor.mkShape]
  rw [dif_pos (show idx < prodShape ([16, 8, 64, 16] : Shape) from by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (64 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (16 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 64 = 4096 from by norm_num,
    show (64 : Nat) * 16 = 1024 from by norm_num]
  simp_rw [show idx % (64 * 16) % 16 = idx % 16 from by omega]

-- batchedMatmul [16,8,16,64] @ [16,8,64,16] → [16,8,16,16]
private lemma valAt_bm_chunk (g t : Tensor) (idx : Nat)
    (hg : g.shape = [16, 8, 16, 64]) (ht : t.shape = [16, 8, 64, 16])
    (hidx : idx < 32768) :
    valAt (batchedMatmul g t) idx =
    ∑ l ∈ Finset.range 64,
      valAt g (idx / 256 * 1024 + (idx % 256) / 16 * 64 + l) *
      valAt t (idx / 256 * 1024 + l * 16 + idx % 16) := by
  cases g with | mk sg vg =>
  cases t with | mk st vt =>
  simp only at hg ht; subst hg; subst ht
  unfold batchedMatmul
  dsimp only [List.reverse, List.reverseAux, List.append]
  simp only [valAt, Tensor.mkShape]
  rw [dif_pos (show idx < prodShape ([16, 8, 16, 16] : Shape) from by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (16 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (16 : Nat) ≠ 0 from by omega,
    show (16 : Nat) * 64 = 1024 from by norm_num,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (16 : Nat) * 16 = 256 from by norm_num]
  simp_rw [show idx % (16 * 16) % 16 = idx % 16 from by omega]

/-! ## Part 2: valAt helpers for allGatherPrimDimN -/

-- allGatherPrimDimN 2 with shard shape [16,8,16,16] → [16,8,64,16]
private lemma valAt_ag2_16_8_16_16 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 8, 16, 16])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 2 4 0 xs) idx =
    valAt (xs.getD (idx % 1024 / 256) (zeroTensor [16, 8, 16, 16]))
      (idx / 1024 * 256 + (idx % 1024 / 16 % 16) * 16 + idx % 16) := by
  have hshape_out : (allGatherPrimDimN 2 4 0 xs).shape = [16, 8, 64, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 2 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 16 = 16 from by norm_num,
    show (16 : Nat) * 4 = 64 from by norm_num,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (16 : Nat) * 16 = 256 from by norm_num,
    (show (1024 : Nat) ≠ 0 by omega), (show (16 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 1024 / 16 / 16 = n % 1024 / 256 from fun n => by omega,
    show ∀ n, n % 1024 % 16 = n % 16 from fun n => by omega]

-- allGatherPrimDimN 2 with shard shape [16,8,16,64] → [16,8,64,64]
private lemma valAt_ag2_16_8_16_64 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 8, 16, 64])
    (hidx : idx < 524288) :
    valAt (allGatherPrimDimN 2 4 0 xs) idx =
    valAt (xs.getD (idx % 4096 / 1024) (zeroTensor [16, 8, 16, 64]))
      (idx / 4096 * 1024 + (idx % 4096 / 64 % 16) * 64 + idx % 64) := by
  have hshape_out : (allGatherPrimDimN 2 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 2 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 64 = 64 from by norm_num,
    show (16 : Nat) * 4 = 64 from by norm_num,
    show (64 : Nat) * 64 = 4096 from by norm_num,
    show (16 : Nat) * 64 = 1024 from by norm_num,
    (show (4096 : Nat) ≠ 0 by omega), (show (64 : Nat) ≠ 0 by omega),
    (show (16 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 4096 / 64 / 16 = n % 4096 / 1024 from fun n => by omega,
    show ∀ n, n % 4096 % 64 = n % 64 from fun n => by omega]

-- allGatherPrimDimN 1 with shard shape [16,2,64,16] → [16,8,64,16]
private lemma valAt_ag1_16_2_64_16 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 2, 64, 16])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 1 4 0 xs) idx =
    valAt (xs.getD (idx % 8192 / 2048) (zeroTensor [16, 2, 64, 16]))
      (idx / 8192 * 2048 + (idx % 8192 / 1024 % 2) * 1024 + idx % 1024) := by
  have hshape_out : (allGatherPrimDimN 1 4 0 xs).shape = [16, 8, 64, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 1 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 64 = 64 from by norm_num,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (2 : Nat) * 4 = 8 from by norm_num,
    show (8 : Nat) * 1024 = 8192 from by norm_num,
    show (2 : Nat) * 1024 = 2048 from by norm_num,
    (show (8192 : Nat) ≠ 0 by omega), (show (1024 : Nat) ≠ 0 by omega),
    (show (2 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 8192 / 1024 / 2 = n % 8192 / 2048 from fun n => by omega,
    show ∀ n, n % 8192 % 1024 = n % 1024 from fun n => by omega]

/-! ## Part 3: valAt helpers for chunkPrimDimN -/

-- chunkPrimDimN 2 4 r on [16,8,64,64] → [16,8,16,64]
private lemma valAt_chunk2_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 2 4 r x) idx =
    valAt x (idx / 1024 * 4096 + (r % 4 * 16 + idx % 1024 / 64) * 64 + idx % 64) := by
  have hout_shape : (chunkPrimDimN 2 4 r x).shape = [16, 8, 16, 64] := by
    rw [chunkPrimDimN_shape 2 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 2 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, List.getElem?_nil,
    Option.getD, List.drop, List.foldl, List.set, List.length]
  norm_num
  simp only [show (64 : Nat) * 64 = 4096 from by norm_num,
    show (16 : Nat) * 64 = 1024 from by norm_num,
    (show (1024 : Nat) ≠ 0 by omega), (show (64 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 1024 % 64 = n % 64 from fun n => by omega,
    Nat.add_assoc]

-- chunkPrimDimN 3 4 r on [16,8,64,64] → [16,8,64,16]
private lemma valAt_chunk3_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 3 4 r x) idx =
    valAt x (idx / 16 * 64 + r % 4 * 16 + idx % 16) := by
  unfold chunkPrimDimN; rw [hshape]
  simp only [List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, List.getElem?_nil,
    Option.getD, List.drop, List.foldl, List.set, List.length, Nat.add_eq]
  norm_num
  conv_lhs => rw [show (if (4 : Nat) = 0 then (0 : Nat) else 64 / 4) = 16 from by decide]
  simp only [valAt, Tensor.mkShape, show prodShape [16, 8, 64, 16] = 131072 from by simp [prodShape]]
  rw [dif_pos hidx]
  simp only [Nat.add_assoc]

-- chunkPrimDimN 1 4 r on [16,8,64,16] → [16,2,64,16]
private lemma valAt_chunk1 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 16]) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 1 4 r x) idx =
    valAt x ((idx / 2048) * 8192 + (r % 4 * 2 + idx % 2048 / 1024) * 1024 + idx % 1024) := by
  have hout_shape : (chunkPrimDimN 1 4 r x).shape = [16, 2, 64, 16] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 1 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, List.getElem?_nil,
    Option.getD, List.drop, List.foldl, List.set, List.length]
  norm_num
  simp only [show (8 : Nat) * (1 * 64 * 16) = 8192 from by norm_num,
    show (2 : Nat) * (1 * 64 * 16) = 2048 from by norm_num,
    show (1 : Nat) * 64 * 16 = 1024 from by norm_num,
    (show (2048 : Nat) ≠ 0 by omega), (show (1024 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 2048 % 1024 = n % 1024 from fun n => by omega,
    Nat.add_assoc]

/-! ## Part 4: valAt helper for transpose2d -/

-- transpose2d on [16,8,64,64]: d1=64, d0=64, innerSize=64*64=4096
-- valAt output: outerIdx * 4096 + (innerFlat % 64) * 64 + innerFlat / 64
private lemma valAt_td_16_8_64_64 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 524288) :
    valAt (transpose2d x) idx =
    valAt x (idx / 4096 * 4096 + (idx % 4096 % 64) * 64 + idx % 4096 / 64) := by
  unfold transpose2d; rw [hshape]
  simp only [List.reverse, List.reverseAux, List.append, valAt, Tensor.mkShape]
  have hps : prodShape [16, 8, 64, 64] = 524288 := by simp [prodShape]
  rw [dif_pos (by rw [hps]; exact hidx)]
  simp only [show (64 : Nat) * 64 ≠ 0 from by omega, ite_false,
    show (64 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 64 = 4096 from by norm_num]

-- transpose2d on [16,8,64,16]: d1=16, d0=64, innerSize=64*16=1024
-- valAt output: outerIdx * 1024 + (innerFlat % 16) * 64 + innerFlat / 16
private lemma valAt_td_16_8_64_16 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 64, 16]) (hidx : idx < 131072) :
    valAt (transpose2d x) idx =
    valAt x (idx / 1024 * 1024 + (idx % 1024 % 16) * 64 + idx % 1024 / 16) := by
  unfold transpose2d; rw [hshape]
  simp only [List.reverse, List.reverseAux, List.append, valAt, Tensor.mkShape]
  have hps : prodShape [16, 8, 16, 64] = 131072 := by simp [prodShape]
  rw [dif_pos (by rw [hps]; exact hidx)]
  simp only [show (64 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (16 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 16 = 1024 from by norm_num]

/-! ## Part 5: transpose2d commutes with chunkPrimDimN 3 → chunkPrimDimN 2 -/

-- Extracted arithmetic lemmas for td_chunk3 commutativity
private lemma g31_td_chunk_idx_eq (idx r : Nat) (_ : idx < 131072) :
    -- LHS: td(chunk3(X))
    -- chunk3 gives: (·/16 * 64 + r%4*16 + ·%16)
    -- td on that gives: (·/1024 * 1024 + (·%1024%16)*64 + ·%1024/16) composed with chunk3 expr
    (idx / 1024 * 1024 + idx % 1024 % 16 * 64 + idx % 1024 / 16) / 16 * 64 +
    r % 4 * 16 + ((idx / 1024 * 1024 + idx % 1024 % 16 * 64 + idx % 1024 / 16) % 16) =
    -- RHS: chunk2(td(X))
    -- td gives: (·/4096 * 4096 + (·%4096%64)*64 + ·%4096/64)
    -- chunk2 on that gives: (·/1024 * 4096 + (r%4*16 + ·%1024/64)*64 + ·%64) composed with td expr
    (idx / 1024 * 4096 + (r % 4 * 16 + idx % 1024 / 64) * 64 + idx % 64) / 4096 * 4096 +
    ((idx / 1024 * 4096 + (r % 4 * 16 + idx % 1024 / 64) * 64 + idx % 64) % 4096 % 64) * 64 +
    (idx / 1024 * 4096 + (r % 4 * 16 + idx % 1024 / 64) * 64 + idx % 64) % 4096 / 64 := by
  omega

private theorem td_chunk3_eq_chunk2_td (X : Tensor) (r : Nat)
    (hX : X.shape = [16, 8, 64, 64]) :
    transpose2d (chunkPrimDimN 3 4 r X) =
    chunkPrimDimN 2 4 r (transpose2d X) := by
  have hchunk3_shape : (chunkPrimDimN 3 4 r X).shape = [16, 8, 64, 16] := by
    rw [chunkPrimDimN_shape 3 4 r _ _ hX (by omega)]; simp [List.set, List.getD]
  have htd_shape : (transpose2d X).shape = [16, 8, 64, 64] :=
    transpose2d_4d_shape X 16 8 64 64 hX
  have hLHS_shape : (transpose2d (chunkPrimDimN 3 4 r X)).shape = [16, 8, 16, 64] :=
    transpose2d_4d_shape _ 16 8 64 16 hchunk3_shape
  have hRHS_shape : (chunkPrimDimN 2 4 r (transpose2d X)).shape = [16, 8, 16, 64] := by
    rw [chunkPrimDimN_shape 2 4 r _ _ htd_shape (by omega)]; simp [List.set, List.getD]
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  have hidx' : idx < 131072 := by
    rw [hLHS_shape] at hidx; simp [prodShape] at hidx; omega
  -- LHS: transpose2d(chunkPrimDimN 3)
  rw [valAt_td_16_8_64_16 _ idx hchunk3_shape hidx']
  rw [valAt_chunk3_64_64 X r _ hX (by omega)]
  -- RHS: chunkPrimDimN 2(transpose2d)
  rw [valAt_chunk2_64_64 (transpose2d X) r idx htd_shape hidx']
  rw [valAt_td_16_8_64_64 X _ hX (by omega)]
  exact congrArg (valAt X) (g31_td_chunk_idx_eq idx r hidx')

/-! ## Part 6: Gather-chunk dim 2 roundtrip on [16,8,64,64] -/

private theorem gather_chunk_dim2_64_64 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 64]) :
    allGatherPrimDimN 2 4 0 [chunkPrimDimN 2 4 0 T, chunkPrimDimN 2 4 1 T,
      chunkPrimDimN 2 4 2 T, chunkPrimDimN 2 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 2 4 r T).shape = [16, 8, 16, 64] := by
    intro r; rw [chunkPrimDimN_shape 2 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 2 4 0 T, chunkPrimDimN 2 4 1 T,
      chunkPrimDimN 2 4 2 T, chunkPrimDimN 2 4 3 T].head?.map (·.shape)).getD []) = [16, 8, 16, 64] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 2 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 524288 := by
      rw [allGatherPrimDimN_shape 2 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag2_16_8_16_64 _ idx hhead hidx']
    set p := idx % 4096 / 1024 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk2_64_64 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Part 7: Gather-chunk dim 1 roundtrip on [16,8,64,16] -/

private theorem gather_chunk_dim1 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 16]) :
    allGatherPrimDimN 1 4 0 [chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 1 4 r T).shape = [16, 2, 64, 16] := by
    intro r; rw [chunkPrimDimN_shape 1 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T].head?.map (·.shape)).getD []) =
      [16, 2, 64, 16] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 1 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 131072 := by
      rw [allGatherPrimDimN_shape 1 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag1_16_2_64_16 _ idx hhead hidx']
    set p := idx % 8192 / 2048 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk1 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Part 8: batchedMatmul distributes over dim-2 chunks -/

private theorem bm_chunk2_dist
    (X : Tensor) (Y : Tensor)
    (hX : X.shape = [16, 8, 64, 64]) (hY : Y.shape = [16, 8, 64, 16]) :
    batchedMatmul X Y =
    allGatherPrimDimN 2 4 0 [batchedMatmul (chunkPrimDimN 2 4 0 X) Y,
                              batchedMatmul (chunkPrimDimN 2 4 1 X) Y,
                              batchedMatmul (chunkPrimDimN 2 4 2 X) Y,
                              batchedMatmul (chunkPrimDimN 2 4 3 X) Y] := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 2 4 r X).shape = [16, 8, 16, 64] := by
    intro r; rw [chunkPrimDimN_shape 2 4 r _ _ hX (by omega)]; simp [List.set, List.getD]
  have hbm_chunk_shape : ∀ r, (batchedMatmul (chunkPrimDimN 2 4 r X) Y).shape = [16, 8, 16, 16] :=
    fun r => batchedMatmul_4d_shape _ _ 16 8 16 64 16 (hchunk_shape r) hY
  have hhead_bm : (([batchedMatmul (chunkPrimDimN 2 4 0 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 1 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 2 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 3 X) Y].head?.map (·.shape)).getD []) = [16, 8, 16, 16] := by
    simp [List.head?, Option.map, hbm_chunk_shape 0]
  have hLHS_shape : (batchedMatmul X Y).shape = [16, 8, 64, 16] :=
    batchedMatmul_4d_shape _ _ 16 8 64 64 16 hX hY
  have hRHS_shape : (allGatherPrimDimN 2 4 0 [batchedMatmul (chunkPrimDimN 2 4 0 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 1 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 2 X) Y,
      batchedMatmul (chunkPrimDimN 2 4 3 X) Y]).shape = [16, 8, 64, 16] := by
    rw [allGatherPrimDimN_shape 2 4 _ _ hhead_bm]; simp [List.set, List.getD]
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  have hidx' : idx < 131072 := by rw [hLHS_shape] at hidx; simp [prodShape] at hidx; omega
  -- LHS
  rw [valAt_bm_full X Y idx hX hY hidx']
  -- RHS: allGatherPrimDimN 2 of bm results
  rw [valAt_ag2_16_8_16_16 _ idx hhead_bm hidx']
  set p := idx % 1024 / 256 with hp_def
  have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
  have h_bm_idx_lt : idx / 1024 * 256 + (idx % 1024 / 16 % 16) * 16 + idx % 16 < 32768 := by omega
  rcases hp_range with hp | hp | hp | hp <;> {
    simp only [hp, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD]
    rw [valAt_bm_chunk _ Y _ (by first | exact hchunk_shape 0 | exact hchunk_shape 1 | exact hchunk_shape 2 | exact hchunk_shape 3) hY h_bm_idx_lt]
    apply Finset.sum_congr rfl
    intro l hl
    have hl64 : l < 64 := Finset.mem_range.mp hl
    have h_outer : (idx / 1024 * 256 + (idx % 1024 / 16 % 16) * 16 + idx % 16) / 256 = idx / 1024 := by omega
    have h_inner_i : (idx / 1024 * 256 + (idx % 1024 / 16 % 16) * 16 + idx % 16) % 256 / 16 = idx % 1024 / 16 % 16 := by omega
    have h_inner_j : (idx / 1024 * 256 + (idx % 1024 / 16 % 16) * 16 + idx % 16) % 16 = idx % 16 := by omega
    rw [h_outer, h_inner_i, h_inner_j]
    congr 1
    rw [valAt_chunk2_64_64 X _ _ hX (by omega)]
    exact congrArg (valAt X) (by omega)
  }

/-! ## Part 9: Main proof -/

theorem prove_goal_31_cut : goal_31_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prerequisites
  have hInit108 : InitGoalHolds pm_goal_31.numRanks goal_10 initSM initPM :=
    hInitGoals goal_10 (by simp [goal_31_cut_initGoals, goal_31_prereqs, initGoals])
  have hInit112 : InitGoalHolds pm_goal_31.numRanks goal_14 initSM initPM :=
    hInitGoals goal_14 (by simp [goal_31_cut_initGoals, goal_31_prereqs, initGoals])
  have hInit140 : InitGoalHolds pm_goal_31.numRanks goal_36 initSM initPM :=
    hInitGoals goal_36 (by simp [goal_31_cut_initGoals, goal_31_prereqs, initGoals])
  -- Extract shapes and reconstruction
  have h108 := initGoalHolds_sharded4 4 goal_10 108 304 305 306 307
    [16, 8, 64, 16] [16, 2, 64, 16] initSM initPM hInit108 rfl rfl rfl rfl
  obtain ⟨h108_sm_shape, h304_shape, h305_shape, h306_shape, h307_shape, h108_rec⟩ := h108
  have h112 := initGoalHolds_sharded4 4 goal_14 112 400 401 402 403
    [16, 8, 64, 64] [16, 2, 64, 64] initSM initPM hInit112 rfl rfl rfl rfl
  obtain ⟨h112_sm_shape, h400_shape, h401_shape, h402_shape, h403_shape, h112_rec⟩ := h112
  have ⟨h140_shape, h140_eq⟩ := initGoalHolds_replicated pm_goal_31.numRanks
    goal_36 140 [16, 8, 64, 16] initSM initPM hInit140 rfl rfl rfl
  have h140_pm_shape : (initPM 140).shape = [16, 8, 64, 16] := by rw [← h140_eq]; exact h140_shape
  -- Reconstruction → allGatherPrimDimN 1
  have h112_ag : initSM 112 = allGatherPrimDimN 1 4 0
      [initPM 400, initPM 401, initPM 402, initPM 403] := by
    rw [h112_rec]; apply reconstructWithDim_cons_cons_nonscalar; rw [h400_shape]; decide
  -- SM evaluation: output 135 = snd(bw_matmul) = batchedMatmul(transpose2d(112), 140)
  have hsm : (denoteGraph sm_goal_31 initSM) 135 =
      (bw_matmul (initSM 140) (initSM 112) (initSM 108)).2 := by
    simp [sm_goal_31, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hsm' : (bw_matmul (initSM 140) (initSM 112) (initSM 108)).2 =
      batchedMatmul (transpose2d (initSM 112)) (initSM 140) := rfl
  -- PM evaluation: each rank computes allToAllPrimWithDims, then bw_matmul, then allToAllPrimWithDims
  have hpm317 : (denoteGraph pm_goal_31 initPM) 317 =
      allToAllPrimWithDims 4 0 [batchedMatmul (transpose2d (allToAllPrimWithDims 4 0 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 1 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 2 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 3 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140)] 2 1 := by
    simp [pm_goal_31, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, bw_matmul, batchedMatmulBwd]
  have hpm319 : (denoteGraph pm_goal_31 initPM) 319 =
      allToAllPrimWithDims 4 1 [batchedMatmul (transpose2d (allToAllPrimWithDims 4 0 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 1 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 2 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 3 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140)] 2 1 := by
    simp [pm_goal_31, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, bw_matmul, batchedMatmulBwd]
  have hpm321 : (denoteGraph pm_goal_31 initPM) 321 =
      allToAllPrimWithDims 4 2 [batchedMatmul (transpose2d (allToAllPrimWithDims 4 0 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 1 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 2 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 3 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140)] 2 1 := by
    simp [pm_goal_31, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, bw_matmul, batchedMatmulBwd]
  have hpm323 : (denoteGraph pm_goal_31 initPM) 323 =
      allToAllPrimWithDims 4 3 [batchedMatmul (transpose2d (allToAllPrimWithDims 4 0 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 1 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 2 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
        batchedMatmul (transpose2d (allToAllPrimWithDims 4 3 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140)] 2 1 := by
    simp [pm_goal_31, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, bw_matmul, batchedMatmulBwd]
  -- Abbreviate the dy list
  set dy_list := [batchedMatmul (transpose2d (allToAllPrimWithDims 4 0 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
    batchedMatmul (transpose2d (allToAllPrimWithDims 4 1 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
    batchedMatmul (transpose2d (allToAllPrimWithDims 4 2 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140),
    batchedMatmul (transpose2d (allToAllPrimWithDims 4 3 [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140)]
  -- allToAllPrimWithDims 4 r [pm400,...] 1 3 = chunkPrimDimN 3 4 r (allGatherPrimDimN 1 4 0 [pm400,...])
  -- = chunkPrimDimN 3 4 r (initSM 112)
  have hata13_eq : ∀ r, allToAllPrimWithDims 4 r [initPM 400, initPM 401, initPM 402, initPM 403] 1 3 =
      chunkPrimDimN 3 4 r (initSM 112) := by
    intro r; simp only [allToAllPrimWithDims]; rw [h112_ag]
  -- Rewrite dy_list using td_chunk3 = chunk2_td
  have htd_shape : (transpose2d (initSM 112)).shape = [16, 8, 64, 64] :=
    transpose2d_4d_shape _ 16 8 64 64 h112_sm_shape
  have hdy_eq : ∀ r, batchedMatmul (transpose2d (allToAllPrimWithDims 4 r
      [initPM 400, initPM 401, initPM 402, initPM 403] 1 3)) (initPM 140) =
    batchedMatmul (chunkPrimDimN 2 4 r (transpose2d (initSM 112))) (initPM 140) := by
    intro r
    rw [hata13_eq r, td_chunk3_eq_chunk2_td _ r h112_sm_shape]
  have hdy_list_eq : dy_list =
      [batchedMatmul (chunkPrimDimN 2 4 0 (transpose2d (initSM 112))) (initPM 140),
       batchedMatmul (chunkPrimDimN 2 4 1 (transpose2d (initSM 112))) (initPM 140),
       batchedMatmul (chunkPrimDimN 2 4 2 (transpose2d (initSM 112))) (initPM 140),
       batchedMatmul (chunkPrimDimN 2 4 3 (transpose2d (initSM 112))) (initPM 140)] := by
    simp only [dy_list, hdy_eq]
  -- allToAllPrimWithDims 4 r dy_list 2 1 = chunkPrimDimN 1 4 r (allGatherPrimDimN 2 4 0 dy_list)
  have hata_eq : ∀ r, allToAllPrimWithDims 4 r dy_list 2 1 =
      chunkPrimDimN 1 4 r (allGatherPrimDimN 2 4 0 dy_list) := fun _ => rfl
  -- Reconstruction
  have hchunk2_shape : ∀ r, (chunkPrimDimN 2 4 r (transpose2d (initSM 112))).shape = [16, 8, 16, 64] := by
    intro r; rw [chunkPrimDimN_shape 2 4 r _ _ htd_shape (by omega)]; simp [List.set, List.getD]
  have hdy_shape : ∀ r, (batchedMatmul (chunkPrimDimN 2 4 r (transpose2d (initSM 112))) (initPM 140)).shape = [16, 8, 16, 16] :=
    fun r => batchedMatmul_4d_shape _ _ 16 8 16 64 16 (hchunk2_shape r) h140_pm_shape
  have hhead_dy : ((dy_list.head?.map (·.shape)).getD []) = [16, 8, 16, 16] := by
    rw [hdy_list_eq]; simp [List.head?, Option.map, hdy_shape 0]
  have hgathered_shape : (allGatherPrimDimN 2 4 0 dy_list).shape = [16, 8, 64, 16] := by
    rw [allGatherPrimDimN_shape 2 4 _ _ hhead_dy]; simp [List.set, List.getD]
  have hrecon_chunks :
      reconstructWithDim 1 4 0
        [allToAllPrimWithDims 4 0 dy_list 2 1,
         allToAllPrimWithDims 4 1 dy_list 2 1,
         allToAllPrimWithDims 4 2 dy_list 2 1,
         allToAllPrimWithDims 4 3 dy_list 2 1] =
      allGatherPrimDimN 1 4 0
        [chunkPrimDimN 1 4 0 (allGatherPrimDimN 2 4 0 dy_list),
         chunkPrimDimN 1 4 1 (allGatherPrimDimN 2 4 0 dy_list),
         chunkPrimDimN 1 4 2 (allGatherPrimDimN 2 4 0 dy_list),
         chunkPrimDimN 1 4 3 (allGatherPrimDimN 2 4 0 dy_list)] := by
    simp only [hata_eq]
    apply reconstructWithDim_cons_cons_nonscalar
    rw [chunkPrimDimN_shape 1 4 0 _ _ hgathered_shape (by omega)]
    simp [List.set, List.getD]
  -- Gather-chunk roundtrip on dim 1
  have hroundtrip_dim1 : allGatherPrimDimN 1 4 0
      [chunkPrimDimN 1 4 0 (allGatherPrimDimN 2 4 0 dy_list),
       chunkPrimDimN 1 4 1 (allGatherPrimDimN 2 4 0 dy_list),
       chunkPrimDimN 1 4 2 (allGatherPrimDimN 2 4 0 dy_list),
       chunkPrimDimN 1 4 3 (allGatherPrimDimN 2 4 0 dy_list)] =
    allGatherPrimDimN 2 4 0 dy_list :=
    gather_chunk_dim1 _ hgathered_shape
  -- bm distribution: allGatherPrimDimN 2 [bm(chunk2_r(td 112), 140)] = bm(td 112, 140)
  have hbm_dist : allGatherPrimDimN 2 4 0 dy_list =
      batchedMatmul (transpose2d (initSM 112)) (initPM 140) := by
    rw [hdy_list_eq]
    exact (bm_chunk2_dist (transpose2d (initSM 112)) (initPM 140) htd_shape h140_pm_shape).symm
  -- Unfold goal
  dsimp only [goal_31_stmt_cut, CoarseLineageHoldsWithInit, goal_31] at *
  simp only [List.map]
  rw [hsm, hsm', hpm317, hpm319, hpm321, hpm323]
  simp only [show pm_goal_31.numRanks = 4 from rfl]
  -- Shape proofs
  have hsm_shape : (batchedMatmul (transpose2d (initSM 112)) (initPM 140)).shape = [16, 8, 64, 16] :=
    batchedMatmul_4d_shape _ _ 16 8 64 64 16
      (transpose2d_4d_shape _ 16 8 64 64 h112_sm_shape) h140_pm_shape
  have hata21_shape : ∀ r, (allToAllPrimWithDims 4 r dy_list 2 1).shape = [16, 2, 64, 16] := by
    intro r; rw [allToAllPrimWithDims_shape 4 r dy_list 2 1 _ hhead_dy (by omega)]
    simp [List.set, List.getD]
  rw [hrecon_chunks, hroundtrip_dim1, hbm_dist, h140_eq]
  refine ⟨hsm_shape, ?_, ?_⟩
  · simp only [hata21_shape]
  · rfl

end TrainVerify.Denote.GeneratedGoals

