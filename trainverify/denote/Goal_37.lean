/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 37 (tensor id: 141)
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common

set_option linter.style.longLine false
set_option linter.flexible false

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_37 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_contiguous", ins := [142, 114], outs := [141] },
  ]

def pm_goal_37 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.AllToAllPrim", ins := [452, 453, 454, 455], outs := [472], params := [3, 0] },
    { rank := 0, op := "OpName.ChunkPrim", ins := [142], outs := [489], params := [0] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [452, 453, 454, 455], outs := [473], params := [3, 0] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [142], outs := [491], params := [0] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [452, 453, 454, 455], outs := [474], params := [3, 0] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [142], outs := [493], params := [0] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [452, 453, 454, 455], outs := [475], params := [3, 0] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [142], outs := [495], params := [0] },
    { rank := 0, op := "OpName.BW_contiguous", ins := [489, 472], outs := [488] },
    { rank := 1, op := "OpName.BW_contiguous", ins := [491, 473], outs := [490] },
    { rank := 2, op := "OpName.BW_contiguous", ins := [493, 474], outs := [492] },
    { rank := 3, op := "OpName.BW_contiguous", ins := [495, 475], outs := [494] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [488, 490, 492, 494], outs := [465], params := [0, 3] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [488, 490, 492, 494], outs := [467], params := [0, 3] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [488, 490, 492, 494], outs := [469], params := [0, 3] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [488, 490, 492, 494], outs := [471], params := [0, 3] },
  ]

def sm_goal_37InitShapes : List (Tid × Shape) := [
  (114, [16, 64, 8, 16]),
  (142, [16, 64, 8, 16]),
]

def sm_goal_37InitEnv : ShapeEnv := shapeEnvOfList sm_goal_37InitShapes

def pm_goal_37InitShapes : List (Tid × Shape) := [
  (142, [16, 64, 8, 16]),
  (452, [16, 64, 8, 4]),
  (453, [16, 64, 8, 4]),
  (454, [16, 64, 8, 4]),
  (455, [16, 64, 8, 4]),
]

def pm_goal_37InitEnv : ShapeEnv := shapeEnvOfList pm_goal_37InitShapes

def goal_37_cut_initGoals : List LineageGoal := initGoals ++ goal_37_prereqs

def goal_37_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_37 pm_goal_37 goal_37 sm_goal_37InitEnv pm_goal_37InitEnv goal_37_cut_initGoals

/-! ## Helper lemmas -/

private lemma valAt_gd0_4_64_8_16 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [4, 64, 8, 16])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 0 4 0 xs) idx =
    valAt (xs.getD (idx % 131072 / 8192 / 4) (zeroTensor [4, 64, 8, 16]))
      ((idx / 8192 % 4) * 8192 + idx % 8192) := by
  have hmm : idx % 131072 = idx := Nat.mod_eq_of_lt hidx
  have hdv : idx / 131072 = 0 := Nat.div_eq_of_lt hidx
  unfold allGatherPrimDimN; rw [hhead]
  simp [valAt, Tensor.mkShape, List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero,
    show (4 : Nat) * 4 = 16 from by norm_num,
    show (4 : Nat) * 8192 = 32768 from by norm_num,
    show (16 : Nat) * 8192 = 131072 from by norm_num,
    prodShape, hmm, hdv, dif_pos hidx]

private lemma valAt_chunk0_16_64_8_16 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 64, 8, 16]) (hr : r < 4) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 0 4 r x) idx = valAt x (r * 32768 + idx) := by
  have hps : prodShape x.shape = 131072 := by simp [hshape, prodShape]
  have hps_chunk : prodShape (chunkPrimDimN 0 4 r x).shape = 32768 := by
    simp [chunkPrimDimN, Tensor.mkShape, hshape, prodShape]
  have hfi_bound : r * 32768 + idx < 131072 := by omega
  rw [valAt_of_lt _ _ (by rw [hps_chunk]; exact hidx)]
  rw [valAt_of_lt _ _ (by rw [hps]; exact hfi_bound)]
  unfold chunkPrimDimN Tensor.mkShape
  simp only [hshape, List.getD, List.getElem?_cons_zero, Option.getD_some, List.drop, List.foldl]
  have : (16 : Nat) / 4 = 4 := by norm_num
  have : (64 : Nat) * 8 * 16 = 8192 := by norm_num
  have : (4 : Nat) * (64 * 8 * 16) = 32768 := by norm_num
  have : (16 : Nat) * (64 * 8 * 16) = 131072 := by norm_num
  have : (4 : Nat) * 8192 = 32768 := by norm_num
  have hne_4 : (4 : Nat) ≠ 0 := by omega
  have hne_8192 : (8192 : Nat) ≠ 0 := by omega
  have hne_32768 : (32768 : Nat) ≠ 0 := by omega
  have hne_131072 : (131072 : Nat) ≠ 0 := by omega
  simp only [*, Nat.one_mul, ite_false]
  have h0 : idx / 32768 = 0 := Nat.div_eq_of_lt hidx
  have hm : idx % 32768 = idx := Nat.mod_eq_of_lt hidx
  simp only [h0, hm, Nat.zero_mul, Nat.zero_add]
  rw [show r % 4 = r from Nat.mod_eq_of_lt hr]
  have heq : (r * 4 + idx / 8192) * 8192 + idx % 8192 = r * 32768 + idx := by omega
  rw [heq, valAt_of_lt _ _ (by rw [hps]; exact hfi_bound)]

private lemma valAt_gd3_16_64_8_4 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 64, 8, 4])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 3 4 0 xs) idx =
    valAt (xs.getD (idx % 16 / 4) (zeroTensor [16, 64, 8, 4]))
      (idx / 16 * 4 + idx % 16 % 4) := by
  have hshape_out : (allGatherPrimDimN 3 4 0 xs).shape = [16, 64, 8, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 3 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (4 : Nat) * 4 = 16 from by norm_num,
    show (4 : Nat) * 1 = 4 from by norm_num,
    (show (16 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    (show (4 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 16 / 1 / 4 = n % 16 / 4 from fun n => by omega,
    show ∀ n, n / 16 * 4 + (n % 16 / 1) % 4 * 1 + n % 16 % 1 =
      n / 16 * 4 + n % 16 % 4 from fun n => by omega]

private lemma valAt_chunk3_16_64_8_16 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 64, 8, 16]) (hr : r < 4) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 3 4 r x) idx = valAt x (idx / 4 * 16 + r * 4 + idx % 4) := by
  have hps : prodShape x.shape = 131072 := by simp [hshape, prodShape]
  have hps_chunk : prodShape (chunkPrimDimN 3 4 r x).shape = 32768 := by
    simp [chunkPrimDimN, Tensor.mkShape, hshape, prodShape]
  have hfi_bound : idx / 4 * 16 + r * 4 + idx % 4 < 131072 := by omega
  rw [valAt_of_lt _ _ (by rw [hps_chunk]; exact hidx)]
  rw [valAt_of_lt _ _ (by rw [hps]; exact hfi_bound)]
  unfold chunkPrimDimN Tensor.mkShape
  simp only [hshape, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD_some, List.drop, List.foldl]
  have : (16 : Nat) / 4 = 4 := by norm_num
  have : (1 : Nat) * 1 = 1 := by norm_num
  have hne_1 : (1 : Nat) ≠ 0 := by omega
  have hne_4 : (4 : Nat) ≠ 0 := by omega
  have h0 : idx / 4 / 8192 = 0 := by omega
  have hm : idx % 4 % 1 = 0 := by omega
  simp only [*, Nat.mul_one, Nat.add_zero, ite_false, Nat.div_one]
  rw [show r % 4 = r from Nat.mod_eq_of_lt hr]
  simp only [Nat.add_assoc]
  rw [valAt_of_lt _ _ (by rw [hps]; omega)]

/-! ## Gather-chunk roundtrips -/

private theorem gather_chunk_dim0 (x : Tensor) (hx : x.shape = [16, 64, 8, 16]) :
    allGatherPrimDimN 0 4 0 [chunkPrimDimN 0 4 0 x, chunkPrimDimN 0 4 1 x,
      chunkPrimDimN 0 4 2 x, chunkPrimDimN 0 4 3 x] = x := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 0 4 r x).shape = [4, 64, 8, 16] := by
    intro r; rw [chunkPrimDimN_shape 0 4 r _ _ hx (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 0 4 0 x, chunkPrimDimN 0 4 1 x,
      chunkPrimDimN 0 4 2 x, chunkPrimDimN 0 4 3 x].head?.map (·.shape)).getD []) =
      [4, 64, 8, 16] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 0 4 _ _ hhead]; simp [List.set, List.getD, hx]
  · intro idx hidx
    have hidx' : idx < 131072 := by
      rw [allGatherPrimDimN_shape 0 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_gd0_4_64_8_16 _ idx hhead hidx']
    have hmm : idx % 131072 = idx := Nat.mod_eq_of_lt hidx'
    simp only [hmm]
    set p := idx / 8192 / 4 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    have h_inner_lt : (idx / 8192 % 4) * 8192 + idx % 8192 < 32768 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
        Option.getD] <;>
      rw [valAt_chunk0_16_64_8_16 x _ _ hx (by omega) h_inner_lt] <;>
      exact congrArg (valAt x) (by omega)

private theorem gather_chunk_dim3 (x : Tensor) (hx : x.shape = [16, 64, 8, 16]) :
    allGatherPrimDimN 3 4 0 [chunkPrimDimN 3 4 0 x, chunkPrimDimN 3 4 1 x,
      chunkPrimDimN 3 4 2 x, chunkPrimDimN 3 4 3 x] = x := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 3 4 r x).shape = [16, 64, 8, 4] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ hx (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 3 4 0 x, chunkPrimDimN 3 4 1 x,
      chunkPrimDimN 3 4 2 x, chunkPrimDimN 3 4 3 x].head?.map (·.shape)).getD []) =
      [16, 64, 8, 4] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 3 4 _ _ hhead]; simp [List.set, List.getD, hx]
  · intro idx hidx
    have hidx' : idx < 131072 := by
      rw [allGatherPrimDimN_shape 3 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_gd3_16_64_8_4 _ idx hhead hidx']
    set p := idx % 16 / 4 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    have h_inner_lt : idx / 16 * 4 + idx % 16 % 4 < 32768 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
        Option.getD] <;>
      rw [valAt_chunk3_16_64_8_16 x _ _ hx (by omega) h_inner_lt] <;>
      exact congrArg (valAt x) (by omega)

/-! ## Main theorem -/

theorem prove_goal_37_cut : goal_37_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prereq: goal_38 (tensor 142 is singleton/replicated)
  have hInit142 : InitGoalHolds pm_goal_37.numRanks goal_38 initSM initPM := by
    apply hInitGoals; simp [goal_37_cut_initGoals, goal_37_prereqs, initGoals]
  have ⟨h142_shape, h142_eq⟩ := initGoalHolds_replicated 4 goal_38 142
    [16, 64, 8, 16] initSM initPM hInit142 rfl rfl rfl
  have h142_pm_shape : (initPM 142).shape = [16, 64, 8, 16] := by rw [← h142_eq]; exact h142_shape
  -- SM computation: BW_contiguous returns first arg unchanged
  have hsm : (denoteGraph sm_goal_37 initSM) 141 = initSM 142 := by
    simp [sm_goal_37, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- PM computation: last AllToAll[0,3] on chunks of 142
  have hpm465 : (denoteGraph pm_goal_37 initPM) 465 =
      allToAllPrimWithDims 4 0 [chunkPrimDimN 0 4 0 (initPM 142),
        chunkPrimDimN 0 4 1 (initPM 142), chunkPrimDimN 0 4 2 (initPM 142),
        chunkPrimDimN 0 4 3 (initPM 142)] 0 3 := by
    simp [pm_goal_37, denoteGraph, List.foldl, applyNode, evalOp, storeSet, allToAllPrimWithDims]
  have hpm467 : (denoteGraph pm_goal_37 initPM) 467 =
      allToAllPrimWithDims 4 1 [chunkPrimDimN 0 4 0 (initPM 142),
        chunkPrimDimN 0 4 1 (initPM 142), chunkPrimDimN 0 4 2 (initPM 142),
        chunkPrimDimN 0 4 3 (initPM 142)] 0 3 := by
    simp [pm_goal_37, denoteGraph, List.foldl, applyNode, evalOp, storeSet, allToAllPrimWithDims]
  have hpm469 : (denoteGraph pm_goal_37 initPM) 469 =
      allToAllPrimWithDims 4 2 [chunkPrimDimN 0 4 0 (initPM 142),
        chunkPrimDimN 0 4 1 (initPM 142), chunkPrimDimN 0 4 2 (initPM 142),
        chunkPrimDimN 0 4 3 (initPM 142)] 0 3 := by
    simp [pm_goal_37, denoteGraph, List.foldl, applyNode, evalOp, storeSet, allToAllPrimWithDims]
  have hpm471 : (denoteGraph pm_goal_37 initPM) 471 =
      allToAllPrimWithDims 4 3 [chunkPrimDimN 0 4 0 (initPM 142),
        chunkPrimDimN 0 4 1 (initPM 142), chunkPrimDimN 0 4 2 (initPM 142),
        chunkPrimDimN 0 4 3 (initPM 142)] 0 3 := by
    simp [pm_goal_37, denoteGraph, List.foldl, applyNode, evalOp, storeSet, allToAllPrimWithDims]
  -- Unfold allToAllPrimWithDims = chunkPrimDimN 3 ∘ allGatherPrimDimN 0
  simp only [allToAllPrimWithDims] at hpm465 hpm467 hpm469 hpm471
  -- Apply gather-chunk dim 0 roundtrip
  set G := allGatherPrimDimN 0 4 0
    [chunkPrimDimN 0 4 0 (initPM 142), chunkPrimDimN 0 4 1 (initPM 142),
     chunkPrimDimN 0 4 2 (initPM 142), chunkPrimDimN 0 4 3 (initPM 142)] with hG_def
  have hG_eq : G = initPM 142 := gather_chunk_dim0 (initPM 142) h142_pm_shape
  rw [hG_eq] at hpm465 hpm467 hpm469 hpm471
  -- Substitute initPM 142 = initSM 142
  rw [← h142_eq] at hpm465 hpm467 hpm469 hpm471
  -- Unfold the goal
  dsimp only [goal_37_stmt_cut, CoarseLineageHoldsWithInit, goal_37] at *
  simp only [List.map]
  rw [hsm, hpm465, hpm467, hpm469, hpm471]
  simp only [show pm_goal_37.numRanks = 4 from rfl]
  -- Reconstruction = allGatherPrimDimN 3
  have hrecon :
      reconstructWithDim 3 4 0
        [chunkPrimDimN 3 4 0 (initSM 142), chunkPrimDimN 3 4 1 (initSM 142),
         chunkPrimDimN 3 4 2 (initSM 142), chunkPrimDimN 3 4 3 (initSM 142)] =
      allGatherPrimDimN 3 4 0
        [chunkPrimDimN 3 4 0 (initSM 142), chunkPrimDimN 3 4 1 (initSM 142),
         chunkPrimDimN 3 4 2 (initSM 142), chunkPrimDimN 3 4 3 (initSM 142)] := by
    apply reconstructWithDim_cons_cons_nonscalar
    rw [chunkPrimDimN_shape 3 4 0 _ _ h142_shape (by omega)]
    simp [List.set, List.getD]
  rw [hrecon]
  -- Shapes + value equality
  have hchunk3_shape : ∀ r, (chunkPrimDimN 3 4 r (initSM 142)).shape = [16, 64, 8, 4] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ h142_shape (by omega)]; simp [List.set, List.getD]
  refine ⟨h142_shape, ?_, ?_⟩
  · simp only [hchunk3_shape]
  · exact (gather_chunk_dim3 (initSM 142) h142_shape).symm

end TrainVerify.Denote.GeneratedGoals
