/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 26 (tensor id: 130)
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common


namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_26 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_transpose", ins := [131, 103], outs := [130], params := [1, 2] },
  ]

def pm_goal_26 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.ChunkPrim", ins := [103], outs := [252], params := [3] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [103], outs := [253], params := [3] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [103], outs := [254], params := [3] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [103], outs := [255], params := [3] },
    { rank := 0, op := "OpName.BW_transpose", ins := [269, 252], outs := [268], params := [1, 2] },
    { rank := 1, op := "OpName.BW_transpose", ins := [271, 253], outs := [270], params := [1, 2] },
    { rank := 2, op := "OpName.BW_transpose", ins := [273, 254], outs := [272], params := [1, 2] },
    { rank := 3, op := "OpName.BW_transpose", ins := [275, 255], outs := [274], params := [1, 2] },
    { rank := 0, op := "OpName.AllGatherPrim", ins := [268, 270, 272, 274], outs := [130], params := [3] },
  ]

def sm_goal_26InitShapes : List (Tid × Shape) := [
  (103, [16, 64, 8, 16]),
  (131, [16, 8, 64, 16]),
]

def sm_goal_26InitEnv : ShapeEnv := shapeEnvOfList sm_goal_26InitShapes

def pm_goal_26InitShapes : List (Tid × Shape) := [
  (103, [16, 64, 8, 16]),
  (269, [16, 8, 64, 4]),
  (271, [16, 8, 64, 4]),
  (273, [16, 8, 64, 4]),
  (275, [16, 8, 64, 4]),
]

def pm_goal_26InitEnv : ShapeEnv := shapeEnvOfList pm_goal_26InitShapes

def goal_26_cut_initGoals : List LineageGoal := initGoals ++ goal_26_prereqs

def goal_26_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_26 pm_goal_26 goal_26 sm_goal_26InitEnv pm_goal_26InitEnv goal_26_cut_initGoals

/-! ### Helper: allGatherPrim = allGatherPrimDimN 3 for head shape [16, 64, 8, 4] -/
set_option linter.flexible false in
/-! ### Helper A: valAt of transposeAxes 1 2 for shape [16,8,64,16] -/
/-! ### Helper B: valAt of transposeAxes 1 2 for shape [16,8,64,4] -/
private lemma valAt_ta12_16_8_64_4 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 64, 4]) (hidx : idx < 32768) :
    valAt (transposeAxes 1 2 x) idx =
    valAt x (idx / 2048 * 2048 + idx % 32 / 4 * 256 +
             idx % 2048 / 32 * 4 + idx % 4) := by
  have hinner : idx / 2048 * 2048 + idx % 32 / 4 * 256 +
      idx % 2048 / 32 * 4 + idx % 4 < 32768 := by omega
  have hps_in : prodShape x.shape = 32768 := by simp [hshape, prodShape]
  have hout_shape : (transposeAxes 1 2 x).shape = [16, 64, 8, 4] := by
    simp [transposeAxes, Tensor.mkShape, listSwapAt, hshape]
  have hps_out : prodShape (transposeAxes 1 2 x).shape = 32768 := by
    rw [hout_shape]; simp [prodShape]
  rw [valAt_of_lt _ _ (by rw [hps_out]; exact hidx),
      valAt_of_lt _ _ (by rw [hps_in]; exact hinner)]
  unfold transposeAxes Tensor.mkShape
  simp only [hshape, listSwapAt, flatToMulti, valAt, List.getD, List.set,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  have ps1 := show prodShape [64, 8, 4] = 2048 from by simp [prodShape]
  have ps2 := show prodShape [8, 4] = 32 from by simp [prodShape]
  have ps3 := show prodShape [4] = 4 from by simp [prodShape]
  have ps4 := show prodShape ([] : List Nat) = 1 from by simp [prodShape]
  have ps5 := show prodShape [8, 64, 4] = 2048 from by simp [prodShape]
  have ps6 := show prodShape [64, 4] = 256 from by simp [prodShape]
  have hne_2048 : (2048 : Nat) ≠ 0 := by omega
  have hne_256 : (256 : Nat) ≠ 0 := by omega
  have hne_32 : (32 : Nat) ≠ 0 := by omega
  have hne_4 : (4 : Nat) ≠ 0 := by omega
  have hne_1 : (1 : Nat) ≠ 0 := by omega
  simp only [ps1, ps2, ps3, ps4,
    hne_2048, hne_32, hne_4, hne_1, Nat.div_one, ite_false]
  have hmm1 : ∀ n, n % 2048 % 32 = n % 32 := fun n => by omega
  have hmm2 : ∀ n, n % 2048 % 32 % 4 = n % 4 := fun n => by omega
  have ps_out := show prodShape [16, 8, 64, 4] = 32768 from by simp [prodShape]
  have hinner' : idx / 2048 * 2048 + (idx % 32 / 4 * 256 +
      (idx % 2048 / 32 * 4 + idx % 4)) < 32768 := by omega
  simp [hmm1, List.set, Option.getD_some, multiToFlat, prodShape, dif_pos hinner']
  simp only [Nat.add_assoc]

/-! ### Helper C: valAt of allGatherPrimDimN 3 4 0 for shard shape [16,8,64,4] -/
private lemma valAt_ag3_4_sh16_8_64_4 (xs : List Tensor) (fi : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 8, 64, 4])
    (hfi : fi < 131072) :
    valAt (allGatherPrimDimN 3 4 0 xs) fi =
    valAt (xs.getD ((fi % 16) / 4) (zeroTensor [16, 8, 64, 4]))
      ((fi / 16) * 4 + fi % 4) := by
  have hshape_out : (allGatherPrimDimN 3 4 0 xs).shape = [16, 8, 64, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : fi < prodShape (allGatherPrimDimN 3 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some,
    (show (16 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    (show (4 : Nat) ≠ 0 by omega), ite_false,
    Nat.div_one, Nat.mod_one, Nat.mul_one, Nat.add_zero,
    show fi % 16 % 4 = fi % 4 from by omega,
    show (4 : Nat) * 4 = 16 from by norm_num]

/-! ### Helper D: valAt of allGatherPrimDimN 3 4 0 for shard shape [16,64,8,4] -/
private lemma valAt_ag3_4_sh16_64_8_4 (xs : List Tensor) (fi : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 64, 8, 4])
    (hfi : fi < 131072) :
    valAt (allGatherPrimDimN 3 4 0 xs) fi =
    valAt (xs.getD ((fi % 16) / 4) (zeroTensor [16, 64, 8, 4]))
      ((fi / 16) * 4 + fi % 4) := by
  have hshape_out : (allGatherPrimDimN 3 4 0 xs).shape = [16, 64, 8, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : fi < prodShape (allGatherPrimDimN 3 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some,
    (show (16 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    (show (4 : Nat) ≠ 0 by omega), ite_false,
    Nat.div_one, Nat.mod_one, Nat.mul_one, Nat.add_zero,
    show fi % 16 % 4 = fi % 4 from by omega,
    show (4 : Nat) * 4 = 16 from by norm_num]

/-! ### Main commutativity: transposeAxes 1 2 commutes with allGatherPrimDimN 3 -/
private theorem transposeAxes_12_allGatherPrimDimN_3_comm
    (g0 g1 g2 g3 : Tensor)
    (h0 : g0.shape = [16, 8, 64, 4])
    (h1 : g1.shape = [16, 8, 64, 4])
    (h2 : g2.shape = [16, 8, 64, 4])
    (h3 : g3.shape = [16, 8, 64, 4]) :
    transposeAxes 1 2 (allGatherPrimDimN 3 4 0 [g0, g1, g2, g3]) =
    allGatherPrimDimN 3 4 0 [transposeAxes 1 2 g0, transposeAxes 1 2 g1,
                              transposeAxes 1 2 g2, transposeAxes 1 2 g3] := by
  -- Shape facts
  have hhead_in : (([g0, g1, g2, g3].head?.map (fun t => t.shape)).getD []) = [16, 8, 64, 4] := by
    simp [List.head?, Option.map, h0]
  have hLHS_gathered_shape : (allGatherPrimDimN 3 4 0 [g0, g1, g2, g3]).shape = [16, 8, 64, 16] := by
    rw [allGatherPrimDimN_shape 3 4 [g0, g1, g2, g3] [16, 8, 64, 4] hhead_in]
    simp [List.getD, List.set]
  have hLHS_shape : (transposeAxes 1 2 (allGatherPrimDimN 3 4 0 [g0, g1, g2, g3])).shape = [16, 64, 8, 16] := by
    simp [transposeAxes, Tensor.mkShape, listSwapAt, hLHS_gathered_shape]
  have ht_shape : ∀ t, t.shape = [16, 8, 64, 4] → (transposeAxes 1 2 t).shape = [16, 64, 8, 4] := by
    intro t ht; simp [transposeAxes, Tensor.mkShape, listSwapAt, ht]
  have hhead_out : (([transposeAxes 1 2 g0, transposeAxes 1 2 g1, transposeAxes 1 2 g2, transposeAxes 1 2 g3].head?.map (fun t => t.shape)).getD []) = [16, 64, 8, 4] := by
    simp [List.head?, Option.map, ht_shape g0 h0]
  have hRHS_shape : (allGatherPrimDimN 3 4 0 [transposeAxes 1 2 g0, transposeAxes 1 2 g1, transposeAxes 1 2 g2, transposeAxes 1 2 g3]).shape = [16, 64, 8, 16] := by
    rw [allGatherPrimDimN_shape 3 4 _ [16, 64, 8, 4] hhead_out]
    simp [List.getD, List.set]
  -- Element-wise equality
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  rw [hLHS_shape] at hidx
  have hidx' : idx < 131072 := by simpa [prodShape] using hidx
  -- Step 1: Rewrite LHS using valAt lemmas
  have hswap_bound : idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
      idx % 8192 / 128 * 16 + idx % 16 < 131072 := by omega
  rw [valAt_ta12_16_8_64_16 _ idx hLHS_gathered_shape hidx']
  rw [valAt_ag3_4_sh16_8_64_4 _ _ hhead_in hswap_bound]
  -- Step 2: Rewrite RHS using valAt lemmas
  rw [valAt_ag3_4_sh16_64_8_4 _ idx hhead_out hidx']
  -- Step 3: Show piece indices are equal
  -- LHS piece: ((idx/8192*8192 + idx%128/16*1024 + idx%8192/128*16 + idx%16) % 16) / 4
  -- RHS piece: (idx % 16) / 4
  -- They're equal because the first three terms are multiples of 16
  have h_piece_eq : ((idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
      idx % 8192 / 128 * 16 + idx % 16) % 16) / 4 = (idx % 16) / 4 := by omega
  rw [h_piece_eq]
  -- Step 4: Case-split on piece index
  have hr_cases : (idx % 16) / 4 = 0 ∨ (idx % 16) / 4 = 1 ∨
      (idx % 16) / 4 = 2 ∨ (idx % 16) / 4 = 3 := by omega
  have h_rhs_inner_bound : (idx / 16) * 4 + idx % 4 < 32768 := by omega
  -- Decompose the index equality into omega-friendly steps
  have h_si_mod4 : (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
      idx % 8192 / 128 * 16 + idx % 16) % 4 = idx % 4 := by omega
  have h_si_div16 : (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
      idx % 8192 / 128 * 16 + idx % 16) / 16 =
      idx / 8192 * 512 + idx % 128 / 16 * 64 + idx % 8192 / 128 := by omega
  have h_ii_div2048 : (idx / 16 * 4 + idx % 4) / 2048 = idx / 8192 := by omega
  have h_ii_mod32_div4 : (idx / 16 * 4 + idx % 4) % 32 / 4 = idx % 128 / 16 := by omega
  have h_ii_mod2048_div32 : (idx / 16 * 4 + idx % 4) % 2048 / 32 =
      idx % 8192 / 128 := by omega
  have h_ii_mod4 : (idx / 16 * 4 + idx % 4) % 4 = idx % 4 := by omega
  rcases hr_cases with h | h | h | h <;>
  · simp only [h, List.getD,
      List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
    -- RHS: apply transposeAxes valAt on piece
    rw [valAt_ta12_16_8_64_4 _ _ (by first | exact h0 | exact h1 | exact h2 | exact h3)
        h_rhs_inner_bound]
    -- Both sides are valAt g_r (index), close by arithmetic identity
    exact congr_arg (valAt _) (by
      rw [h_si_mod4, h_si_div16, h_ii_div2048, h_ii_mod32_div4,
          h_ii_mod2048_div32, h_ii_mod4]
      ring)

theorem prove_goal_26_cut : goal_26_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prerequisite goals
  have hInit131 : InitGoalHolds pm_goal_26.numRanks goal_27 initSM initPM := by
    apply hInitGoals; simp [goal_26_cut_initGoals, goal_26_prereqs, initGoals]
  have hInit103 : InitGoalHolds pm_goal_26.numRanks goal_5 initSM initPM := by
    apply hInitGoals; simp [goal_26_cut_initGoals, goal_26_prereqs, initGoals]
  -- Extract shapes
  have h131 := initGoalHolds_sharded4 4 goal_27 131 269 271 273 275
    [16, 8, 64, 16] [16, 8, 64, 4] initSM initPM hInit131 rfl rfl rfl rfl
  obtain ⟨h131_sm_shape, h269_shape, h271_shape, h273_shape, h275_shape, h131_rec⟩ := h131
  have ⟨h103_shape, h103_eq⟩ := initGoalHolds_replicated pm_goal_26.numRanks
    goal_5 103 [16, 64, 8, 16] initSM initPM hInit103 rfl rfl rfl
  have h103_pm_shape : (initPM 103).shape = [16, 64, 8, 16] := by rw [← h103_eq]; exact h103_shape
  -- Convert reconstruction to allGatherPrimDimN 3
  have h131_ag : initSM 131 = allGatherPrimDimN 3 4 0
      [initPM 269, initPM 271, initPM 273, initPM 275] := by
    rw [h131_rec]
    apply reconstructWithDim_cons_cons_nonscalar
    rw [h269_shape]; decide
  -- SM store evaluation
  have hsm : (denoteGraph sm_goal_26 initSM) 130 = transposeAxes 1 2 (initSM 131) := by
    simp [sm_goal_26, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- PM store evaluation
  have hpm : (denoteGraph pm_goal_26 initPM) 130 =
      allGatherPrimDimN 3 4 0 [transposeAxes 1 2 (initPM 269), transposeAxes 1 2 (initPM 271),
                          transposeAxes 1 2 (initPM 273), transposeAxes 1 2 (initPM 275)] := by
    simp [pm_goal_26, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      h103_pm_shape, transposeAxes, Tensor.mkShape, listSwapAt, h269_shape, h271_shape, h273_shape, h275_shape,
      chunkPrimDimN, List.set, List.getD, List.drop]
  -- Unfold goal
  dsimp only [goal_26_stmt_cut, CoarseLineageHoldsWithInit, goal_26] at *
  simp only [List.map]
  rw [hsm, hpm, h131_ag]
  -- Apply commutativity: transposeAxes 1 2 commutes with allGatherPrimDimN 3
  rw [transposeAxes_12_allGatherPrimDimN_3_comm _ _ _ _ h269_shape h271_shape h273_shape h275_shape]
  refine ⟨?_, ?_, ?_⟩
  · -- Shape of SM output
    simp [allGatherPrimDimN, Tensor.mkShape, transposeAxes, listSwapAt,
      h269_shape, List.head?, Option.map]
  · -- Shape of PM output
    simp [allGatherPrimDimN, Tensor.mkShape, transposeAxes, listSwapAt,
      h269_shape, List.head?, Option.map]
  · -- Value equality
    rw [reconstructWithDim_singleton]

end TrainVerify.Denote.GeneratedGoals
