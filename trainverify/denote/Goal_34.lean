/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 34 (tensor id: 138)
    SM: BW_softmax(139, 111) → [138]
    PM: AllToAll on forward output (dim 3→1),
        BW_softmax per rank,
        AllToAll on results (dim 1→3)
    Key: softmaxBwd distributes over dim-1 chunks (last dim preserved)
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common

set_option linter.flexible false

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_34 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_softmax", ins := [139, 111], outs := [138] },
  ]

def pm_goal_34 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.AllToAllPrim", ins := [376, 377, 378, 379], outs := [396], params := [3, 1] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [376, 377, 378, 379], outs := [397], params := [3, 1] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [376, 377, 378, 379], outs := [398], params := [3, 1] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [376, 377, 378, 379], outs := [399], params := [3, 1] },
    { rank := 0, op := "OpName.BW_softmax", ins := [413, 396], outs := [412] },
    { rank := 1, op := "OpName.BW_softmax", ins := [415, 397], outs := [414] },
    { rank := 2, op := "OpName.BW_softmax", ins := [417, 398], outs := [416] },
    { rank := 3, op := "OpName.BW_softmax", ins := [419, 399], outs := [418] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [412, 414, 416, 418], outs := [389], params := [1, 3] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [412, 414, 416, 418], outs := [391], params := [1, 3] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [412, 414, 416, 418], outs := [393], params := [1, 3] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [412, 414, 416, 418], outs := [395], params := [1, 3] },
  ]

def sm_goal_34InitShapes : List (Tid × Shape) := [
  (111, [16, 8, 64, 64]),
  (139, [16, 8, 64, 64]),
]

def sm_goal_34InitEnv : ShapeEnv := shapeEnvOfList sm_goal_34InitShapes

def pm_goal_34InitShapes : List (Tid × Shape) := [
  (376, [16, 8, 64, 16]),
  (377, [16, 8, 64, 16]),
  (378, [16, 8, 64, 16]),
  (379, [16, 8, 64, 16]),
  (413, [16, 2, 64, 64]),
  (415, [16, 2, 64, 64]),
  (417, [16, 2, 64, 64]),
  (419, [16, 2, 64, 64]),
]

def pm_goal_34InitEnv : ShapeEnv := shapeEnvOfList pm_goal_34InitShapes

def goal_34_cut_initGoals : List LineageGoal := initGoals ++ goal_34_prereqs

def goal_34_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_34 pm_goal_34 goal_34 sm_goal_34InitEnv pm_goal_34InitEnv goal_34_cut_initGoals

/-! ## Part 1: valAt helper for chunkPrimDimN 1 on [16,8,64,64] -/

private lemma valAt_chunk1_8_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 1 4 r x) idx =
    valAt x ((idx / 8192) * 32768 + (r % 4 * 2 + (idx / 4096) % 2) * 4096 + idx % 4096) := by
  have hout_shape : (chunkPrimDimN 1 4 r x).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 1 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num
  simp only [show ∀ n, n % 8192 / 4096 = (n / 4096) % 2 from fun n => by omega,
    Nat.add_assoc]

/-! ## Part 2: valAt helper for chunkPrimDimN 3 on [16,8,64,64] -/

private lemma valAt_chunk3_8_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 3 4 r x) idx =
    valAt x (idx / 16 * 64 + r % 4 * 16 + idx % 16) := by
  have hout_shape : (chunkPrimDimN 3 4 r x).shape = [16, 8, 64, 16] := by
    rw [chunkPrimDimN_shape 3 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 3 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num
  simp only [Nat.mod_one, Nat.add_zero, Nat.add_assoc]

/-! ## Part 3: valAt helpers for allGatherPrimDimN -/

private lemma valAt_ag3_16_8_64_16 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 8, 64, 16])
    (hidx : idx < 524288) :
    valAt (allGatherPrimDimN 3 4 0 xs) idx =
    valAt (xs.getD (idx % 64 / 16) (zeroTensor [16, 8, 64, 16]))
      (idx / 64 * 16 + idx % 16) := by
  have hshape_out : (allGatherPrimDimN 3 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 3 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 16 = 16 from by norm_num,
    show (16 : Nat) * 4 = 64 from by norm_num,
    (show (64 : Nat) ≠ 0 by omega), (show (16 : Nat) ≠ 0 by omega),
    (show (1 : Nat) ≠ 0 by omega),
    ite_false, Nat.mod_one, Nat.add_zero, Nat.div_one, Nat.mul_one]
  simp only [show ∀ n, n % 64 % 16 = n % 16 from fun n => by omega]

private lemma valAt_ag1_16_2_64_64 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 2, 64, 64])
    (hidx : idx < 524288) :
    valAt (allGatherPrimDimN 1 4 0 xs) idx =
    valAt (xs.getD (idx % 32768 / 8192) (zeroTensor [16, 2, 64, 64]))
      (idx / 32768 * 8192 + (idx % 32768 / 4096 % 2) * 4096 + idx % 4096) := by
  have hshape_out : (allGatherPrimDimN 1 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 1 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 64 * 64 = 4096 from by norm_num,
    show (2 : Nat) * 4 = 8 from by norm_num,
    show (8 : Nat) * (1 * 64 * 64) = 32768 from by norm_num,
    show (2 : Nat) * (1 * 64 * 64) = 8192 from by norm_num,
    (show (32768 : Nat) ≠ 0 by omega), (show (4096 : Nat) ≠ 0 by omega),
    (show (2 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 32768 / 4096 / 2 = n % 32768 / 8192 from fun n => by omega,
    show ∀ n, n % 32768 % 4096 = n % 4096 from fun n => by omega,
    Nat.add_assoc]

/-! ## Part 4: valAt helpers for bw_softmax -/

private lemma valAt_bw_softmax_16_8 (g y : Tensor) (idx : Nat)
    (hy : y.shape = [16, 8, 64, 64]) (hidx : idx < 524288) :
    valAt (bw_softmax g y) idx =
    valAt y idx * (valAt g idx -
      ∑ j ∈ Finset.range 64, valAt y (idx / 64 * 64 + j) * valAt g (idx / 64 * 64 + j)) := by
  unfold bw_softmax softmaxBwd; rw [hy]
  simp only [List.reverse, List.reverseAux, Tensor.mkShape]
  rw [valAt_of_lt _ _ (by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (64 : Nat) ≠ 0 from by omega, ite_false]
  simp only [show idx / 64 * 64 + idx % 64 = idx from by omega]

private lemma valAt_bw_softmax_16_2 (g y : Tensor) (idx : Nat)
    (hy : y.shape = [16, 2, 64, 64]) (hidx : idx < 131072) :
    valAt (bw_softmax g y) idx =
    valAt y idx * (valAt g idx -
      ∑ j ∈ Finset.range 64, valAt y (idx / 64 * 64 + j) * valAt g (idx / 64 * 64 + j)) := by
  unfold bw_softmax softmaxBwd; rw [hy]
  simp only [List.reverse, List.reverseAux, Tensor.mkShape]
  rw [valAt_of_lt _ _ (by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (64 : Nat) ≠ 0 from by omega, ite_false]
  simp only [show idx / 64 * 64 + idx % 64 = idx from by omega]

/-! ## Part 5: softmaxBwd distributes over chunkPrimDimN 1 -/

-- Key arithmetic: chunk1 base+j preservation
private lemma chunk1_base_eq (r idx j : Nat) (_ : idx < 131072) (_ : j < 64) :
    (idx / 64 * 64 + j) / 8192 * 32768 +
    (r % 4 * 2 + ((idx / 64 * 64 + j) / 4096) % 2) * 4096 +
    (idx / 64 * 64 + j) % 4096 =
    (idx / 8192 * 32768 + (r % 4 * 2 + (idx / 4096) % 2) * 4096 + idx % 4096) / 64 * 64 + j := by
  omega

private theorem softmaxBwd_chunk1_comm (G Y : Tensor) (r : Nat)
    (hG : G.shape = [16, 8, 64, 64]) (hY : Y.shape = [16, 8, 64, 64]) :
    bw_softmax (chunkPrimDimN 1 4 r G) (chunkPrimDimN 1 4 r Y) =
    chunkPrimDimN 1 4 r (bw_softmax G Y) := by
  have hchunkY : (chunkPrimDimN 1 4 r Y).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hY (by omega)]; simp [List.set, List.getD]
  have hchunkG : (chunkPrimDimN 1 4 r G).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hG (by omega)]; simp [List.set, List.getD]
  have hbw_shape : (bw_softmax G Y).shape = [16, 8, 64, 64] := by
    unfold bw_softmax softmaxBwd; rw [hY]
    simp [List.reverse, List.reverseAux, Tensor.mkShape]
  have hLHS_shape : (bw_softmax (chunkPrimDimN 1 4 r G) (chunkPrimDimN 1 4 r Y)).shape = [16, 2, 64, 64] := by
    unfold bw_softmax softmaxBwd; rw [hchunkY]
    simp [List.reverse, List.reverseAux, Tensor.mkShape]
  have hRHS_shape : (chunkPrimDimN 1 4 r (bw_softmax G Y)).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hbw_shape (by omega)]; simp [List.set, List.getD]
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  have hidx' : idx < 131072 := by rw [hLHS_shape] at hidx; simp [prodShape] at hidx; omega
  -- LHS: expand bw_softmax of chunks
  rw [valAt_bw_softmax_16_2 _ _ idx hchunkY hidx']
  -- RHS: first chunk, then expand bw_softmax
  rw [valAt_chunk1_8_64_64 (bw_softmax G Y) r idx hbw_shape hidx']
  have hm_lt : idx / 8192 * 32768 + (r % 4 * 2 + (idx / 4096) % 2) * 4096 + idx % 4096 < 524288 := by omega
  rw [valAt_bw_softmax_16_8 G Y _ hY hm_lt]
  -- Rewrite standalone chunk valAt to original tensor valAt
  rw [valAt_chunk1_8_64_64 Y r idx hY hidx',
      valAt_chunk1_8_64_64 G r idx hG hidx']
  -- congr 1 auto-closes valAt Y m = valAt Y m, leaving the subtraction
  congr 1
  -- congr 1 auto-closes valAt G m = valAt G m, leaving the sum
  congr 1
  apply Finset.sum_congr rfl
  intro j hj
  have hj64 : j < 64 := Finset.mem_range.mp hj
  have hbase_j_lt : idx / 64 * 64 + j < 131072 := by omega
  rw [valAt_chunk1_8_64_64 Y r _ hY hbase_j_lt,
      valAt_chunk1_8_64_64 G r _ hG hbase_j_lt,
      chunk1_base_eq r idx j hidx' hj64]

/-! ## Part 6: Chunk-of-gather roundtrip (dim 1) -/
private lemma chunk1_of_gather1 (xs : List Tensor) (r : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 2, 64, 64])
    (hr : r < 4)
    (hshapes : ∀ i, i < 4 → (xs.getD i (zeroTensor [16, 2, 64, 64])).shape = [16, 2, 64, 64]) :
    chunkPrimDimN 1 4 r (allGatherPrimDimN 1 4 0 xs) =
    xs.getD r (zeroTensor [16, 2, 64, 64]) := by
  have hag_shape : (allGatherPrimDimN 1 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hchunk_shape : (chunkPrimDimN 1 4 r (allGatherPrimDimN 1 4 0 xs)).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hag_shape (by omega)]; simp [List.set, List.getD]
  apply Tensor.ext
  · rw [hchunk_shape, hshapes r hr]
  · intro idx hidx
    have hidx' : idx < 131072 := by rw [hchunk_shape] at hidx; simp [prodShape] at hidx; omega
    rw [valAt_chunk1_8_64_64 _ r idx hag_shape hidx']
    rw [valAt_ag1_16_2_64_64 _ _ hhead (by omega)]
    -- piece index = r, mapped index = idx
    exact congrArg₂ valAt (by congr 1; omega) (by omega)

/-! ## Part 7: Gather-chunk roundtrips -/

-- Gather-chunk roundtrip dim 3 on [16,8,64,64]
private theorem gather_chunk_dim3 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 64]) :
    allGatherPrimDimN 3 4 0 [chunkPrimDimN 3 4 0 T, chunkPrimDimN 3 4 1 T,
      chunkPrimDimN 3 4 2 T, chunkPrimDimN 3 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 3 4 r T).shape = [16, 8, 64, 16] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 3 4 0 T, chunkPrimDimN 3 4 1 T,
      chunkPrimDimN 3 4 2 T, chunkPrimDimN 3 4 3 T].head?.map (·.shape)).getD []) = [16, 8, 64, 16] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 3 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 524288 := by
      rw [allGatherPrimDimN_shape 3 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag3_16_8_64_16 _ idx hhead hidx']
    set p := idx % 64 / 16 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk3_8_64_64 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

-- Gather-chunk roundtrip dim 1 on [16,8,64,64]
private theorem gather_chunk_dim1 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 64]) :
    allGatherPrimDimN 1 4 0 [chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 1 4 r T).shape = [16, 2, 64, 64] := by
    intro r; rw [chunkPrimDimN_shape 1 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T].head?.map (·.shape)).getD []) =
      [16, 2, 64, 64] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 1 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 524288 := by
      rw [allGatherPrimDimN_shape 1 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag1_16_2_64_64 _ idx hhead hidx']
    set p := idx % 32768 / 8192 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk1_8_64_64 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Part 8: Main proof -/
theorem prove_goal_34_cut : goal_34_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prerequisites
  have hInit111 : InitGoalHolds pm_goal_34.numRanks goal_13 initSM initPM :=
    hInitGoals goal_13 (by simp [goal_34_cut_initGoals, goal_34_prereqs, initGoals])
  have hInit139 : InitGoalHolds pm_goal_34.numRanks goal_35 initSM initPM :=
    hInitGoals goal_35 (by simp [goal_34_cut_initGoals, goal_34_prereqs, initGoals])
  -- Extract shapes and reconstruction for tensor 111 (sharded dim 3)
  have h111 := initGoalHolds_sharded4 4 goal_13 111 376 377 378 379
    [16, 8, 64, 64] [16, 8, 64, 16] initSM initPM hInit111 rfl rfl rfl rfl
  obtain ⟨h111_sm_shape, h376_shape, h377_shape, h378_shape, h379_shape, h111_rec⟩ := h111
  -- Extract shapes and reconstruction for tensor 139 (sharded dim 1)
  have h139 := initGoalHolds_sharded4 4 goal_35 139 413 415 417 419
    [16, 8, 64, 64] [16, 2, 64, 64] initSM initPM hInit139 rfl rfl rfl rfl
  obtain ⟨h139_sm_shape, h413_shape, h415_shape, h417_shape, h419_shape, h139_rec⟩ := h139
  -- Reconstruction → allGatherPrimDimN
  have h111_ag : initSM 111 = allGatherPrimDimN 3 4 0
      [initPM 376, initPM 377, initPM 378, initPM 379] := by
    rw [h111_rec]; apply reconstructWithDim_cons_cons_nonscalar; rw [h376_shape]; decide
  have h139_ag : initSM 139 = allGatherPrimDimN 1 4 0
      [initPM 413, initPM 415, initPM 417, initPM 419] := by
    rw [h139_rec]; apply reconstructWithDim_cons_cons_nonscalar; rw [h413_shape]; decide
  -- SM evaluation
  have hsm : (denoteGraph sm_goal_34 initSM) 138 =
      bw_softmax (initSM 139) (initSM 111) := by
    simp [sm_goal_34, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- PM evaluation: first AllToAll converts dim-3 shards to dim-1 chunks
  have hata31_eq : ∀ r, allToAllPrimWithDims 4 r [initPM 376, initPM 377, initPM 378, initPM 379] 3 1 =
      chunkPrimDimN 1 4 r (initSM 111) := by
    intro r; simp only [allToAllPrimWithDims]; rw [h111_ag]
  -- initPM (413+2r) = chunkPrimDimN 1 4 r (initSM 139) via chunk-of-gather roundtrip
  have h_grad_head : ([initPM 413, initPM 415, initPM 417, initPM 419].head?.map (·.shape)).getD [] = [16, 2, 64, 64] := by
    simp [List.head?, Option.map, h413_shape]
  have h_grad_shapes : ∀ i, i < 4 → ([initPM 413, initPM 415, initPM 417, initPM 419].getD i (zeroTensor [16, 2, 64, 64])).shape = [16, 2, 64, 64] := by
    intro i hi
    have : i = 0 ∨ i = 1 ∨ i = 2 ∨ i = 3 := by omega
    rcases this with rfl | rfl | rfl | rfl <;>
      simp [List.getD, h413_shape, h415_shape, h417_shape, h419_shape]
  -- Prove each initPM = chunk of initSM 139
  have hgrad0 : initPM 413 = chunkPrimDimN 1 4 0 (initSM 139) := by
    rw [h139_ag]; have h := chunk1_of_gather1 _ 0 h_grad_head (by omega) h_grad_shapes
    simp [List.getD] at h; exact h.symm
  have hgrad1 : initPM 415 = chunkPrimDimN 1 4 1 (initSM 139) := by
    rw [h139_ag]; have h := chunk1_of_gather1 _ 1 h_grad_head (by omega) h_grad_shapes
    simp [List.getD] at h; exact h.symm
  have hgrad2 : initPM 417 = chunkPrimDimN 1 4 2 (initSM 139) := by
    rw [h139_ag]; have h := chunk1_of_gather1 _ 2 h_grad_head (by omega) h_grad_shapes
    simp [List.getD] at h; exact h.symm
  have hgrad3 : initPM 419 = chunkPrimDimN 1 4 3 (initSM 139) := by
    rw [h139_ag]; have h := chunk1_of_gather1 _ 3 h_grad_head (by omega) h_grad_shapes
    simp [List.getD] at h; exact h.symm
  -- PM output: denoteGraph produces allToAllPrimWithDims 4 r [bw_softmax results] 1 3
  have hpm389 : (denoteGraph pm_goal_34 initPM) 389 =
      allToAllPrimWithDims 4 0 [bw_softmax (initPM 413) (allToAllPrimWithDims 4 0 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 415) (allToAllPrimWithDims 4 1 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 417) (allToAllPrimWithDims 4 2 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 419) (allToAllPrimWithDims 4 3 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1)] 1 3 := by
    simp [pm_goal_34, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm391 : (denoteGraph pm_goal_34 initPM) 391 =
      allToAllPrimWithDims 4 1 [bw_softmax (initPM 413) (allToAllPrimWithDims 4 0 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 415) (allToAllPrimWithDims 4 1 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 417) (allToAllPrimWithDims 4 2 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 419) (allToAllPrimWithDims 4 3 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1)] 1 3 := by
    simp [pm_goal_34, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm393 : (denoteGraph pm_goal_34 initPM) 393 =
      allToAllPrimWithDims 4 2 [bw_softmax (initPM 413) (allToAllPrimWithDims 4 0 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 415) (allToAllPrimWithDims 4 1 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 417) (allToAllPrimWithDims 4 2 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 419) (allToAllPrimWithDims 4 3 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1)] 1 3 := by
    simp [pm_goal_34, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm395 : (denoteGraph pm_goal_34 initPM) 395 =
      allToAllPrimWithDims 4 3 [bw_softmax (initPM 413) (allToAllPrimWithDims 4 0 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 415) (allToAllPrimWithDims 4 1 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 417) (allToAllPrimWithDims 4 2 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
        bw_softmax (initPM 419) (allToAllPrimWithDims 4 3 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1)] 1 3 := by
    simp [pm_goal_34, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- Abbreviate the BW_softmax output list
  set bw_list := [bw_softmax (initPM 413) (allToAllPrimWithDims 4 0 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
    bw_softmax (initPM 415) (allToAllPrimWithDims 4 1 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
    bw_softmax (initPM 417) (allToAllPrimWithDims 4 2 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1),
    bw_softmax (initPM 419) (allToAllPrimWithDims 4 3 [initPM 376, initPM 377, initPM 378, initPM 379] 3 1)]
  -- Rewrite bw_list using distribution
  have hbw_list_eq : bw_list =
      [chunkPrimDimN 1 4 0 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 1 4 1 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 1 4 2 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 1 4 3 (bw_softmax (initSM 139) (initSM 111))] := by
    simp only [bw_list, hata31_eq, hgrad0, hgrad1, hgrad2, hgrad3]
    congr 1
    · exact softmaxBwd_chunk1_comm _ _ 0 h139_sm_shape h111_sm_shape
    congr 1
    · exact softmaxBwd_chunk1_comm _ _ 1 h139_sm_shape h111_sm_shape
    congr 1
    · exact softmaxBwd_chunk1_comm _ _ 2 h139_sm_shape h111_sm_shape
    congr 1
    · exact softmaxBwd_chunk1_comm _ _ 3 h139_sm_shape h111_sm_shape
  -- allToAllPrimWithDims 4 r bw_list 1 3 = chunkPrimDimN 3 4 r (allGatherPrimDimN 1 4 0 bw_list)
  have hata_eq : ∀ r, allToAllPrimWithDims 4 r bw_list 1 3 =
      chunkPrimDimN 3 4 r (allGatherPrimDimN 1 4 0 bw_list) := fun _ => rfl
  -- Gather-chunk roundtrip dim 1
  have hbw_sm_shape : (bw_softmax (initSM 139) (initSM 111)).shape = [16, 8, 64, 64] := by
    unfold bw_softmax softmaxBwd; rw [h111_sm_shape]
    simp [List.reverse, List.reverseAux, Tensor.mkShape]
  have hroundtrip_dim1 : allGatherPrimDimN 1 4 0 bw_list =
      bw_softmax (initSM 139) (initSM 111) := by
    rw [hbw_list_eq]
    exact gather_chunk_dim1 _ hbw_sm_shape
  -- Unfold goal
  dsimp only [goal_34_stmt_cut, CoarseLineageHoldsWithInit, goal_34] at *
  simp only [List.map]
  rw [hsm, hpm389, hpm391, hpm393, hpm395]
  simp only [show pm_goal_34.numRanks = 4 from rfl]
  -- Shape proofs
  have hata13_shape : ∀ r, (allToAllPrimWithDims 4 r bw_list 1 3).shape = [16, 8, 64, 16] := by
    intro r; rw [hata_eq, hroundtrip_dim1]
    rw [chunkPrimDimN_shape 3 4 r _ _ hbw_sm_shape (by omega)]
    simp [List.set, List.getD]
  -- Reconstruction
  have hrecon : reconstructWithDim 3 4 0
      [allToAllPrimWithDims 4 0 bw_list 1 3,
       allToAllPrimWithDims 4 1 bw_list 1 3,
       allToAllPrimWithDims 4 2 bw_list 1 3,
       allToAllPrimWithDims 4 3 bw_list 1 3] =
    allGatherPrimDimN 3 4 0
      [chunkPrimDimN 3 4 0 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 1 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 2 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 3 (bw_softmax (initSM 139) (initSM 111))] := by
    simp only [hata_eq, hroundtrip_dim1]
    apply reconstructWithDim_cons_cons_nonscalar
    rw [chunkPrimDimN_shape 3 4 0 _ _ hbw_sm_shape (by omega)]
    simp [List.set, List.getD]
  have hroundtrip_dim3 : allGatherPrimDimN 3 4 0
      [chunkPrimDimN 3 4 0 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 1 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 2 (bw_softmax (initSM 139) (initSM 111)),
       chunkPrimDimN 3 4 3 (bw_softmax (initSM 139) (initSM 111))] =
    bw_softmax (initSM 139) (initSM 111) :=
    gather_chunk_dim3 _ hbw_sm_shape
  rw [hrecon, hroundtrip_dim3]
  refine ⟨?_, ?_, ?_⟩
  · exact hbw_sm_shape
  · simp only [hata13_shape]
  · rfl

end TrainVerify.Denote.GeneratedGoals
