/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 35 (tensor id: 139)
    SM: BW_matmul(140, 112, 108) → [139, 135]
    PM: AllToAll on inputs (dim 1→2, dim 1→3),
        BW_matmul per rank,
        AllToAll on dx outputs (dim 3→1)
    Key: batchedMatmul(g, transpose2d(chunk2(y))) = chunk3(batchedMatmul(g, transpose2d(y)))
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common

set_option linter.style.longLine false
set_option linter.flexible false

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_35 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_matmul", ins := [140, 112, 108], outs := [139, 135] },
  ]

def pm_goal_35 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [424], params := [1, 2] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [420], params := [1, 3] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [425], params := [1, 2] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [421], params := [1, 3] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [426], params := [1, 2] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [422], params := [1, 3] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [304, 305, 306, 307], outs := [427], params := [1, 2] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [400, 401, 402, 403], outs := [423], params := [1, 3] },
    { rank := 0, op := "OpName.BW_matmul", ins := [140, 420, 424], outs := [440, 441] },
    { rank := 1, op := "OpName.BW_matmul", ins := [140, 421, 425], outs := [442, 443] },
    { rank := 2, op := "OpName.BW_matmul", ins := [140, 422, 426], outs := [444, 445] },
    { rank := 3, op := "OpName.BW_matmul", ins := [140, 423, 427], outs := [446, 447] },
    { rank := 0, op := "OpName.AllToAllPrim", ins := [440, 442, 444, 446], outs := [413], params := [3, 1] },
    { rank := 1, op := "OpName.AllToAllPrim", ins := [440, 442, 444, 446], outs := [415], params := [3, 1] },
    { rank := 2, op := "OpName.AllToAllPrim", ins := [440, 442, 444, 446], outs := [417], params := [3, 1] },
    { rank := 3, op := "OpName.AllToAllPrim", ins := [440, 442, 444, 446], outs := [419], params := [3, 1] },
  ]

def sm_goal_35InitShapes : List (Tid × Shape) := [
  (108, [16, 8, 64, 16]),
  (112, [16, 8, 64, 64]),
  (140, [16, 8, 64, 16]),
]

def sm_goal_35InitEnv : ShapeEnv := shapeEnvOfList sm_goal_35InitShapes

def pm_goal_35InitShapes : List (Tid × Shape) := [
  (140, [16, 8, 64, 16]),
  (304, [16, 2, 64, 16]),
  (305, [16, 2, 64, 16]),
  (306, [16, 2, 64, 16]),
  (307, [16, 2, 64, 16]),
  (400, [16, 2, 64, 64]),
  (401, [16, 2, 64, 64]),
  (402, [16, 2, 64, 64]),
  (403, [16, 2, 64, 64]),
]

def pm_goal_35InitEnv : ShapeEnv := shapeEnvOfList pm_goal_35InitShapes

def goal_35_cut_initGoals : List LineageGoal := initGoals ++ goal_35_prereqs

def goal_35_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_35 pm_goal_35 goal_35 sm_goal_35InitEnv pm_goal_35InitEnv goal_35_cut_initGoals

/-! ## Shape helpers -/

private theorem batchedMatmul_4d_shape (x y : Tensor) (d0 d1 n k m : Nat)
    (hx : x.shape = [d0, d1, n, k]) (hy : y.shape = [d0, d1, k, m]) :
    (batchedMatmul x y).shape = [d0, d1, n, m] := by
  unfold batchedMatmul; rw [hx, hy]; simp [Tensor.mkShape]

private theorem transpose2d_4d_shape (x : Tensor) (d0 d1 d2 d3 : Nat)
    (hx : x.shape = [d0, d1, d2, d3]) :
    (transpose2d x).shape = [d0, d1, d3, d2] := by
  unfold transpose2d; rw [hx]; simp [Tensor.mkShape]

/-! ## Part 1: valAt helpers for batchedMatmul -/

-- batchedMatmul [16,8,64,16] @ [16,8,16,16] → [16,8,64,16]
private lemma valAt_bm_pm (A B : Tensor) (idx : Nat)
    (hA : A.shape = [16, 8, 64, 16]) (hB : B.shape = [16, 8, 16, 16])
    (hidx : idx < 131072) :
    valAt (batchedMatmul A B) idx =
    ∑ l ∈ Finset.range 16,
      valAt A (idx / 1024 * 1024 + (idx % 1024) / 16 * 16 + l) *
      valAt B (idx / 1024 * 256 + l * 16 + idx % 16) := by
  cases A with | mk sA vA =>
  cases B with | mk sB vB =>
  simp only at hA hB; subst hA; subst hB
  unfold batchedMatmul
  dsimp only [List.reverse, List.reverseAux, List.append]
  simp only [valAt, Tensor.mkShape]
  rw [dif_pos (show idx < prodShape ([16, 8, 64, 16] : Shape) from by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (64 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (16 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (16 : Nat) * 16 = 256 from by norm_num]
  simp_rw [show idx % (64 * 16) % 16 = idx % 16 from by omega]

-- batchedMatmul [16,8,64,16] @ [16,8,16,64] → [16,8,64,64]
private lemma valAt_bm_sm (A B : Tensor) (idx : Nat)
    (hA : A.shape = [16, 8, 64, 16]) (hB : B.shape = [16, 8, 16, 64])
    (hidx : idx < 524288) :
    valAt (batchedMatmul A B) idx =
    ∑ l ∈ Finset.range 16,
      valAt A (idx / 4096 * 1024 + (idx % 4096) / 64 * 16 + l) *
      valAt B (idx / 4096 * 1024 + l * 64 + idx % 64) := by
  cases A with | mk sA vA =>
  cases B with | mk sB vB =>
  simp only at hA hB; subst hA; subst hB
  unfold batchedMatmul
  dsimp only [List.reverse, List.reverseAux, List.append]
  simp only [valAt, Tensor.mkShape]
  rw [dif_pos (show idx < prodShape ([16, 8, 64, 64] : Shape) from by simp [prodShape]; omega)]
  dsimp only []
  simp only [show (64 : Nat) * 64 ≠ 0 from by omega, ite_false,
    show (64 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (64 : Nat) * 64 = 4096 from by norm_num]
  simp_rw [show idx % (64 * 64) % 64 = idx % 64 from by omega]

/-! ## Part 2: valAt helpers for allGatherPrimDimN -/

-- allGatherPrimDimN 3 with shard shape [16,8,64,16] → [16,8,64,64]
private lemma valAt_ag3_np4 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 8, 64, 16])
    (hidx : idx < 524288) :
    valAt (allGatherPrimDimN 3 4 0 xs) idx =
    valAt (xs.getD (idx % 64 / 16) (zeroTensor [16, 8, 64, 16]))
      (idx / 64 * 16 + idx % 16) := by
  have hshape_out : (allGatherPrimDimN 3 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 3 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (16 : Nat) * 4 = 64 from by norm_num,
    show (16 : Nat) * 1 = 16 from by norm_num,
    (show (64 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    (show (16 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 64 / 1 / 16 = n % 64 / 16 from fun n => by omega,
    show ∀ n, n / 64 * 16 + (n % 64 / 1) % 16 * 1 + n % 64 % 1 =
      n / 64 * 16 + n % 16 from fun n => by omega]

-- allGatherPrimDimN 1 with shard shape [16,2,64,64] → [16,8,64,64]
private lemma valAt_ag1_np4 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 2, 64, 64])
    (hidx : idx < 524288) :
    valAt (allGatherPrimDimN 1 4 0 xs) idx =
    valAt (xs.getD (idx % 32768 / 4096 / 2) (zeroTensor [16, 2, 64, 64]))
      (idx / 32768 * 8192 + (idx % 32768 / 4096 % 2) * 4096 + idx % 4096) := by
  have hshape_out : (allGatherPrimDimN 1 4 0 xs).shape = [16, 8, 64, 64] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 1 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 64 = 64 from by norm_num,
    show (64 : Nat) * 64 = 4096 from by norm_num,
    show (2 : Nat) * 4 = 8 from by norm_num,
    show (8 : Nat) * 4096 = 32768 from by norm_num,
    show (2 : Nat) * 4096 = 8192 from by norm_num,
    (show (32768 : Nat) ≠ 0 by omega), (show (4096 : Nat) ≠ 0 by omega),
    (show (2 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 32768 / 4096 / 2 = n % 32768 / 8192 from fun n => by omega,
    show ∀ n, n % 32768 % 4096 = n % 4096 from fun n => by omega]

/-! ## Part 3: valAt helpers for chunkPrimDimN -/

-- chunkPrimDimN 2 4 r on [16,8,64,16] → [16,8,16,16]
private lemma valAt_chunk2_64_16 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 16]) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 2 4 r x) idx =
    valAt x (idx / 256 * 1024 + (r % 4 * 16 + idx % 256 / 16) * 16 + idx % 16) := by
  have hout_shape : (chunkPrimDimN 2 4 r x).shape = [16, 8, 16, 16] := by
    rw [chunkPrimDimN_shape 2 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 2 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num

-- chunkPrimDimN 3 4 r on [16,8,16,64] → [16,8,16,16]
private lemma valAt_chunk3_16_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 16, 64]) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 3 4 r x) idx =
    valAt x (idx / 16 * 64 + r % 4 * 16 + idx % 16) := by
  have hout_shape : (chunkPrimDimN 3 4 r x).shape = [16, 8, 16, 16] := by
    rw [chunkPrimDimN_shape 3 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 3 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num
  simp only [Nat.mod_one, Nat.add_zero, Nat.add_assoc]

-- chunkPrimDimN 3 4 r on [16,8,64,64] → [16,8,64,16]
private lemma valAt_chunk3_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 3 4 r x) idx =
    valAt x (idx / 16 * 64 + r % 4 * 16 + idx % 16) := by
  have hout_shape : (chunkPrimDimN 3 4 r x).shape = [16, 8, 64, 16] := by
    rw [chunkPrimDimN_shape 3 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 3 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num
  simp only [Nat.mod_one, Nat.add_zero, Nat.add_assoc]

-- chunkPrimDimN 1 4 r on [16,8,64,64] → [16,2,64,64]
private lemma valAt_chunk1_64_64 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 8, 64, 64]) (hidx : idx < 131072) :
    valAt (chunkPrimDimN 1 4 r x) idx =
    valAt x (idx / 8192 * 32768 + (r % 4 * 2 + idx % 8192 / 4096) * 4096 + idx % 4096) := by
  have hout_shape : (chunkPrimDimN 1 4 r x).shape = [16, 2, 64, 64] := by
    rw [chunkPrimDimN_shape 1 4 r _ _ hshape (by omega)]; simp [List.set, List.getD]
  have hlt_prod : idx < prodShape (chunkPrimDimN 1 4 r x).shape := by
    simp only [hout_shape, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [chunkPrimDimN, Tensor.mkShape, hshape,
    List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl]
  norm_num

/-! ## Part 4: valAt helpers for transpose2d -/

-- transpose2d on [16,8,16,16]: d1=16, d0=16, innerSize=256
private lemma valAt_td_16_8_16_16 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 16, 16]) (hidx : idx < 32768) :
    valAt (transpose2d x) idx =
    valAt x (idx / 256 * 256 + (idx % 256 % 16) * 16 + idx % 256 / 16) := by
  unfold transpose2d; rw [hshape]
  simp only [List.reverse, List.reverseAux, valAt, Tensor.mkShape]
  have hps : prodShape [16, 8, 16, 16] = 32768 := by simp [prodShape]
  rw [dif_pos (by rw [hps]; exact hidx)]
  simp only [show (16 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (16 : Nat) ≠ 0 from by omega,
    show (16 : Nat) * 16 = 256 from by norm_num]

-- transpose2d on [16,8,64,16]: d1=16, d0=64, innerSize=1024
private lemma valAt_td_16_8_64_16 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 64, 16]) (hidx : idx < 131072) :
    valAt (transpose2d x) idx =
    valAt x (idx / 1024 * 1024 + (idx % 1024 % 64) * 16 + idx % 1024 / 64) := by
  unfold transpose2d; rw [hshape]
  simp only [List.reverse, List.reverseAux, valAt, Tensor.mkShape]
  have hps : prodShape [16, 8, 16, 64] = 131072 := by simp [prodShape]
  rw [dif_pos (by rw [hps]; exact hidx)]
  simp only [show (64 : Nat) * 16 ≠ 0 from by omega, ite_false,
    show (64 : Nat) ≠ 0 from by omega,
    show (64 : Nat) * 16 = 1024 from by norm_num]

/-! ## Part 5: transpose2d commutes with chunkPrimDimN 2 → chunkPrimDimN 3 -/

-- Extracted index equality for td_chunk2 ↔ chunk3_td commutativity
private lemma g35_td_chunk_idx_eq (idx r : Nat) (_ : idx < 32768) :
    (idx / 256 * 256 + idx % 256 % 16 * 16 + idx % 256 / 16) / 256 * 1024 +
    (r % 4 * 16 + (idx / 256 * 256 + idx % 256 % 16 * 16 + idx % 256 / 16) % 256 / 16) * 16 +
    (idx / 256 * 256 + idx % 256 % 16 * 16 + idx % 256 / 16) % 16 =
    (idx / 16 * 64 + r % 4 * 16 + idx % 16) / 1024 * 1024 +
    ((idx / 16 * 64 + r % 4 * 16 + idx % 16) % 1024 % 64) * 16 +
    (idx / 16 * 64 + r % 4 * 16 + idx % 16) % 1024 / 64 := by
  -- Pre-simplify nested mod/div
  have hmod16 : idx % 256 % 16 = idx % 16 := by omega
  have hdivmod : idx % 256 / 16 = (idx / 16) % 16 := by omega
  simp only [hmod16, hdivmod]
  set j := idx / 256 * 256 + idx % 16 * 16 + (idx / 16) % 16
  set k := idx / 16 * 64 + r % 4 * 16 + idx % 16
  have hj_div : j / 256 = idx / 256 := by omega
  have hj_mod_div : j % 256 / 16 = idx % 16 := by omega
  have hj_mod16 : j % 16 = (idx / 16) % 16 := by omega
  have hk_div : k / 1024 = idx / 256 := by omega
  have hk_mod_mod : k % 1024 % 64 = r % 4 * 16 + idx % 16 := by omega
  have hk_mod_div : k % 1024 / 64 = (idx / 16) % 16 := by omega
  rw [hj_div, hj_mod_div, hj_mod16, hk_div, hk_mod_mod, hk_mod_div]

-- td(chunk2_r(y)) = chunk3_r(td(y)) for y shape [16,8,64,16]
private theorem td_chunk2_eq_chunk3_td (y : Tensor) (r : Nat)
    (hy : y.shape = [16, 8, 64, 16]) :
    transpose2d (chunkPrimDimN 2 4 r y) = chunkPrimDimN 3 4 r (transpose2d y) := by
  have hchunk2_shape : (chunkPrimDimN 2 4 r y).shape = [16, 8, 16, 16] := by
    rw [chunkPrimDimN_shape 2 4 r _ _ hy (by omega)]; simp [List.set, List.getD]
  have htd_shape : (transpose2d y).shape = [16, 8, 16, 64] :=
    transpose2d_4d_shape y 16 8 64 16 hy
  have hLHS_shape : (transpose2d (chunkPrimDimN 2 4 r y)).shape = [16, 8, 16, 16] :=
    transpose2d_4d_shape _ 16 8 16 16 hchunk2_shape
  have hRHS_shape : (chunkPrimDimN 3 4 r (transpose2d y)).shape = [16, 8, 16, 16] := by
    rw [chunkPrimDimN_shape 3 4 r _ _ htd_shape (by omega)]; simp [List.set, List.getD]
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  have hidx' : idx < 32768 := by
    rw [hLHS_shape] at hidx; simp [prodShape] at hidx; omega
  -- LHS: transpose2d(chunkPrimDimN 2 r y)
  rw [valAt_td_16_8_16_16 _ idx hchunk2_shape hidx']
  rw [valAt_chunk2_64_16 y r _ hy (by omega)]
  -- RHS: chunkPrimDimN 3 r (transpose2d y)
  rw [valAt_chunk3_16_64 (transpose2d y) r idx htd_shape hidx']
  rw [valAt_td_16_8_64_16 y _ hy (by omega)]
  exact congrArg (valAt y) (g35_td_chunk_idx_eq idx r hidx')

/-! ## Part 6: Distribution: allGather3([bm(g, chunk3_r(B))]) = bm(g, B) -/

-- Extracted index equalities for the distribution identity
private lemma g35_bm_g_idx_eq (idx l : Nat) (_ : idx < 524288) (_ : l < 16) :
    (idx / 64 * 16 + idx % 16) / 1024 * 1024 +
    ((idx / 64 * 16 + idx % 16) % 1024) / 16 * 16 + l =
    idx / 4096 * 1024 + (idx % 4096) / 64 * 16 + l := by
  set inner := idx / 64 * 16 + idx % 16
  have h1 : inner / 1024 = idx / 4096 := by omega
  have h2 : inner % 1024 / 16 = idx % 4096 / 64 := by omega
  rw [h1, h2]

private lemma g35_bm_B_idx_eq (idx l r : Nat) (_ : idx < 524288) (_ : l < 16)
    (hr : idx % 64 / 16 = r) :
    ((idx / 64 * 16 + idx % 16) / 1024 * 256 + l * 16 + (idx / 64 * 16 + idx % 16) % 16) / 16 * 64 +
    r % 4 * 16 + ((idx / 64 * 16 + idx % 16) / 1024 * 256 + l * 16 + (idx / 64 * 16 + idx % 16) % 16) % 16 =
    idx / 4096 * 1024 + l * 64 + idx % 64 := by
  set inner := idx / 64 * 16 + idx % 16
  have h_inner_mod16 : inner % 16 = idx % 16 := by omega
  set pos := inner / 1024 * 256 + l * 16 + inner % 16
  have h_pos_div : pos / 16 = inner / 1024 * 16 + l := by omega
  have h_pos_mod : pos % 16 = inner % 16 := by omega
  rw [h_pos_div, h_pos_mod, h_inner_mod16, ← hr]
  have h_inner_div : inner / 1024 = idx / 4096 := by omega
  rw [h_inner_div]
  omega

-- Combined distribution identity
set_option maxRecDepth 4096 in
private theorem bm_g_chunk3_dist
    (g B : Tensor)
    (hg : g.shape = [16, 8, 64, 16]) (hB : B.shape = [16, 8, 16, 64]) :
    allGatherPrimDimN 3 4 0 [batchedMatmul g (chunkPrimDimN 3 4 0 B),
                              batchedMatmul g (chunkPrimDimN 3 4 1 B),
                              batchedMatmul g (chunkPrimDimN 3 4 2 B),
                              batchedMatmul g (chunkPrimDimN 3 4 3 B)] =
    batchedMatmul g B := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 3 4 r B).shape = [16, 8, 16, 16] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ hB (by omega)]; simp [List.set, List.getD]
  have hbm_chunk_shape : ∀ r, (batchedMatmul g (chunkPrimDimN 3 4 r B)).shape = [16, 8, 64, 16] :=
    fun r => batchedMatmul_4d_shape _ _ 16 8 64 16 16 hg (hchunk_shape r)
  have hhead_bm : (([batchedMatmul g (chunkPrimDimN 3 4 0 B),
      batchedMatmul g (chunkPrimDimN 3 4 1 B),
      batchedMatmul g (chunkPrimDimN 3 4 2 B),
      batchedMatmul g (chunkPrimDimN 3 4 3 B)].head?.map (·.shape)).getD []) = [16, 8, 64, 16] := by
    simp [List.head?, Option.map, hbm_chunk_shape 0]
  have hLHS_shape : (allGatherPrimDimN 3 4 0 [batchedMatmul g (chunkPrimDimN 3 4 0 B),
      batchedMatmul g (chunkPrimDimN 3 4 1 B),
      batchedMatmul g (chunkPrimDimN 3 4 2 B),
      batchedMatmul g (chunkPrimDimN 3 4 3 B)]).shape = [16, 8, 64, 64] := by
    rw [allGatherPrimDimN_shape 3 4 _ _ hhead_bm]; simp [List.set, List.getD]
  have hRHS_shape : (batchedMatmul g B).shape = [16, 8, 64, 64] :=
    batchedMatmul_4d_shape _ _ 16 8 64 16 64 hg hB
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  have hidx' : idx < 524288 := by rw [hLHS_shape] at hidx; simp [prodShape] at hidx; omega
  -- LHS: allGather3([bm_r])
  rw [valAt_ag3_np4 _ idx hhead_bm hidx']
  -- RHS: bm(g, B)
  rw [valAt_bm_sm g B idx hg hB hidx']
  set p := idx % 64 / 16 with hp_def
  have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
  have h_inner_lt : idx / 64 * 16 + idx % 16 < 131072 := by omega
  rcases hp_range with hp | hp | hp | hp <;> {
    simp only [hp, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD]
    rw [valAt_bm_pm g _ _ hg (hchunk_shape _) h_inner_lt]
    apply Finset.sum_congr rfl
    intro l hl
    have hl16 : l < 16 := Finset.mem_range.mp hl
    congr 1
    · -- g indices: show they match
      exact congrArg (valAt g) (g35_bm_g_idx_eq idx l hidx' hl16)
    · -- B indices: unfold chunk3, show they match
      rw [valAt_chunk3_16_64 B _ _ hB (by omega)]
      exact congrArg (valAt B) (g35_bm_B_idx_eq idx l _ hidx' hl16 (hp_def.symm.trans hp))
  }

/-! ## Part 7: Gather-chunk dim 3 roundtrip on [16,8,64,64] -/

private theorem gather_chunk_dim3 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 64]) :
    allGatherPrimDimN 3 4 0 [chunkPrimDimN 3 4 0 T, chunkPrimDimN 3 4 1 T,
      chunkPrimDimN 3 4 2 T, chunkPrimDimN 3 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 3 4 r T).shape = [16, 8, 64, 16] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 3 4 0 T, chunkPrimDimN 3 4 1 T,
      chunkPrimDimN 3 4 2 T, chunkPrimDimN 3 4 3 T].head?.map (·.shape)).getD []) =
      [16, 8, 64, 16] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 3 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 524288 := by
      rw [allGatherPrimDimN_shape 3 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag3_np4 _ idx hhead hidx']
    set p := idx % 64 / 16 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk3_64_64 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Part 8: Gather-chunk dim 1 roundtrip on [16,8,64,64] -/

private theorem gather_chunk_dim1_64_64 (T : Tensor)
    (hT : T.shape = [16, 8, 64, 64]) :
    allGatherPrimDimN 1 4 0 [chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 1 4 r T).shape = [16, 2, 64, 64] := by
    intro r; rw [chunkPrimDimN_shape 1 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 1 4 0 T, chunkPrimDimN 1 4 1 T,
      chunkPrimDimN 1 4 2 T, chunkPrimDimN 1 4 3 T].head?.map (·.shape)).getD []) =
      [16, 2, 64, 64] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 1 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 524288 := by
      rw [allGatherPrimDimN_shape 1 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_ag1_np4 _ idx hhead hidx']
    set p := idx % 32768 / 4096 / 2 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD] <;>
      rw [valAt_chunk1_64_64 T _ _ hT (by omega)] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Part 9: Main proof -/

theorem prove_goal_35_cut : goal_35_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prerequisites
  have hInit108 : InitGoalHolds pm_goal_35.numRanks goal_10 initSM initPM :=
    hInitGoals goal_10 (by simp [goal_35_cut_initGoals, goal_35_prereqs, initGoals])
  have hInit112 : InitGoalHolds pm_goal_35.numRanks goal_14 initSM initPM :=
    hInitGoals goal_14 (by simp [goal_35_cut_initGoals, goal_35_prereqs, initGoals])
  have hInit140 : InitGoalHolds pm_goal_35.numRanks goal_36 initSM initPM :=
    hInitGoals goal_36 (by simp [goal_35_cut_initGoals, goal_35_prereqs, initGoals])
  -- Extract shapes and reconstruction
  have h108 := initGoalHolds_sharded4 4 goal_10 108 304 305 306 307
    [16, 8, 64, 16] [16, 2, 64, 16] initSM initPM hInit108 rfl rfl rfl rfl
  obtain ⟨h108_sm_shape, h304_shape, h305_shape, h306_shape, h307_shape, h108_rec⟩ := h108
  have h112 := initGoalHolds_sharded4 4 goal_14 112 400 401 402 403
    [16, 8, 64, 64] [16, 2, 64, 64] initSM initPM hInit112 rfl rfl rfl rfl
  obtain ⟨h112_sm_shape, h400_shape, h401_shape, h402_shape, h403_shape, h112_rec⟩ := h112
  have ⟨h140_shape, h140_eq⟩ := initGoalHolds_replicated pm_goal_35.numRanks
    goal_36 140 [16, 8, 64, 16] initSM initPM hInit140 rfl rfl rfl
  have h140_pm_shape : (initPM 140).shape = [16, 8, 64, 16] := by rw [← h140_eq]; exact h140_shape
  -- Reconstruction → allGatherPrimDimN 1
  have h108_ag : initSM 108 = allGatherPrimDimN 1 4 0
      [initPM 304, initPM 305, initPM 306, initPM 307] := by
    rw [h108_rec]; apply reconstructWithDim_cons_cons_nonscalar; rw [h304_shape]; decide
  -- SM evaluation: output 139 = fst(bw_matmul) = batchedMatmul(140, transpose2d(108))
  have hsm : (denoteGraph sm_goal_35 initSM) 139 =
      (bw_matmul (initSM 140) (initSM 112) (initSM 108)).1 := by
    simp [sm_goal_35, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hsm' : (bw_matmul (initSM 140) (initSM 112) (initSM 108)).1 =
      batchedMatmul (initSM 140) (transpose2d (initSM 108)) := rfl
  -- PM: AllToAllPrimWithDims on 108 shards: chunk2_r(initSM 108)
  have hata12_eq : ∀ r, allToAllPrimWithDims 4 r
      [initPM 304, initPM 305, initPM 306, initPM 307] 1 2 =
      chunkPrimDimN 2 4 r (initSM 108) := by
    intro r; simp only [allToAllPrimWithDims]; rw [h108_ag]
  -- PM evaluation: each rank computes bw_matmul then AllToAll[3,1]
  have hpm413 : (denoteGraph pm_goal_35 initPM) 413 =
      allToAllPrimWithDims 4 0 [batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 0 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 1 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 2 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 3 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2))] 3 1 := by
    simp [pm_goal_35, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, batchedMatmulBwd]
  have hpm415 : (denoteGraph pm_goal_35 initPM) 415 =
      allToAllPrimWithDims 4 1 [batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 0 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 1 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 2 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 3 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2))] 3 1 := by
    simp [pm_goal_35, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, batchedMatmulBwd]
  have hpm417 : (denoteGraph pm_goal_35 initPM) 417 =
      allToAllPrimWithDims 4 2 [batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 0 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 1 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 2 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 3 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2))] 3 1 := by
    simp [pm_goal_35, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, batchedMatmulBwd]
  have hpm419 : (denoteGraph pm_goal_35 initPM) 419 =
      allToAllPrimWithDims 4 3 [batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 0 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 1 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 2 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
        batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 3 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2))] 3 1 := by
    simp [pm_goal_35, denoteGraph, List.foldl, applyNode, evalOp, storeSet,
      allToAllPrimWithDims, batchedMatmulBwd]
  -- Abbreviate the dx list
  set dx_list := [batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 0 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
    batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 1 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
    batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 2 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)),
    batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 3 [initPM 304, initPM 305, initPM 306, initPM 307] 1 2))]
  -- Rewrite dx_list using td_chunk2 = chunk3_td
  have htd_shape : (transpose2d (initSM 108)).shape = [16, 8, 16, 64] :=
    transpose2d_4d_shape _ 16 8 64 16 h108_sm_shape
  have hdx_eq : ∀ r, batchedMatmul (initPM 140) (transpose2d (allToAllPrimWithDims 4 r
      [initPM 304, initPM 305, initPM 306, initPM 307] 1 2)) =
    batchedMatmul (initPM 140) (chunkPrimDimN 3 4 r (transpose2d (initSM 108))) := by
    intro r
    rw [hata12_eq r, td_chunk2_eq_chunk3_td _ r h108_sm_shape]
  have hdx_list_eq : dx_list =
      [batchedMatmul (initPM 140) (chunkPrimDimN 3 4 0 (transpose2d (initSM 108))),
       batchedMatmul (initPM 140) (chunkPrimDimN 3 4 1 (transpose2d (initSM 108))),
       batchedMatmul (initPM 140) (chunkPrimDimN 3 4 2 (transpose2d (initSM 108))),
       batchedMatmul (initPM 140) (chunkPrimDimN 3 4 3 (transpose2d (initSM 108)))] := by
    simp only [dx_list, hdx_eq]
  -- allToAllPrimWithDims 4 r dx_list 3 1 = chunkPrimDimN 1 4 r (allGatherPrimDimN 3 4 0 dx_list)
  have hata_eq : ∀ r, allToAllPrimWithDims 4 r dx_list 3 1 =
      chunkPrimDimN 1 4 r (allGatherPrimDimN 3 4 0 dx_list) := fun _ => rfl
  -- Reconstruction
  have hchunk3_shape : ∀ r, (chunkPrimDimN 3 4 r (transpose2d (initSM 108))).shape = [16, 8, 16, 16] := by
    intro r; rw [chunkPrimDimN_shape 3 4 r _ _ htd_shape (by omega)]; simp [List.set, List.getD]
  have hdx_shape : ∀ r, (batchedMatmul (initPM 140) (chunkPrimDimN 3 4 r (transpose2d (initSM 108)))).shape = [16, 8, 64, 16] :=
    fun r => batchedMatmul_4d_shape _ _ 16 8 64 16 16 h140_pm_shape (hchunk3_shape r)
  have hhead_dx : ((dx_list.head?.map (·.shape)).getD []) = [16, 8, 64, 16] := by
    rw [hdx_list_eq]; simp [List.head?, Option.map, hdx_shape 0]
  have hgathered_shape : (allGatherPrimDimN 3 4 0 dx_list).shape = [16, 8, 64, 64] := by
    rw [allGatherPrimDimN_shape 3 4 _ _ hhead_dx]; simp [List.set, List.getD]
  have hrecon_chunks :
      reconstructWithDim 1 4 0
        [allToAllPrimWithDims 4 0 dx_list 3 1,
         allToAllPrimWithDims 4 1 dx_list 3 1,
         allToAllPrimWithDims 4 2 dx_list 3 1,
         allToAllPrimWithDims 4 3 dx_list 3 1] =
      allGatherPrimDimN 1 4 0
        [chunkPrimDimN 1 4 0 (allGatherPrimDimN 3 4 0 dx_list),
         chunkPrimDimN 1 4 1 (allGatherPrimDimN 3 4 0 dx_list),
         chunkPrimDimN 1 4 2 (allGatherPrimDimN 3 4 0 dx_list),
         chunkPrimDimN 1 4 3 (allGatherPrimDimN 3 4 0 dx_list)] := by
    simp only [hata_eq]
    apply reconstructWithDim_cons_cons_nonscalar
    rw [chunkPrimDimN_shape 1 4 0 _ _ hgathered_shape (by omega)]
    simp [List.set, List.getD]
  -- Gather-chunk roundtrip on dim 1
  have hroundtrip_dim1 : allGatherPrimDimN 1 4 0
      [chunkPrimDimN 1 4 0 (allGatherPrimDimN 3 4 0 dx_list),
       chunkPrimDimN 1 4 1 (allGatherPrimDimN 3 4 0 dx_list),
       chunkPrimDimN 1 4 2 (allGatherPrimDimN 3 4 0 dx_list),
       chunkPrimDimN 1 4 3 (allGatherPrimDimN 3 4 0 dx_list)] =
    allGatherPrimDimN 3 4 0 dx_list :=
    gather_chunk_dim1_64_64 _ hgathered_shape
  -- bm distribution: allGatherPrimDimN 3 [bm(140, chunk3_r(td 108))] = bm(140, td 108)
  have hbm_dist : allGatherPrimDimN 3 4 0 dx_list =
      batchedMatmul (initPM 140) (transpose2d (initSM 108)) := by
    rw [hdx_list_eq]
    exact bm_g_chunk3_dist (initPM 140) (transpose2d (initSM 108)) h140_pm_shape htd_shape
  -- Unfold goal
  dsimp only [goal_35_stmt_cut, CoarseLineageHoldsWithInit, goal_35] at *
  simp only [List.map]
  rw [hsm, hsm', hpm413, hpm415, hpm417, hpm419]
  simp only [show pm_goal_35.numRanks = 4 from rfl]
  -- Shape proofs
  have hsm_shape : (batchedMatmul (initPM 140) (transpose2d (initSM 108))).shape = [16, 8, 64, 64] :=
    batchedMatmul_4d_shape _ _ 16 8 64 16 64 h140_pm_shape htd_shape
  have hata31_shape : ∀ r, (allToAllPrimWithDims 4 r dx_list 3 1).shape = [16, 2, 64, 64] := by
    intro r; rw [allToAllPrimWithDims_shape 4 r dx_list 3 1 _ hhead_dx (by omega)]
    simp [List.set, List.getD]
  rw [hrecon_chunks, hroundtrip_dim1, hbm_dist, h140_eq]
  refine ⟨hsm_shape, ?_, ?_⟩
  · simp only [hata31_shape]
  · rfl

end TrainVerify.Denote.GeneratedGoals
