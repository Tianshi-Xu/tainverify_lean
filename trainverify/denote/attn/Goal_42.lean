/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 42 (tensor id: 159)
-/
import denote.GeneratedData

open TrainVerify.Denote
open TrainVerify.Denote.Generated

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_42 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.FW_multiref", ins := [121], outs := [159, 163, 167] },
  ]

def pm_goal_42 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158] },
    { rank := 1, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158] },
    { rank := 2, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158] },
    { rank := 3, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158] },
    { rank := 0, op := "OpName.ChunkPrim", ins := [156], outs := [168] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [156], outs := [169] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [156], outs := [170] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [156], outs := [171] },
  ]

def sm_goal_42InitShapes : List (Tid × Shape) := [
  (121, [16, 64, 128]),
]

def sm_goal_42InitEnv : ShapeEnv := shapeEnvOfList sm_goal_42InitShapes

def pm_goal_42InitShapes : List (Tid × Shape) := [
  (121, [16, 64, 128]),
]

def pm_goal_42InitEnv : ShapeEnv := shapeEnvOfList pm_goal_42InitShapes

def goal_42_cut_initGoals : List LineageGoal := initGoals

def goal_42_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_42 pm_goal_42 goal_42 sm_goal_42InitEnv pm_goal_42InitEnv goal_42_cut_initGoals

-- The core semantic lemma: allGatherPrimDim0 of chunkPrimDim0 chunks reconstructs the original tensor
-- This requires a lemma analogous to allGatherPrim_chunkPrim_eq but for dim0.
-- For now, we leave this as a sorry and note the proof strategy.

theorem prove_goal_42_cut : goal_42_stmt_cut := by
  intro initSM initPM hsmShapes hpmShapes hInitGoals
  -- Goal structure:
  -- SM: FW_multiref(121) -> 159 (159 = initSM 121)
  -- PM: FW_multiref(121) -> 156 then ChunkPrimDim0(156) -> 168+r for each rank r
  -- Need: initSM 121 = allGatherPrimDim0 4 0 [chunk0, chunk1, chunk2, chunk3]
  -- where chunk_r = chunkPrimDim0 4 r (initPM 121) and initSM 121 = initPM 121 (from initGoal_121)
  --
  -- This is exactly the inverse relationship: allGatherPrimDim0 ∘ chunkPrimDim0 = id
  -- Analogous to allGatherPrim_chunkPrim_eq but for dimension 0
  sorry

end TrainVerify.Denote.GeneratedGoals

