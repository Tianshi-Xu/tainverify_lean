/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 46 (tensor id: 167)
    SM: FW_multiref(121) → [159, 163, 167]
    PM: FW_multiref(121) → [156, 157, 158], ChunkPrim[dim=2](158) → [224-227]
    gatherDim = 2
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common

set_option linter.flexible false

namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_46 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.FW_multiref", ins := [121], outs := [159, 163, 167], params := [3] },
  ]

def pm_goal_46 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158], params := [3] },
    { rank := 1, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158], params := [3] },
    { rank := 2, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158], params := [3] },
    { rank := 3, op := "OpName.FW_multiref", ins := [121], outs := [156, 157, 158], params := [3] },
    { rank := 0, op := "OpName.ChunkPrim", ins := [158], outs := [224], params := [2] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [158], outs := [225], params := [2] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [158], outs := [226], params := [2] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [158], outs := [227], params := [2] },
  ]

def sm_goal_46InitShapes : List (Tid × Shape) := [
  (121, [16, 64, 128]),
]

def sm_goal_46InitEnv : ShapeEnv := shapeEnvOfList sm_goal_46InitShapes

def pm_goal_46InitShapes : List (Tid × Shape) := [
  (121, [16, 64, 128]),
]

def pm_goal_46InitEnv : ShapeEnv := shapeEnvOfList pm_goal_46InitShapes

def goal_46_cut_initGoals : List LineageGoal := initGoals

def goal_46_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_46 pm_goal_46 goal_46 sm_goal_46InitEnv pm_goal_46InitEnv goal_46_cut_initGoals

/-! ## Helper lemmas for gather-chunk roundtrip on dim 2 with shape [16, 64, 128] -/

private lemma valAt_gd2_16_64_32 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 64, 32])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 2 4 0 xs) idx =
    valAt (xs.getD (idx % 128 / 32) (zeroTensor [16, 64, 32]))
      (idx / 128 * 32 + idx % 32) := by
  have hshape_out : (allGatherPrimDimN 2 4 0 xs).shape = [16, 64, 128] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt : idx < prodShape (allGatherPrimDimN 2 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some,
    show (32 : Nat) * 4 = 128 from by norm_num,
    show (1 : Nat) * 32 = 32 from by norm_num,
    show (128 : Nat) ≠ 0 from by omega,
    show (32 : Nat) ≠ 0 from by omega,
    show (1 : Nat) ≠ 0 from by omega,
    ite_false,
    show ∀ n, n % 128 % 32 = n % 32 from fun n => by omega,
    Nat.div_one, Nat.mod_one, Nat.mul_one, Nat.add_zero]

private lemma valAt_chunk2 (x : Tensor) (r idx : Nat)
    (hshape : x.shape = [16, 64, 128]) (hidx : idx < 32768) :
    valAt (chunkPrimDimN 2 4 r x) idx =
    valAt x (idx / 32 * 128 + (r % 4) * 32 + idx % 32) := by
  simp only [chunkPrimDimN, hshape, List.getD, List.getElem?_cons_zero,
    List.getElem?_cons_succ,
    Option.getD, List.drop, List.foldl, List.set,
    valAt, Tensor.mkShape,
    show prodShape [16, 64, 32] = 32768 from by simp [prodShape],
    show prodShape [16, 64, 128] = 131072 from by simp [prodShape],
    show (128 : Nat) / 4 = 32 from by norm_num,
    show (1 : Nat) * 32 = 32 from by norm_num,
    show (128 : Nat) * 1 = 128 from by norm_num,
    show (4 : Nat) ≠ 0 from by omega,
    show (32 : Nat) ≠ 0 from by omega,
    show (1 : Nat) ≠ 0 from by omega,
    ite_false, Nat.mul_one, Nat.div_one, Nat.mod_one,
    Nat.add_zero, Nat.add_assoc]
  rw [dif_pos hidx]

private theorem gather_chunk_dim2 (T : Tensor)
    (hT : T.shape = [16, 64, 128]) :
    allGatherPrimDimN 2 4 0 [chunkPrimDimN 2 4 0 T, chunkPrimDimN 2 4 1 T,
      chunkPrimDimN 2 4 2 T, chunkPrimDimN 2 4 3 T] = T := by
  have hchunk_shape : ∀ r, (chunkPrimDimN 2 4 r T).shape = [16, 64, 32] := by
    intro r; rw [chunkPrimDimN_shape 2 4 r _ _ hT (by omega)]; simp [List.set, List.getD]
  have hhead : (([chunkPrimDimN 2 4 0 T, chunkPrimDimN 2 4 1 T,
      chunkPrimDimN 2 4 2 T, chunkPrimDimN 2 4 3 T].head?.map (·.shape)).getD []) =
      [16, 64, 32] := by
    simp [List.head?, Option.map, hchunk_shape 0]
  apply Tensor.ext
  · rw [allGatherPrimDimN_shape 2 4 _ _ hhead]; simp [List.set, List.getD, hT]
  · intro idx hidx
    have hidx' : idx < 131072 := by
      rw [allGatherPrimDimN_shape 2 4 _ _ hhead] at hidx
      simp [List.set, List.getD, prodShape] at hidx; omega
    rw [valAt_gd2_16_64_32 _ idx hhead hidx']
    set p := idx % 128 / 32 with hp_def
    have hp_range : p = 0 ∨ p = 1 ∨ p = 2 ∨ p = 3 := by omega
    have hlocal_bound : idx / 128 * 32 + idx % 32 < 32768 := by omega
    rcases hp_range with h | h | h | h <;>
      simp only [h, List.getD, List.getElem?_cons_zero, List.getElem?_cons_succ,
        Option.getD] <;>
      rw [valAt_chunk2 T _ _ hT hlocal_bound] <;>
      exact congrArg (valAt T) (by omega)

/-! ## Main proof -/

theorem prove_goal_46_cut : goal_46_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract initGoal_121: tensor 121 is replicated
  have hInit121 : InitGoalHolds pm_goal_46.numRanks initGoal_121 initSM initPM := by
    apply hInitGoals; simp [goal_46_cut_initGoals, initGoals]
  have ⟨h121_shape, h121_eq⟩ := initGoalHolds_replicated 4 initGoal_121 121
    [16, 64, 128] initSM initPM hInit121 rfl rfl rfl
  have h121_pm_shape : (initPM 121).shape = [16, 64, 128] := by rw [← h121_eq]; exact h121_shape
  -- SM evaluation: smStore 167 = initSM 121
  have hsm : (denoteGraph sm_goal_46 initSM) 167 = initSM 121 := by
    simp [sm_goal_46, denoteGraph, applyNode, evalOp, storeSet]
  -- PM evaluation: pmStore 224-227 = chunkPrimDimN 2 4 r (initPM 121)
  have hpm224 : (denoteGraph pm_goal_46 initPM) 224 = chunkPrimDimN 2 4 0 (initPM 121) := by
    simp [pm_goal_46, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm225 : (denoteGraph pm_goal_46 initPM) 225 = chunkPrimDimN 2 4 1 (initPM 121) := by
    simp [pm_goal_46, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm226 : (denoteGraph pm_goal_46 initPM) 226 = chunkPrimDimN 2 4 2 (initPM 121) := by
    simp [pm_goal_46, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  have hpm227 : (denoteGraph pm_goal_46 initPM) 227 = chunkPrimDimN 2 4 3 (initPM 121) := by
    simp [pm_goal_46, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- Chunk shapes
  have hchunk_shape : ∀ r, (chunkPrimDimN 2 4 r (initPM 121)).shape = [16, 64, 32] := by
    intro r; rw [chunkPrimDimN_shape 2 4 r _ _ h121_pm_shape (by omega)]
    simp [List.set, List.getD]
  -- Reconstruction = allGatherPrimDimN (gatherDim = 2)
  have hrecon : reconstructWithDim 2 4 0
      [chunkPrimDimN 2 4 0 (initPM 121), chunkPrimDimN 2 4 1 (initPM 121),
       chunkPrimDimN 2 4 2 (initPM 121), chunkPrimDimN 2 4 3 (initPM 121)] =
      allGatherPrimDimN 2 4 0
      [chunkPrimDimN 2 4 0 (initPM 121), chunkPrimDimN 2 4 1 (initPM 121),
       chunkPrimDimN 2 4 2 (initPM 121), chunkPrimDimN 2 4 3 (initPM 121)] := by
    apply reconstructWithDim_cons_cons_nonscalar
    rw [hchunk_shape]; decide
  -- Gather-chunk roundtrip
  have hroundtrip : allGatherPrimDimN 2 4 0
      [chunkPrimDimN 2 4 0 (initPM 121), chunkPrimDimN 2 4 1 (initPM 121),
       chunkPrimDimN 2 4 2 (initPM 121), chunkPrimDimN 2 4 3 (initPM 121)] = initPM 121 :=
    gather_chunk_dim2 (initPM 121) h121_pm_shape
  -- Prove three conjuncts
  dsimp only [goal_46_stmt_cut, CoarseLineageHoldsWithInit, goal_46]
  simp only [List.map]
  rw [hsm, hpm224, hpm225, hpm226, hpm227]
  simp only [show pm_goal_46.numRanks = 4 from rfl]
  rw [hrecon, hroundtrip, h121_eq]
  exact ⟨by rw [← h121_eq]; exact h121_shape, by simp [hchunk_shape], rfl⟩

end TrainVerify.Denote.GeneratedGoals
