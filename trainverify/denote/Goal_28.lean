/- Auto-generated by Verdict/graph_to_lean.py
    Goal: 28 (tensor id: 132)
-/
import denote.GeneratedData
import denote.Common

open TrainVerify.Denote
open TrainVerify.Denote.Generated
open TrainVerify.Denote.Common


namespace TrainVerify.Denote.GeneratedGoals

def sm_goal_28 : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.BW_transpose", ins := [133, 105], outs := [132], params := [1, 2] },
  ]

def pm_goal_28 : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.ChunkPrim", ins := [105], outs := [276], params := [2] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [105], outs := [277], params := [2] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [105], outs := [278], params := [2] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [105], outs := [279], params := [2] },
    { rank := 0, op := "OpName.BW_transpose", ins := [293, 276], outs := [292], params := [1, 2] },
    { rank := 1, op := "OpName.BW_transpose", ins := [295, 277], outs := [294], params := [1, 2] },
    { rank := 2, op := "OpName.BW_transpose", ins := [297, 278], outs := [296], params := [1, 2] },
    { rank := 3, op := "OpName.BW_transpose", ins := [299, 279], outs := [298], params := [1, 2] },
    { rank := 0, op := "OpName.AllGatherPrim", ins := [292, 294, 296, 298], outs := [132], params := [2] },
  ]

def sm_goal_28InitShapes : List (Tid × Shape) := [
  (105, [16, 64, 8, 16]),
  (133, [16, 8, 64, 16]),
]

def sm_goal_28InitEnv : ShapeEnv := shapeEnvOfList sm_goal_28InitShapes

def pm_goal_28InitShapes : List (Tid × Shape) := [
  (105, [16, 64, 8, 16]),
  (293, [16, 2, 64, 16]),
  (295, [16, 2, 64, 16]),
  (297, [16, 2, 64, 16]),
  (299, [16, 2, 64, 16]),
]

def pm_goal_28InitEnv : ShapeEnv := shapeEnvOfList pm_goal_28InitShapes

def goal_28_cut_initGoals : List LineageGoal := initGoals ++ goal_28_prereqs

def goal_28_stmt_cut : Prop :=
  CoarseLineageHoldsWithInit sm_goal_28 pm_goal_28 goal_28 sm_goal_28InitEnv pm_goal_28InitEnv goal_28_cut_initGoals

/-! ### Helper A: valAt of transposeAxes 1 2 for shape [16, 8, 64, 16] -/
private lemma valAt_ta12_16_8_64_16 (x : Tensor) (idx : Nat)
    (hshape : x.shape = [16, 8, 64, 16]) (hidx : idx < 131072) :
    valAt (transposeAxes 1 2 x) idx =
    valAt x (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
             idx % 8192 / 128 * 16 + idx % 16) := by
  have hinner : idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
      idx % 8192 / 128 * 16 + idx % 16 < 131072 := by omega
  have hps_in : prodShape x.shape = 131072 := by simp [hshape, prodShape]
  have hout_shape : (transposeAxes 1 2 x).shape = [16, 64, 8, 16] := by
    simp [transposeAxes, Tensor.mkShape, listSwapAt, hshape]
  have hps_out : prodShape (transposeAxes 1 2 x).shape = 131072 := by
    rw [hout_shape]; simp [prodShape]
  rw [valAt_of_lt _ _ (by rw [hps_out]; exact hidx),
      valAt_of_lt _ _ (by rw [hps_in]; exact hinner)]
  unfold transposeAxes Tensor.mkShape
  simp only [hshape, listSwapAt, flatToMulti, valAt, List.getD, List.set,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  have ps1 := show prodShape [8, 64, 16] = 8192 from by simp [prodShape]
  have ps2 := show prodShape [64, 16] = 1024 from by simp [prodShape]
  have ps3 := show prodShape [16] = 16 from by simp [prodShape]
  have ps4 := show prodShape ([] : List Nat) = 1 from by simp [prodShape]
  simp only [ps3, ps4,
    (show (16 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    Nat.div_one, ite_false]
  have hmm1 : ∀ n, n % 8192 % 128 = n % 128 := fun n => by omega
  have hinner' : idx / 8192 * 8192 + (idx % 128 / 16 * 1024 +
      (idx % 8192 / 128 * 16 + idx % 16)) < 131072 := by omega
  simp [hmm1, List.set, Option.getD_some, multiToFlat, prodShape, dif_pos hinner']
  simp only [Nat.add_assoc]

/-! ### Helper B: valAt of transposeAxes 1 2 for shape [16, 2, 64, 16] -/
private lemma valAt_ta12_16_2_64_16 (x : Tensor) (fi : Nat)
    (hshape : x.shape = [16, 2, 64, 16]) (hfi : fi < 32768) :
    valAt (transposeAxes 1 2 x) fi =
    valAt x (fi / 2048 * 2048 + fi % 32 / 16 * 1024 +
             fi % 2048 / 32 * 16 + fi % 16) := by
  have hinner : fi / 2048 * 2048 + fi % 32 / 16 * 1024 +
      fi % 2048 / 32 * 16 + fi % 16 < 32768 := by omega
  have hps_in : prodShape x.shape = 32768 := by simp [hshape, prodShape]
  have hout_shape : (transposeAxes 1 2 x).shape = [16, 64, 2, 16] := by
    simp [transposeAxes, Tensor.mkShape, listSwapAt, hshape]
  have hps_out : prodShape (transposeAxes 1 2 x).shape = 32768 := by
    rw [hout_shape]; simp [prodShape]
  rw [valAt_of_lt _ _ (by rw [hps_out]; exact hfi),
      valAt_of_lt _ _ (by rw [hps_in]; exact hinner)]
  unfold transposeAxes Tensor.mkShape
  simp only [hshape, listSwapAt, flatToMulti, valAt, List.getD, List.set,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  have ps1 := show prodShape [2, 64, 16] = 2048 from by simp [prodShape]
  have ps2 := show prodShape [64, 16] = 1024 from by simp [prodShape]
  have ps3 := show prodShape [16] = 16 from by simp [prodShape]
  have ps4 := show prodShape ([] : List Nat) = 1 from by simp [prodShape]
  simp only [ps3, ps4,
    (show (16 : Nat) ≠ 0 by omega), (show (1 : Nat) ≠ 0 by omega),
    Nat.div_one, ite_false]
  have hmm1 : ∀ n, n % 2048 % 32 = n % 32 := fun n => by omega
  have hinner' : fi / 2048 * 2048 + (fi % 32 / 16 * 1024 +
      (fi % 2048 / 32 * 16 + fi % 16)) < 32768 := by omega
  simp [hmm1, List.set, Option.getD_some, multiToFlat, prodShape, dif_pos hinner']
  simp only [Nat.add_assoc]

/-! ### Helper C: valAt of allGatherPrimDimN 1 4 0 for shard shape [16, 2, 64, 16] -/
private lemma valAt_ag1_4_sh16_2_64_16 (xs : List Tensor) (idx : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 2, 64, 16])
    (hidx : idx < 131072) :
    valAt (allGatherPrimDimN 1 4 0 xs) idx =
    valAt (xs.getD (idx % 8192 / 2048) (zeroTensor [16, 2, 64, 16]))
      (idx / 8192 * 2048 + (idx % 8192 / 1024 % 2) * 1024 + idx % 1024) := by
  have hshape_out : (allGatherPrimDimN 1 4 0 xs).shape = [16, 8, 64, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : idx < prodShape (allGatherPrimDimN 1 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 64 = 64 from by norm_num,
    show (64 : Nat) * 16 = 1024 from by norm_num,
    show (2 : Nat) * 4 = 8 from by norm_num,
    show (8 : Nat) * 1024 = 8192 from by norm_num,
    show (2 : Nat) * 1024 = 2048 from by norm_num,
    (show (8192 : Nat) ≠ 0 by omega), (show (1024 : Nat) ≠ 0 by omega),
    (show (2 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 8192 / 1024 / 2 = n % 8192 / 2048 from fun n => by omega,
    show ∀ n, n % 8192 % 1024 = n % 1024 from fun n => by omega]

/-! ### Helper D: valAt of allGatherPrimDimN 2 4 0 for shard shape [16, 64, 2, 16] -/
private lemma valAt_ag2_4_sh16_64_2_16 (xs : List Tensor) (fi : Nat)
    (hhead : (xs.head?.map (·.shape)).getD [] = [16, 64, 2, 16])
    (hfi : fi < 131072) :
    valAt (allGatherPrimDimN 2 4 0 xs) fi =
    valAt (xs.getD (fi % 128 / 32) (zeroTensor [16, 64, 2, 16]))
      (fi / 128 * 32 + (fi % 128 / 16 % 2) * 16 + fi % 16) := by
  have hshape_out : (allGatherPrimDimN 2 4 0 xs).shape = [16, 64, 8, 16] := by
    simp [allGatherPrimDimN, Tensor.mkShape, hhead]
  have hlt_prod : fi < prodShape (allGatherPrimDimN 2 4 0 xs).shape := by
    simp only [hshape_out, prodShape, List.foldl, Nat.one_mul]; omega
  rw [valAt_of_lt _ _ hlt_prod]
  simp only [allGatherPrimDimN, Tensor.mkShape, hhead,
    List.getD, List.drop, List.foldl,
    List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
  simp only [show (1 : Nat) * 16 = 16 from by norm_num,
    show (2 : Nat) * 4 = 8 from by norm_num,
    show (8 : Nat) * 16 = 128 from by norm_num,
    show (2 : Nat) * 16 = 32 from by norm_num,
    (show (128 : Nat) ≠ 0 by omega), (show (16 : Nat) ≠ 0 by omega),
    (show (2 : Nat) ≠ 0 by omega),
    ite_false]
  simp only [show ∀ n, n % 128 / 16 / 2 = n % 128 / 32 from fun n => by omega,
    show ∀ n, n % 128 % 16 = n % 16 from fun n => by omega]

/-! ### Extracted arithmetic lemmas for commutativity theorem -/
private lemma g28_swap_bound (idx : Nat) (_ : idx < 131072) :
    idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
    idx % 8192 / 128 * 16 + idx % 16 < 131072 := by omega

private lemma g28_rhs_bound (idx : Nat) (_ : idx < 131072) :
    idx / 128 * 32 + (idx % 128 / 16 % 2) * 16 + idx % 16 < 32768 := by omega

private lemma g28_piece_eq (idx : Nat) (_ : idx < 131072) :
    (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
     idx % 8192 / 128 * 16 + idx % 16) % 8192 / 2048 = idx % 128 / 32 := by
  have : idx % 128 / 16 * 1024 + idx % 8192 / 128 * 16 + idx % 16 < 8192 := by omega
  have : idx % 8192 / 128 * 16 + idx % 16 < 1024 := by omega
  have : idx % 128 / 16 ≤ 7 := by omega
  simp only [Nat.add_assoc]; omega

private lemma g28_h1a (idx : Nat) (_ : idx < 131072) :
    (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
     idx % 8192 / 128 * 16 + idx % 16) / 8192 = idx / 8192 := by
  have : idx % 128 / 16 * 1024 + idx % 8192 / 128 * 16 + idx % 16 < 8192 := by omega
  simp only [Nat.add_assoc]; omega

private lemma g28_h1b (idx : Nat) (_ : idx < 131072) :
    (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
     idx % 8192 / 128 * 16 + idx % 16) % 8192 / 1024 % 2 = idx % 128 / 16 % 2 := by
  have : idx % 128 / 16 * 1024 + idx % 8192 / 128 * 16 + idx % 16 < 8192 := by omega
  have : idx % 8192 / 128 * 16 + idx % 16 < 1024 := by omega
  simp only [Nat.add_assoc]
  have h_mod : (idx / 8192 * 8192 + (idx % 128 / 16 * 1024 +
      (idx % 8192 / 128 * 16 + idx % 16))) % 8192 =
      idx % 128 / 16 * 1024 + (idx % 8192 / 128 * 16 + idx % 16) := by omega
  rw [h_mod]
  have h_div : (idx % 128 / 16 * 1024 + (idx % 8192 / 128 * 16 + idx % 16)) / 1024 =
      idx % 128 / 16 := by omega
  rw [h_div]

private lemma g28_h1c (idx : Nat) (_ : idx < 131072) :
    (idx / 8192 * 8192 + idx % 128 / 16 * 1024 +
     idx % 8192 / 128 * 16 + idx % 16) % 1024 = idx % 8192 / 128 * 16 + idx % 16 := by
  have : idx % 8192 / 128 * 16 + idx % 16 < 1024 := by omega
  have : idx % 128 / 16 ≤ 7 := by omega
  simp only [Nat.add_assoc]; omega

private lemma g28_h2a (idx : Nat) (_ : idx < 131072) :
    (idx / 128 * 32 + (idx % 128 / 16 % 2) * 16 + idx % 16) / 2048 = idx / 8192 := by omega

private lemma g28_h2b (idx : Nat) (_ : idx < 131072) :
    (idx / 128 * 32 + (idx % 128 / 16 % 2) * 16 + idx % 16) % 32 / 16 =
    idx % 128 / 16 % 2 := by omega

private lemma g28_h2c (idx : Nat) (_ : idx < 131072) :
    (idx / 128 * 32 + (idx % 128 / 16 % 2) * 16 + idx % 16) % 2048 / 32 =
    idx % 8192 / 128 := by omega

private lemma g28_h2d (idx : Nat) (_ : idx < 131072) :
    (idx / 128 * 32 + (idx % 128 / 16 % 2) * 16 + idx % 16) % 16 = idx % 16 := by omega

/-! ### Main commutativity: transposeAxes 1 2 ∘ allGatherPrimDimN 1 = allGatherPrimDimN 2 ∘ transposeAxes 1 2 -/
private theorem transposeAxes_12_allGatherPrimDimN_1_comm
    (g0 g1 g2 g3 : Tensor)
    (h0 : g0.shape = [16, 2, 64, 16])
    (h1 : g1.shape = [16, 2, 64, 16])
    (h2 : g2.shape = [16, 2, 64, 16])
    (h3 : g3.shape = [16, 2, 64, 16]) :
    transposeAxes 1 2 (allGatherPrimDimN 1 4 0 [g0, g1, g2, g3]) =
    allGatherPrimDimN 2 4 0 [transposeAxes 1 2 g0, transposeAxes 1 2 g1,
                              transposeAxes 1 2 g2, transposeAxes 1 2 g3] := by
  have hhead_in : (([g0, g1, g2, g3].head?.map (fun t => t.shape)).getD []) = [16, 2, 64, 16] := by
    simp [List.head?, Option.map, h0]
  have hLHS_gathered_shape : (allGatherPrimDimN 1 4 0 [g0, g1, g2, g3]).shape = [16, 8, 64, 16] := by
    rw [allGatherPrimDimN_shape 1 4 [g0, g1, g2, g3] [16, 2, 64, 16] hhead_in]
    simp [List.getD, List.set]
  have hLHS_shape : (transposeAxes 1 2 (allGatherPrimDimN 1 4 0 [g0, g1, g2, g3])).shape = [16, 64, 8, 16] := by
    simp [transposeAxes, Tensor.mkShape, listSwapAt, hLHS_gathered_shape]
  have ht_shape : ∀ t, t.shape = [16, 2, 64, 16] → (transposeAxes 1 2 t).shape = [16, 64, 2, 16] := by
    intro t ht; simp [transposeAxes, Tensor.mkShape, listSwapAt, ht]
  have hhead_out : (([transposeAxes 1 2 g0, transposeAxes 1 2 g1, transposeAxes 1 2 g2, transposeAxes 1 2 g3].head?.map (fun t => t.shape)).getD []) = [16, 64, 2, 16] := by
    simp [List.head?, Option.map, ht_shape g0 h0]
  have hRHS_shape : (allGatherPrimDimN 2 4 0 [transposeAxes 1 2 g0, transposeAxes 1 2 g1, transposeAxes 1 2 g2, transposeAxes 1 2 g3]).shape = [16, 64, 8, 16] := by
    rw [allGatherPrimDimN_shape 2 4 _ [16, 64, 2, 16] hhead_out]
    simp [List.getD, List.set]
  apply Tensor.ext (by rw [hLHS_shape, hRHS_shape])
  intro idx hidx
  replace hidx : idx < 131072 := by
    rw [hLHS_shape] at hidx; simpa [prodShape] using hidx
  -- LHS: transpose ∘ gather
  rw [valAt_ta12_16_8_64_16 _ idx hLHS_gathered_shape hidx]
  rw [valAt_ag1_4_sh16_2_64_16 _ _ hhead_in (g28_swap_bound idx hidx)]
  -- RHS: gather ∘ transpose
  rw [valAt_ag2_4_sh16_64_2_16 _ idx hhead_out hidx]
  -- Piece index equality
  rw [g28_piece_eq idx hidx]
  -- Case-split on piece index
  have hr_cases : idx % 128 / 32 = 0 ∨ idx % 128 / 32 = 1 ∨
      idx % 128 / 32 = 2 ∨ idx % 128 / 32 = 3 := by omega
  rcases hr_cases with h | h | h | h <;>
  · simp only [h, List.getD,
      List.getElem?_cons_zero, List.getElem?_cons_succ, Option.getD_some]
    rw [valAt_ta12_16_2_64_16 _ _ (by first | exact h0 | exact h1 | exact h2 | exact h3)
        (g28_rhs_bound idx hidx)]
    exact congr_arg (valAt _) (by
      rw [g28_h1a idx hidx, g28_h1b idx hidx, g28_h1c idx hidx,
          g28_h2a idx hidx, g28_h2b idx hidx, g28_h2c idx hidx, g28_h2d idx hidx]; ring)

theorem prove_goal_28_cut : goal_28_stmt_cut := by
  intro initSM initPM hSmInit hPmInit hInitGoals
  -- Extract prerequisite goals
  have hInit133 : InitGoalHolds pm_goal_28.numRanks goal_29 initSM initPM := by
    apply hInitGoals; simp [goal_28_cut_initGoals, goal_28_prereqs, initGoals]
  have hInit105 : InitGoalHolds pm_goal_28.numRanks goal_7 initSM initPM := by
    apply hInitGoals; simp [goal_28_cut_initGoals, goal_28_prereqs, initGoals]
  -- Extract shapes from goal_29 (sharded on dim 1)
  have h133 := initGoalHolds_sharded4 4 goal_29 133 293 295 297 299
    [16, 8, 64, 16] [16, 2, 64, 16] initSM initPM hInit133 rfl rfl rfl rfl
  obtain ⟨h133_sm_shape, h293_shape, h295_shape, h297_shape, h299_shape, h133_rec⟩ := h133
  -- Extract shapes from goal_7 (replicated)
  have ⟨h105_shape, h105_eq⟩ := initGoalHolds_replicated pm_goal_28.numRanks
    goal_7 105 [16, 64, 8, 16] initSM initPM hInit105 rfl rfl rfl
  have h105_pm_shape : (initPM 105).shape = [16, 64, 8, 16] := by rw [← h105_eq]; exact h105_shape
  -- Convert reconstruction to allGatherPrimDimN 1
  have h133_ag : initSM 133 = allGatherPrimDimN 1 4 0
      [initPM 293, initPM 295, initPM 297, initPM 299] := by
    rw [h133_rec]
    apply reconstructWithDim_cons_cons_nonscalar
    rw [h293_shape]; decide
  -- SM store evaluation
  have hsm : (denoteGraph sm_goal_28 initSM) 132 = transposeAxes 1 2 (initSM 133) := by
    simp [sm_goal_28, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- PM store evaluation
  have hpm : (denoteGraph pm_goal_28 initPM) 132 =
      allGatherPrimDimN 2 4 0 [transposeAxes 1 2 (initPM 293), transposeAxes 1 2 (initPM 295),
                                transposeAxes 1 2 (initPM 297), transposeAxes 1 2 (initPM 299)] := by
    simp [pm_goal_28, denoteGraph, List.foldl, applyNode, evalOp, storeSet]
  -- Unfold goal
  dsimp only [goal_28_stmt_cut, CoarseLineageHoldsWithInit, goal_28] at *
  simp only [List.map]
  rw [hsm, hpm, h133_ag]
  rw [transposeAxes_12_allGatherPrimDimN_1_comm _ _ _ _ h293_shape h295_shape h297_shape h299_shape]
  refine ⟨?_, ?_, ?_⟩
  · -- Shape of SM output
    simp [allGatherPrimDimN, Tensor.mkShape, transposeAxes, listSwapAt,
      h293_shape, List.head?, Option.map]
  · -- Shape of PM output
    simp [allGatherPrimDimN, Tensor.mkShape, transposeAxes, listSwapAt,
      h293_shape, List.head?, Option.map]
  · -- Value equality (singleton reconstruction)
    rw [reconstructWithDim_singleton]

end TrainVerify.Denote.GeneratedGoals
