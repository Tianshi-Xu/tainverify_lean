/- Auto-generated by Verdict/graph_to_lean.py
    Module: trainverify.denote.GeneratedData
-/
import denote.Denote

set_option linter.style.longLine false
set_option linter.style.nativeDecide false

open TrainVerify.Denote

namespace TrainVerify.Denote.Generated

def sm : GraphDecl := by
  refine { numRanks := 1, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.FW_linear", ins := [20, 16], outs := [17] },
    { rank := 0, op := "OpName.FW_sum", ins := [17], outs := [15] },
    { rank := 0, op := "OpName.BW_sum", ins := [25, 17], outs := [24] },
    { rank := 0, op := "OpName.BW_linear", ins := [24, 20, 16], outs := [21, 23] },
  ]

def pm : GraphDecl := by
  refine { numRanks := 4, nodes := ?_ }
  exact [
    { rank := 0, op := "OpName.ChunkPrim", ins := [20], outs := [26] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [20], outs := [27] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [20], outs := [28] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [20], outs := [29] },
    { rank := 0, op := "OpName.FW_linear", ins := [26, 30], outs := [34] },
    { rank := 1, op := "OpName.FW_linear", ins := [27, 31], outs := [35] },
    { rank := 2, op := "OpName.FW_linear", ins := [28, 32], outs := [36] },
    { rank := 3, op := "OpName.FW_linear", ins := [29, 33], outs := [37] },
    { rank := 0, op := "OpName.AllReducePrim", ins := ((List.range 4).map (fun r => 34 + r)), outs := [17] },
    { rank := 0, op := "OpName.ChunkPrim", ins := [17], outs := [54] },
    { rank := 1, op := "OpName.ChunkPrim", ins := [17], outs := [55] },
    { rank := 2, op := "OpName.ChunkPrim", ins := [17], outs := [56] },
    { rank := 3, op := "OpName.ChunkPrim", ins := [17], outs := [57] },
    { rank := 0, op := "OpName.FW_sum", ins := [54], outs := [58] },
    { rank := 0, op := "OpName.BW_sum", ins := [25, 54], outs := [70] },
    { rank := 1, op := "OpName.FW_sum", ins := [55], outs := [59] },
    { rank := 1, op := "OpName.BW_sum", ins := [25, 55], outs := [71] },
    { rank := 2, op := "OpName.FW_sum", ins := [56], outs := [60] },
    { rank := 2, op := "OpName.BW_sum", ins := [25, 56], outs := [72] },
    { rank := 3, op := "OpName.FW_sum", ins := [57], outs := [61] },
    { rank := 3, op := "OpName.BW_sum", ins := [25, 57], outs := [73] },
    { rank := 0, op := "OpName.AllGatherPrim", ins := ((List.range 4).map (fun r => 70 + r)), outs := [24] },
    { rank := 0, op := "OpName.BW_linear", ins := [24, 26, 30], outs := [46, 47] },
    { rank := 1, op := "OpName.BW_linear", ins := [24, 27, 31], outs := [48, 49] },
    { rank := 2, op := "OpName.BW_linear", ins := [24, 28, 32], outs := [50, 51] },
    { rank := 3, op := "OpName.BW_linear", ins := [24, 29, 33], outs := [52, 53] },
  ]

def smInitShapes : List (Tid × Shape) := [
  (16, [128, 128]),
  (20, [128, 128]),
  (25, [1]),
]

def smInitEnv : ShapeEnv := shapeEnvOfList smInitShapes

def pmInitShapes : List (Tid × Shape) := [
  (20, [128, 128]),
  (25, [1]),
  (30, [128, 32]),
  (31, [128, 32]),
  (32, [128, 32]),
  (33, [128, 32]),
]

def pmInitEnv : ShapeEnv := shapeEnvOfList pmInitShapes

def initGoal_16 : LineageGoal :=
  { ts := 16, tsShape := [128, 128], tps := [{ rank := 0, tid := 30 }, { rank := 1, tid := 31 }, { rank := 2, tid := 32 }, { rank := 3, tid := 33 }], tpShapes := [[128, 32], [128, 32], [128, 32], [128, 32]] }

def initGoal_20 : LineageGoal :=
  { ts := 20, tsShape := [128, 128], tps := [{ rank := 0, tid := 20 }], tpShapes := [[128, 128]] }

def initGoal_25 : LineageGoal :=
  { ts := 25, tsShape := [1], tps := [{ rank := 0, tid := 25 }], tpShapes := [[1]] }

def initGoals : List LineageGoal := [initGoal_16, initGoal_20, initGoal_25]

def obsTids : List Nat := [15, 21, 23]

def goal_15 : LineageGoal :=
  { ts := 15, tsShape := [1], tps := [{ rank := 0, tid := 58 }, { rank := 1, tid := 59 }, { rank := 2, tid := 60 }, { rank := 3, tid := 61 }], tpShapes := [[1], [1], [1], [1]] }

def goal_21 : LineageGoal :=
  { ts := 21, tsShape := [128, 128], tps := [{ rank := 0, tid := 46 }, { rank := 1, tid := 48 }, { rank := 2, tid := 50 }, { rank := 3, tid := 52 }], tpShapes := [[128, 32], [128, 32], [128, 32], [128, 32]] }

def goal_23 : LineageGoal :=
  { ts := 23, tsShape := [128, 128], tps := [{ rank := 0, tid := 47 }, { rank := 1, tid := 49 }, { rank := 2, tid := 51 }, { rank := 3, tid := 53 }], tpShapes := [[128, 32], [128, 32], [128, 32], [128, 32]] }

def goals : List LineageGoal := [goal_15, goal_21, goal_23]

-- Auto shape/dimension checks (decidable, fail-fast)

def smShapeCheck : Except String (List (Tid × Shape)) :=
  TrainVerify.Denote.graphShapesCheck sm smInitShapes

def pmShapeCheck : Except String (List (Tid × Shape)) :=
  TrainVerify.Denote.graphShapesCheck pm pmInitShapes

theorem smShapeCheck_ok : smShapeCheck.isOk := by
  native_decide

theorem smShapeCheck_exists : ∃ m, smShapeCheck = Except.ok m := by
  exact (TrainVerify.Denote.Except.isOk_iff_exists smShapeCheck).1 smShapeCheck_ok

theorem pmShapeCheck_ok : pmShapeCheck.isOk := by
  native_decide

theorem pmShapeCheck_exists : ∃ m, pmShapeCheck = Except.ok m := by
  exact (TrainVerify.Denote.Except.isOk_iff_exists pmShapeCheck).1 pmShapeCheck_ok

theorem sm_denoteGraph_unfold (init : Store) :
    denoteGraph sm init =
      applyNode sm (applyNode sm (applyNode sm (applyNode sm (init) ({ rank := 0, op := "OpName.FW_linear", ins := [20, 16], outs := [17] })) ({ rank := 0, op := "OpName.FW_sum", ins := [17], outs := [15] })) ({ rank := 0, op := "OpName.BW_sum", ins := [25, 17], outs := [24] })) ({ rank := 0, op := "OpName.BW_linear", ins := [24, 20, 16], outs := [21, 23] }) := by
  simp [sm, denoteGraph]

-- Intermediate tensor lineage goals (shared by multiple output goals)
def intermediateGoal_17 : LineageGoal :=
  { ts := 17, tsShape := [128, 128], tps := [{ rank := 0, tid := 34 }, { rank := 1, tid := 35 }, { rank := 2, tid := 36 }, { rank := 3, tid := 37 }], tpShapes := [[128, 128], [128, 128], [128, 128], [128, 128]] }

def intermediateGoal_24 : LineageGoal :=
  { ts := 24, tsShape := [128, 128], tps := [{ rank := 0, tid := 24 }, { rank := 1, tid := 24 }, { rank := 2, tid := 24 }, { rank := 3, tid := 24 }], tpShapes := [[128, 128], [128, 128], [128, 128], [128, 128]] }

def intermediateGoals : List LineageGoal := [intermediateGoal_17, intermediateGoal_24]

-- Statements for intermediate goals (can be proved independently)
def intermediateGoal_17_stmt : Prop :=
  CoarseLineageHoldsWithInit sm pm intermediateGoal_17 smInitEnv pmInitEnv initGoals

def intermediateGoal_24_stmt : Prop :=
  CoarseLineageHoldsWithInit sm pm intermediateGoal_24 smInitEnv pmInitEnv initGoals

def goal_15_stmt : Prop :=
  CoarseLineageHoldsWithInit sm pm goal_15 smInitEnv pmInitEnv initGoals

-- goal_21 depends on intermediate tensors: [17]
def goal_21_prereqs : List LineageGoal := [intermediateGoal_17]

def goal_21_stmt_incremental : Prop :=
  CoarseLineageHoldsWithIntermediates sm pm goal_21 smInitEnv pmInitEnv initGoals goal_21_prereqs

def goal_21_stmt : Prop :=
  CoarseLineageHoldsWithInit sm pm goal_21 smInitEnv pmInitEnv initGoals

-- goal_23 depends on intermediate tensors: [17, 24]
def goal_23_prereqs : List LineageGoal := [intermediateGoal_17, intermediateGoal_24]

def goal_23_stmt_incremental : Prop :=
  CoarseLineageHoldsWithIntermediates sm pm goal_23 smInitEnv pmInitEnv initGoals goal_23_prereqs

def goal_23_stmt : Prop :=
  CoarseLineageHoldsWithInit sm pm goal_23 smInitEnv pmInitEnv initGoals

def all_goals_stmt : Prop :=
  ∀ g ∈ goals, CoarseLineageHoldsWithInit sm pm g smInitEnv pmInitEnv initGoals

/-!
## Auto-generated PM tid computation lemmas

These lemmas unfold the PM graph to show what each PM-computed tid evaluates to.
For init tids, we use `init tid` directly; for computed tids, we reference
the intermediate store using `denoteGraph pm init tid`.
-/

-- PM tid 17 is computed by node 8: OpName.AllReducePrim
theorem pm_tid_17_eq (init : Store) :
    (denoteGraph pm init) 17 = allReducePrim pm.numRanks 0 [denoteGraph pm init 34, denoteGraph pm init 35, denoteGraph pm init 36, denoteGraph pm init 37] := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 24 is computed by node 21: OpName.AllGatherPrim
theorem pm_tid_24_eq (init : Store) :
    (denoteGraph pm init) 24 = allGatherPrim pm.numRanks 0 [denoteGraph pm init 70, denoteGraph pm init 71, denoteGraph pm init 72, denoteGraph pm init 73] := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 26 is computed by node 0: OpName.ChunkPrim
theorem pm_tid_26_eq (init : Store) :
    (denoteGraph pm init) 26 = chunkPrim pm.numRanks 0 (init 20) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 27 is computed by node 1: OpName.ChunkPrim
theorem pm_tid_27_eq (init : Store) :
    (denoteGraph pm init) 27 = chunkPrim pm.numRanks 1 (init 20) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 28 is computed by node 2: OpName.ChunkPrim
theorem pm_tid_28_eq (init : Store) :
    (denoteGraph pm init) 28 = chunkPrim pm.numRanks 2 (init 20) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 29 is computed by node 3: OpName.ChunkPrim
theorem pm_tid_29_eq (init : Store) :
    (denoteGraph pm init) 29 = chunkPrim pm.numRanks 3 (init 20) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 34 is computed by node 4: OpName.FW_linear
theorem pm_tid_34_eq (init : Store) :
    (denoteGraph pm init) 34 = fw_linear (denoteGraph pm init 26) (init 30) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 35 is computed by node 5: OpName.FW_linear
theorem pm_tid_35_eq (init : Store) :
    (denoteGraph pm init) 35 = fw_linear (denoteGraph pm init 27) (init 31) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 36 is computed by node 6: OpName.FW_linear
theorem pm_tid_36_eq (init : Store) :
    (denoteGraph pm init) 36 = fw_linear (denoteGraph pm init 28) (init 32) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 37 is computed by node 7: OpName.FW_linear
theorem pm_tid_37_eq (init : Store) :
    (denoteGraph pm init) 37 = fw_linear (denoteGraph pm init 29) (init 33) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 46 is computed by node 22: OpName.BW_linear
theorem pm_tid_46_eq (init : Store) :
    (denoteGraph pm init) 46 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 26) (init 30)).1 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 47 is computed by node 22: OpName.BW_linear
theorem pm_tid_47_eq (init : Store) :
    (denoteGraph pm init) 47 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 26) (init 30)).2 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 48 is computed by node 23: OpName.BW_linear
theorem pm_tid_48_eq (init : Store) :
    (denoteGraph pm init) 48 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 27) (init 31)).1 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 49 is computed by node 23: OpName.BW_linear
theorem pm_tid_49_eq (init : Store) :
    (denoteGraph pm init) 49 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 27) (init 31)).2 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 50 is computed by node 24: OpName.BW_linear
theorem pm_tid_50_eq (init : Store) :
    (denoteGraph pm init) 50 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 28) (init 32)).1 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 51 is computed by node 24: OpName.BW_linear
theorem pm_tid_51_eq (init : Store) :
    (denoteGraph pm init) 51 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 28) (init 32)).2 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 52 is computed by node 25: OpName.BW_linear
theorem pm_tid_52_eq (init : Store) :
    (denoteGraph pm init) 52 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 29) (init 33)).1 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 53 is computed by node 25: OpName.BW_linear
theorem pm_tid_53_eq (init : Store) :
    (denoteGraph pm init) 53 = (bw_linear (denoteGraph pm init 24) (denoteGraph pm init 29) (init 33)).2 := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 54 is computed by node 9: OpName.ChunkPrim
theorem pm_tid_54_eq (init : Store) :
    (denoteGraph pm init) 54 = chunkPrim pm.numRanks 0 (denoteGraph pm init 17) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 55 is computed by node 10: OpName.ChunkPrim
theorem pm_tid_55_eq (init : Store) :
    (denoteGraph pm init) 55 = chunkPrim pm.numRanks 1 (denoteGraph pm init 17) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 56 is computed by node 11: OpName.ChunkPrim
theorem pm_tid_56_eq (init : Store) :
    (denoteGraph pm init) 56 = chunkPrim pm.numRanks 2 (denoteGraph pm init 17) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 57 is computed by node 12: OpName.ChunkPrim
theorem pm_tid_57_eq (init : Store) :
    (denoteGraph pm init) 57 = chunkPrim pm.numRanks 3 (denoteGraph pm init 17) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 58 is computed by node 13: OpName.FW_sum
theorem pm_tid_58_eq (init : Store) :
    (denoteGraph pm init) 58 = fw_sum (denoteGraph pm init 54) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 59 is computed by node 15: OpName.FW_sum
theorem pm_tid_59_eq (init : Store) :
    (denoteGraph pm init) 59 = fw_sum (denoteGraph pm init 55) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 60 is computed by node 17: OpName.FW_sum
theorem pm_tid_60_eq (init : Store) :
    (denoteGraph pm init) 60 = fw_sum (denoteGraph pm init 56) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 61 is computed by node 19: OpName.FW_sum
theorem pm_tid_61_eq (init : Store) :
    (denoteGraph pm init) 61 = fw_sum (denoteGraph pm init 57) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 70 is computed by node 14: OpName.BW_sum
theorem pm_tid_70_eq (init : Store) :
    (denoteGraph pm init) 70 = bw_sum (init 25) (denoteGraph pm init 54) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 71 is computed by node 16: OpName.BW_sum
theorem pm_tid_71_eq (init : Store) :
    (denoteGraph pm init) 71 = bw_sum (init 25) (denoteGraph pm init 55) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 72 is computed by node 18: OpName.BW_sum
theorem pm_tid_72_eq (init : Store) :
    (denoteGraph pm init) 72 = bw_sum (init 25) (denoteGraph pm init 56) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

-- PM tid 73 is computed by node 20: OpName.BW_sum
theorem pm_tid_73_eq (init : Store) :
    (denoteGraph pm init) 73 = bw_sum (init 25) (denoteGraph pm init 57) := by
  sorry  -- Proof: unfold denoteGraph, apply to nodes up to this tid

/-!
## Auto-generated SM tid computation lemmas

These lemmas show what each SM tid computes in terms of the init store.
-/

theorem sm_tid_15_eq (init : Store) :
    (denoteGraph sm init) 15 = fw_sum (denoteGraph sm init 17) := by
  simp only [sm_denoteGraph_unfold, applyNode, evalOp, storeSet, List.map, List.zip]
  rfl

theorem sm_tid_17_eq (init : Store) :
    (denoteGraph sm init) 17 = fw_linear (init 20) (init 16) := by
  simp only [sm_denoteGraph_unfold, applyNode, evalOp, storeSet, List.map, List.zip]
  rfl

theorem sm_tid_21_eq (init : Store) :
    (denoteGraph sm init) 21 = (bw_linear (denoteGraph sm init 24) (init 20) (init 16)).1 := by
  simp only [sm_denoteGraph_unfold, applyNode, evalOp, storeSet, List.map, List.zip]
  rfl

theorem sm_tid_23_eq (init : Store) :
    (denoteGraph sm init) 23 = (bw_linear (denoteGraph sm init 24) (init 20) (init 16)).2 := by
  simp only [sm_denoteGraph_unfold, applyNode, evalOp, storeSet, List.map, List.zip]
  rfl

theorem sm_tid_24_eq (init : Store) :
    (denoteGraph sm init) 24 = bw_sum (init 25) (denoteGraph sm init 17) := by
  simp only [sm_denoteGraph_unfold, applyNode, evalOp, storeSet, List.map, List.zip]
  rfl

/-!
## PM init tids

The following tids are PM init tids (not written by any PM node): [20, 25, 30, 31, 32, 33]

To prove `(denoteGraph pm init) tid = init tid` for an init tid,
use `denoteGraph_tid_eq_of_forall_not_mem_outs` with `native_decide`
to show no node outputs that tid.
-/

/-!
## Auto-generated shape lemmas for PM goal tids

These lemmas state the shapes of PM outputs used in lineage goals.
-/

-- All PM outputs for goal_15 have shape [1]
theorem pm_goal_15_shapes (init : Store)
    (hInit : StoreShapesHold init pmInitEnv) :
    [(denoteGraph pm init 58).shape, (denoteGraph pm init 59).shape, (denoteGraph pm init 60).shape, (denoteGraph pm init 61).shape] =
    [[1], [1], [1], [1]] := by
  sorry  -- requires shape propagation through PM graph

-- All PM outputs for goal_21 have shape [128, 32]
theorem pm_goal_21_shapes (init : Store)
    (hInit : StoreShapesHold init pmInitEnv) :
    [(denoteGraph pm init 46).shape, (denoteGraph pm init 48).shape, (denoteGraph pm init 50).shape, (denoteGraph pm init 52).shape] =
    [[128, 32], [128, 32], [128, 32], [128, 32]] := by
  sorry  -- requires shape propagation through PM graph

-- All PM outputs for goal_23 have shape [128, 32]
theorem pm_goal_23_shapes (init : Store)
    (hInit : StoreShapesHold init pmInitEnv) :
    [(denoteGraph pm init 47).shape, (denoteGraph pm init 49).shape, (denoteGraph pm init 51).shape, (denoteGraph pm init 53).shape] =
    [[128, 32], [128, 32], [128, 32], [128, 32]] := by
  sorry  -- requires shape propagation through PM graph

/-!
## Incremental Proof Strategy

The goals have the following dependency structure:
- goal_15 has no prerequisites (base case)
- goal_21 depends on intermediate tensors: [17]
- goal_23 depends on intermediate tensors: [17, 24]

To prove `goal_X_stmt` from `goal_X_stmt_incremental`, use
`CoarseLineageHoldsWithInit_of_incremental` with proofs of the prerequisite
intermediate goals.
-/

theorem goal_21_of_incremental
    (hincr : goal_21_stmt_incremental)
    (hintermediateGoal_17 : intermediateGoal_17_stmt)
    : goal_21_stmt := by
  unfold goal_21_stmt goal_21_stmt_incremental at *
  apply CoarseLineageHoldsWithInit_of_incremental
  · exact hincr
  · intro g hg
    simp only [goal_21_prereqs, List.mem_cons, List.mem_nil_iff] at hg
    cases hg with
    | inl h => subst h; exact hintermediateGoal_17
    | inr h => exact False.elim h

theorem goal_23_of_incremental
    (hincr : goal_23_stmt_incremental)
    (hintermediateGoal_17 : intermediateGoal_17_stmt)
    (hintermediateGoal_24 : intermediateGoal_24_stmt)
    : goal_23_stmt := by
  unfold goal_23_stmt goal_23_stmt_incremental at *
  apply CoarseLineageHoldsWithInit_of_incremental
  · exact hincr
  · intro g hg
    simp only [goal_23_prereqs, List.mem_cons, List.mem_nil_iff] at hg
    cases hg with
    | inl h => subst h; exact hintermediateGoal_17
    | inr hg =>
      cases hg with
      | inl h => subst h; exact hintermediateGoal_24
      | inr h => exact False.elim h

end TrainVerify.Denote.Generated

